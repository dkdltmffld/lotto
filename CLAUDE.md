# 새 게임 프로젝트

## 개요
- **엔진**: Godot 4.6.3-stable (official)
- **프로젝트 경로**: `E:/projects/새-게임-프로젝트/`
- **MCP 연동**: godot-mcp 2.17.0 (addon/server 버전 일치, 정상 연결)
- **장르/컨셉**: 횡스크롤 아케이드 액션 — "스크래치 복권을 긁어 공격하는 게임"
- **아트 스타일**: **2D 도트(픽셀 아트)** — 현재 단계는 더미 위주
- **방향/디바이스**: **모바일 세로 (Portrait, 9:19.5)** — 최신 폰 위주
- **타겟 플랫폼**: **HTML5 (Web Export)** ⚠️ 가장 중요한 제약 — 모든 설계·기술 선택은 웹 호환성을 전제로 함

## 아트/렌더링 설정 (모바일 세로)
- **내부 해상도**: **360×780** (9:19.5 세로)
- **개발 윈도우**: 432×936 (1.2배, 데스크탑 1080p 모니터에 맞춤)
- **Stretch Mode**: `canvas_items`
- **Stretch Aspect**: `keep` — 디자인(360×780) 전체를 비율 유지하며 화면에 맞춰 확대/축소, 안 맞는 부분은 레터박스(여백). 세로 폰에선 여백 거의 없고, 가로/4:3 등에선 좌우/상하 여백. **모든 비율에서 게임 전체가 항상 정상 표시**됨(세로 모바일 게임 표준). 입력·배치 모두 네이티브로 안정.
- **Orientation**: `1` (SCREEN_PORTRAIT) — 모바일 디바이스에서 세로 강제
- **텍스처 필터**: `nearest` (전역 기본값, 픽셀 룩 유지용 — 필요시 노드별 `Linear` 오버라이드)
- **픽셀 스냅**: ❌ **사용 안 함** (스프라이트 이미지 사용이라 픽셀-퍼펙트 강제 불필요, 서브픽셀 위치/움직임 허용)

### 화면 영역 가이드 (360×780)
- 상단 안전영역(상태바·노치): ~상단 60px는 비워두거나 UI만 배치
- 하단 안전영역(홈 인디케이터): ~하단 40px도 동일
- 실제 게임 영역: 대략 360×680 (중앙 부분)
- 가로 폭이 좁으므로 UI는 세로 스택, 큰 버튼(터치) 위주로 디자인

### 에셋·UI 제작 가이드
- 스프라이트 이미지를 그대로 사용 — 픽셀 그리드 정렬 강제 없음, 자유로운 크기·움직임 가능
- 터치 버튼 권장 최소 크기: 44×44px (내부 해상도 기준)
- 새 PNG 임포트 시 자동으로 nearest 필터 적용 (픽셀 룩 유지)
- 특정 노드만 부드럽게 하고 싶으면 그 노드의 `texture_filter`를 `Linear`로 오버라이드
- 위치·회전·스케일에 소수점 값 사용 OK (스냅 없으므로 부드러운 트윈·이동 가능)

## 디렉토리 구조
```
새-게임-프로젝트/
├── addons/             # Godot 애드온 (godot-mcp 포함)
├── scenes/             # login.tscn / main.tscn, scratch_card / enemy / hp_bar
├── scripts/            # GDScript — 컨트롤러/헬퍼/데이터 (balance, upgrades, game, scratch_card, enemy, player_controller, upgrade_panel, audio_manager, game_data …)
│   └── backend/        # BackendService(autoload) + LocalBackendProvider(구글/게스트 로컬 슬롯)
├── data/               # 데이터 주도 설정 — src/*.yaml(원본·주석) → *.json(게임 로드, GameData)
│   └── src/            # YAML 원본 (balance/upgrades/achievements/enemies) — 변환은 tools/build_data.gd
├── assets/             # sprites/ backgrounds/ ui/ fonts/ audio/(sfx·bgm, 더미) (픽셀 아트 + 한글 폰트)
├── tools/              # build_web.ps1(웹 빌드, 기본 debug export) + serve.py(로컬 서버) + build_data.gd(YAML→JSON 변환)
│   └── tests/          # 상설 테스트 (test_*.gd 5종 + run_tests.gd 통합 러너 — godot --headless --script 로 실행)
├── docs/design/        # 기획서 정리본 (게임 컨셉·스테이지·전투·스크래치·강화·오프라인/방치·UI·백엔드·로그인·설정)
├── icon.svg            # 프로젝트 아이콘
├── project.godot       # Godot 프로젝트 파일 (autoload: BackendService, Audio, MCPGameBridge)
├── export_presets.cfg  # Web export preset (thread_support=false)
└── CLAUDE.md           # 이 파일
```

> 씬·스크립트·리소스가 추가되면 이 섹션도 업데이트할 것.

## 메인 씬
`res://scenes/login.tscn` (로그인 게이트) → 계정 선택 후 `res://scenes/main.tscn`(게임)으로 전환.
씬 전환은 `login.gd`/`game.gd`의 `_goto_scene()` 헬퍼(현재는 `change_scene_to_file`로 폴백) 사용.
로그인 기획: `docs/design/로그인 시스템 정리본.md`.

## 게임 컨셉 (확정)
> 상세 기획서: `docs/design/게임 컨셉 정리본.md` (+ 스테이지/전투/스크래치/UI/백엔드/로그인/일시정지 정리본). 아래는 요약.

### 한 줄 요약
자동으로 긁히는 복권으로 싸우는 횡스크롤 **방치형(idle)**. 골드로 복권·캐릭터를 키워 무한 스테이지 진행. 10단위 보스에서만 사망(블록 첫 스테이지로 복귀).

### 코어 루프
복권 항상 긁힘(방치=느림 / 능동=손가락 속도) → 9칸 → 공격 → 적 처치 → 골드 → 강화 → 더 센 적(스테이지) → 반복.

### 전투력 / 성장
- **데미지 = 캐릭터 기본공격력 × 복권 족보배율** (곱연산)
- 성장축(단일 골드): **캐릭터**(공격력 + HP/생존력) / **복권**(배율·족보·잭팟·숫자·등급·자동긁기속도[상한]) / **저장소**(오프라인 누적 상한)

### 스크래치 / 족보
- **3×3 고정.** 각 칸 1~9 무작위. 같은 숫자 많을수록·높을수록 강함(동점 시 높은 숫자). 자동/능동 통합 — 운(족보)은 동일, 능동은 throughput만 ↑. 수동 입력 후 2초간 자동 정지.

### 스테이지 / 실패
- 무한 스테이지, 시스템 배율 스케일(적 HP·골드 = 기본 × 성장률^s). **블록=10 + 10단위 보스.** 일반 적도 PC를 약하게 공격(보스보다 빈도·데미지 현저히 낮음), **보스가 치명적**(사망 시 블록 첫 스테이지로, HP 풀충전). **게임오버 없음.**

### 방치 / 경제
- 자동 긁기로 방치 진행(속도 상한). 오프라인 누적(금액 상한=저장소 업글 압박). 저장=강화 등 이산 이벤트 즉시 flush + 주기/생애주기. 랭킹 비활성, 환생 보류.

### 조작
- 모바일 터치 (데스크탑/웹은 마우스 드래그). 손가락이 지나간 칸 공개(드래그).

## 작업 규칙
- **씬/노드 편집**: `mcp__godot-mcp__scene`, `mcp__godot-mcp__node` 사용
- **2D/3D**: 장르 결정 시 명시
- **스크립트 파일**: GDScript는 `Read`/`Edit`/`Write` 도구로 직접 편집
- **에디터 상태 확인**: `mcp__godot-mcp__editor` (스크린샷·로그·에러 확인)
- **공식 문서**: `mcp__godot-mcp__godot_docs`로 조회
- ⚠️ **외부 공개 배포는 사용자가 명시적으로 "배포해"라고 할 때만 실행.** 현재 프로젝트에는 업로드 제공자·CLI·배포 스크립트가 없다. `tools/build_web.ps1`로 로컬 웹 빌드는 가능하지만 사용자 요청 없이 어떤 업로드도 하지 않는다.
- **AI 협업 브릿지 (Codex와 소통)**: 사용자가 "브릿지 확인/남겨"라고 하면 `.ai-bridge/thread.md`를 읽고(상대=Codex의 최신 메시지 확인) 답을 **맨 끝에 append**한다(append-only, 상대 글 수정 금지). 형식·규칙은 `.ai-bridge/README.md`. 복붙 중계를 없애기 위한 로컬 우편함이며, 폴링하지 않고 사용자 트리거 시에만 확인한다.
  - ⚠️ **필수 규칙**: 사용자가 **"리뷰"·"검토"(또는 봐줘/확인/점검 등)를 요청하면**, 리뷰 수행 후 그 결과를 **반드시 `.ai-bridge/thread.md`에 `[Claude → Codex]` 메시지로 남긴다**(사용자 보고와 별개). Codex에게도 같은 지시가 되어 있어 양쪽 리뷰가 브릿지에 모여 교차 검증된다.
  - ⚠️ **브릿지에 글을 남길 때마다 사용자에게도 "무슨 내용으로 남겼는지" 설명을 함께 한다.** thread.md append로 끝내지 말 것 — 사용자는 매번 브릿지를 직접 열지 않으므로, 내가 Codex에게 뭐라고 했는지 요지를 보고한다.

## HTML5 타겟 제약사항 (필수 준수)
완성본을 HTML5로 빌드할 것이므로 아래 제약을 항상 고려해야 함:

- **스레딩 제한**: 웹 빌드는 메인 스레드 외 사용에 제약이 있음. `Thread`·`Mutex`·`Semaphore` 사용 시 반드시 웹에서 동작 검증
- **파일 시스템**: 웹에서는 `user://`만 사용 가능, `res://`는 읽기 전용. 일반 OS 경로 사용 불가
- **외부 프로세스/네이티브**: `OS.execute`, `GDExtension`, C# 사용 불가 (또는 매우 제한적)
- **오디오 포맷**: Ogg Vorbis 권장, MP3 사용 시 라이선스·디코딩 이슈 확인
- **에셋 사이즈**: 다운로드 시간에 직결되므로 텍스처·오디오 압축 적극 활용
- **입력**: 마우스·키보드·터치 모두 고려. 일부 키(Ctrl+W 등)는 브라우저가 가로챔
- **저장**: IndexedDB 기반이라 `user://` 데이터는 브라우저별로 격리됨
- **CORS**: 외부 HTTP 요청 시 서버 측 CORS 헤더 필요
- **렌더링**: WebGL 2 (Compatibility 렌더러) 권장. Forward+/Mobile은 WebGPU 필요해 호환성 떨어짐
- **마우스 잠금·풀스크린**: 사용자 입력(클릭 등) 이후에만 가능

## 결정 사항 (Decision Log)
- 2026-09-02: **분리 무기 표시 임시 비활성화.** `player_controller.gd`의 `SEPARATE_WEAPON_ENABLED=false`로 별도 `w_0001` Sprite 생성·표시를 중단하고 캐릭터 애니메이션 에셋에 포함된 기본 무기만 사용. 공격 판정·애니메이션 타이밍·장비 스탯은 유지.
- 2026-09-01: **기존 외부 플랫폼 연동 전면 제거.** 로그인 화면은 유지하되 구글/게스트 두 버튼만 남김. 둘 다 외부 인증 없이 게스트처럼 즉시 진입하며, `user://save.json`(구글)·`user://save_guest.json`(게스트) 로컬 슬롯으로 분리. 웹 Provider·인증·클라우드·랭킹·점수제출·배포 CLI/스크립트·프로젝트 메타데이터 제거. 이 결정이 아래의 연동·배포 관련 과거 기록을 모두 대체함.
- 2026-09-01: **PC 달리기·공격 아트 재제작.** `PC_idle`/`PC_attacked`/`PC_dead`의 상세 디자인을 기준으로 256px 규격의 8프레임 빈손 달리기 `PC_run_v2.png`와 2포즈 빈손 공격 `PC_attack_02_0.png`/`PC_attack_02_1.png`를 추가하고 런타임 경로를 교체. 기존 48px 원본은 보존했으며 분리 무기 앵커를 새 손 위치에 맞춤.
- 2026-05-27: 프로젝트 시작, 장기 세션 연속성을 위해 CLAUDE.md + 메모리 시스템 세팅
- 2026-05-27: **타겟 플랫폼을 HTML5로 확정** — 모든 기술 선택은 웹 호환성을 최우선으로 함
- 2026-05-27: 엔진·MCP·출력 검증용으로 홀덤 포커 테스트 진행 → 폐기. 본 작업 컨셉 결정 대기 중
- 2026-05-27: Web export preset 사용자가 직접 설정 완료 (`thread_support=false`)
- 2026-05-27: **아트 스타일 = 2D 픽셀 아트** (스프라이트 이미지 사용, 픽셀-퍼펙트 강제 없음)
- 2026-05-27: **디바이스/방향 = 모바일 세로 (9:19.5, 360×780)** — 가로 모드 미지원
- 2026-05-27: 본 작업 단계는 더미 에셋 우선, 진짜 아트 도입은 메커니즘 안정 후
- 2026-05-27: 픽셀 스냅 OFF — 스프라이트 이미지 사용이라 서브픽셀 위치·움직임 허용
- 2026-05-28: **Stretch Aspect를 `keep` → `expand`로 변경** — 다양한 폰 비율(9:16 ~ 9:21+)에 자연스럽게 적응. UI 노드들은 anchor 기반으로 재정렬(상단·하단 anchor 분리, Arena는 늘어남), Player·적의 Y 위치는 Arena 하단 기준 동적 계산
- 2026-05-29: **화면 적응 cover 시도 → keep으로 최종 확정.** cover(SubViewport+KEEP_ASPECT_COVERED)는 데스크탑 세로 비율에선 됐으나, **삭제된 외부 플랫폼 프리뷰가 가로(16:9) 프레임**이라 세로 게임이 폭 기준으로 과확대돼 HUD·스크래치가 다 잘리는 치명적 문제 발생(실기기 포함 대부분 해상도에서 깨짐). 세로 게임을 임의 비율(특히 가로)에서 올바르게 보이려면 cover/expand로는 불가 → **`keep`(레터박스)로 확정**. 게임 전체가 항상 정상 비율로 표시, 안 맞는 부분만 여백(세로 폰은 여백 미미). cover용 `stage.tscn`/`stage.gd` + content_scale_factor 방식 모두 폐기. (변천: keep→expand→keep_height→cover→**keep**) ※ 삭제된 외부 플랫폼 프레임이 가로면 좌우 여백 큼 — 삭제된 외부 플랫폼 게임 orientation을 세로로 설정 가능한지 확인 필요.
- 2026-05-29: **한글 폰트 번들** — 웹은 시스템 폰트 폴백이 없어 기본 폰트로 한글이 깨짐(□). `assets/fonts/korean.ttf`(Do Hyeon, OFL, ~860KB)를 추가하고 `project.godot [gui] theme/custom_font`로 전역 기본 폰트 지정. (픽셀 한글 폰트로 추후 교체 가능 — 더미 단계)
- 2026-05-29: **스크래치 웹 수정(2건)** — ①입력: `scratch_card.gd`가 터치 이벤트만 처리 → 마우스인 데스크탑/웹에서 안 긁힘 → `_gui_input`에 마우스(좌버튼 드래그) 처리 추가. ②크기: **웹에서 anchor 기반 `size`가 계속 (0,0)**으로 남아 격자가 안 만들어지고 영역이 안 보임(데스크탑은 260×240 정상). 콘솔 로그로 확인. → anchor 측정에 의존하지 않고 `ScratchCard.configure(grid_size)`로 레이아웃 상수(260×240)를 명시 지정해 빌드. `scratch_card.tscn` anchor를 top-left 고정으로 변경, `game.gd._setup_scratch_card_layout`가 configure 호출. 격자 빌드·입력 판정은 `_grid_size` 사용.
- 2026-05-29: **로그인 시스템 기획·구현** — 계정 3종(구글/삭제된 외부 플랫폼=인증 동일동작, 게스트=로컬·랭킹등록제한). 기동 시 로그인 게이트(`login.tscn`) 1회. 게스트→로그인 직접전환 불가(로그아웃=데이터 전부삭제 후 재선택). 로그인 유인=랭킹. **'리더보드'→'랭킹' 명칭 통일**(우리 API `show_ranking`, 삭제된 외부 플랫폼 프로토콜 문자열은 유지). 기획서 `docs/design/로그인 시스템 정리본.md`.
- 2026-05-29: **백엔드 = 삭제된 외부 플랫폼 플랫폼 SDK로 확정** — 리더보드·진행사항 저장(클라우드)·로그인을 직접 서버 구축 없이 삭제된 외부 플랫폼 SDK로 구현. 우리 HTML5 zip 배포 방식과 호환. 연동은 Godot `JavaScriptBridge` ↔ `삭제된 플랫폼 API`(postMessage), 웹 전용이라 `OS.has_feature("web")` 분기 + 비웹 `user://` 로컬 폴백. autoload `BackendService` 싱글톤으로 추상화 예정. 상세 규격은 메모리 `삭제된 플랫폼 백엔드 문서.md` 참조. (저장 한도: key≤128자/value≤10KB/게임당 100key·100KB, 기기간 동기화는 로그인 필수)
- 2026-06-01: **인게임 일시정지 추가** — 우측 상단 `PauseButton`("II") → `PausePanel`(전체화면 오버레이, `process_mode=ALWAYS`). 게임오버 시 PauseButton 숨김. 씬 전환 핸들러는 `paused=false` 선행 필수. 전용 기획서 `docs/design/일시정지 시스템 정리본.md`. (버튼 전부 임시 placeholder, 추후 UI 스프라이트 교체)
- 2026-06-01: **장르 피벗 확정 — 횡스크롤 시간압박 액션 → "복권을 키우는 방치형(idle)".** 스크래치 핵심 유지, 실패 조건 제거(HP·타이머·게임오버 폐기), 성장 메인=복권. 코어: 복권 항상 긁힘(자동=느림/능동=손가락속도, 운은 동일·throughput만 차이) → 9칸 → 공격 → 적 처치 → 골드 → 업그레이드. 전투력=캐릭터 기본공격력(베이스, 반복 도파민) × 복권 족보배율(폭발·운). 성장축: 캐릭터 공격력 / 복권(배율·족보·잭팟·숫자·등급 + 자동긁기속도[긴 텀·상한]) / 저장소 용량. 경제=단일 골드(적 처치+잭팟+오프라인), 3 sink 경쟁. 방치=능동 중심·보조, 상한 2개(자동속도 rate 상한 + 오프라인 누적 상한[금액기준=의도된 저장소 업글 압박]), 오프라인은 현재 스테이지 farming. 무한 스테이지=시스템 배율 스케일(적HP·골드=기본×성장률^s), N마리/스테이지+10단위 보스, 적 외형 cosmetic. 랭킹=비활성화만(코드 유지), 환생=보류. **전체 기획서 `docs/design/게임 컨셉 정리본.md`.** ⚠️ 기존 컨셉/전투/스크래치 문서·메모리·HP/게임오버 코드 대규모 재작업 예정(피벗 영향 §11).
- 2026-06-01: **일시정지 메뉴에 랭킹/로그아웃 추가 + "메인 메뉴" 명칭** — 최종 5버튼: 계속하기/다시하기/랭킹 보기/메인 메뉴/로그아웃. 기존 "로그인 화면으로"를 **"메인 메뉴"로 변경** — `login.tscn`이 사실상 메인 메뉴 역할을 겸한다(별도 메인 메뉴 화면 없음). 메인 메뉴=세션 유지하고 화면 이동(discard), 로그아웃=세션 리셋. 랭킹 보기는 이탈 아님(paused 유지). 노드 `ToLoginButton`→`RankingButton`/`MainMenuButton`/`LogoutButton`, 핸들러 `_on_main_menu_pressed`/`_on_pause_logout_pressed`(+랭킹은 게임오버와 공용 `_on_ranking_pressed`).
- 2026-06-01: **서버 저장 시점 정책 = 게임 종료(게임오버) 1회로 확정** — 기존엔 업적 해금이 `set_value`에서 즉시 flush됐으나, 한 판 진행분(점수·통계·업적)은 메모리(`_data`)에만 쌓고 게임오버 `flush()`에서만 영속화하도록 변경. `set_value` 즉시 flush 제거. `BackendService._baseline`(마지막 저장 스냅샷, `_load`/`flush` 직후 갱신) + `discard_session()`(미저장분 폐기) 추가. **다시하기·로그인 화면으로(게임오버 전 이탈)는 `discard_session()` 호출 → 현재 판 서버 미반영.** 기획서: 백엔드 §2, UI §10-2. ⚠️ **아래 2026-06-01 방치형 결정으로 대체됨.**
- 2026-06-01: **(방치형 후속) 스테이지 게이트 = 보스 사망/블록 재시작.** "실패 없음"의 구멍(아무리 센 적도 시간 지나면 클리어) 보완. **일반 적은 PC 위협 X(항상 클리어), 10단위 보스만 PC 공격**(공격 vs 생존 DPS 레이스). **블록=10스테이지**, 보스=10·20·30…. PC HP 0 시 **블록 첫 스테이지로**(보스10→1, 20→11, 30→21, 골드·강화 유지). 死스파이럴 없음(블록 잡몹 farming). **생존력(PC HP/방어)을 캐릭터 성장 축에 추가**(캐릭터=공격력+생존력). PC attacked/dead 상태는 보스전 한정 부활. 게임오버(영구 종료) 여전히 없음. 기획서: 컨셉 §9·§10, 전투 §2·§3-3·§4-1.
- 2026-06-01: **(방치형 후속) 저장 정책 = 이중 트리거 + "다시하기" 제거.** 게임오버가 없어져 위 "게임 종료 1회 저장"이 무효. → ① 업그레이드·강화·구매 등 골드 쓰는 **이산 이벤트 즉시 flush**, ② 연속 골드 수입은 **주기적(10~30초)+앱 종료/백그라운드+오프라인 정산 시** flush. 골드 수입마다 저장 X(삭제된 외부 플랫폼 클라우드 과호출 방지). 진행이 영구라 **`discard_session`/`_baseline` 폐기, 일시정지 "다시하기" 버튼 제거**(리셋은 환생으로만). 일시정지 메뉴 = 계속하기/랭킹 보기/메인 메뉴/로그아웃(4버튼). 기획서: 백엔드 §2, 일시정지 §3·§5.
- 2026-06-01: **방치형 코드 재작업 1차 완료 + 실행 검증.** 위 설계대로 코드 전면 교체: `balance.gd`(스테이지/블록/보스/골드 스케일), `upgrades.gd`(신설 — 캐릭터 공격력/HP·복권 배율/자동긁기속도·저장소 트랙+cost/value+buy), `backend_service.gd`(골드 get/add/spend·stage·오프라인 last_seen·이중 트리거 autosave·`discard_session`/`_baseline` 제거), `enemy.gd`(is_boss, 보스만 공격), `hand_evaluator.gd`(damage→mult), `scratch_card.gd`(자동 긁기 `auto_rate`), `game.gd`(스테이지/블록 루프·곱연산 데미지·골드·보스 PC공격→사망 시 블록 첫 스테이지로·게임오버/점수/타이머/다시하기 제거·강화 패널 토글), `player_controller.gd`(set_max_hp/set_hp_visible/revive, `Balance.PLAYER_MAX_HP` 의존 제거), `upgrade_panel.gd`(신설 — 코드 빌드 강화 오버레이). `main.tscn`: GameOverPanel·일시정지 RestartButton 제거, ScoreLabel→골드, StageLabel·UpgradeButton(강화) 추가. **에디터 실행 검증: 파싱·런타임 에러 0, 자동 긁기로 골드 누적·스테이지 진행 동작 확인(스크린샷).** ⚠️ 미검증(MCP 마우스 입력 불가): 수동 긁기·강화 패널 클릭·오프라인 정산 — 라이브 실행 시 확인 필요. 수치는 전부 잠정(밸런스 미튜닝).
- 2026-06-01: **기획서 정리 + 스테이지 별도 문서 분리.** 흩어져 있던 스테이지 내용을 **`docs/design/스테이지 시스템 정리본.md`** 신규로 분리(블록/보스/스케일링/사망·재시작/오프라인/성장 벽/데이터 위치). 전투 §4·게임 컨셉 §9는 요약+포인터로 축소. **CLAUDE.md "게임 컨셉(확정)" 본문을 방치형 현재 기준으로 재작성**(구 아케이드 설명 폐기). 디렉토리 구조에 tools/·문서 목록 갱신. 추가 검증: 보스 사망→블록 재시작·피격 플로터 잔여 버그 수정·보스 더미 외형(발밑 기준 확대) 실기 확인 완료. (테스트 플래그 `BOSS_ONESHOT_TEST`/`START_STAGE_TEST` 사용 — 정상 플레이 시 false/0)
- 2026-06-01: **일시정지 제거 → 설정(계정 메뉴) 전환.** 방치형이라 일시정지가 무의미 → `PauseButton`/`PausePanel` 제거, 같은 자리(우측 상단)에 `SettingsButton`("설정"). `scripts/settings_panel.gd`(코드 빌드 오버레이) 신설 — **메인 메뉴(계정 변경)/로그아웃/계정 탈퇴/닫기**(계정 관련만, 랭킹 제거). game.gd: pause/`get_tree().paused` 전부 제거, `_on_settings_pressed`/`_build_settings_panel`/`_on_main_menu_pressed`/`_on_logout_pressed`/`_on_delete_account_pressed`. 설정 패널은 게임 정지 안 함(방치 계속). 기획서 `일시정지 시스템 정리본.md`→**`설정 시스템 정리본.md`로 교체**, UI §10-2·로그인·백엔드 참조 갱신. 헤드리스 빌드로 컴파일 검증(에러 0). ⚠️ **에디터에 game.gd 열려 있으면 외부 편집이 run 시 덮어쓰일 수 있음** — 외부 수정 후 에디터에서 reload 필요.
- 2026-06-01: **강화 롱프레스 연타 + 긁기 범위(brush) 트랙 추가.** 강화 버튼을 2초 이상 꾹 누르면 0.06초 간격 연타(`upgrade_panel.gd` button_down/up + _process). **`Upgrades.buy`는 flush 안 하고(메모리+dirty), 손 뗄 때(`_on_buy_up`) `flush()` 1회** — 연타마다 flush하면 삭제된 외부 플랫폼 클라우드 폭주라서. 신규 트랙 `brush`(긁기 범위, 수동 `brush_radius` 30+5/Lv, 상한 Lv10) 구현 — game.gd가 `scratch_card.brush_radius`에 반영. 기획서 강화 §2·§5·§6, 스크래치 §7. 헤드리스 빌드 검증(에러 0).
- 2026-06-01: **개발용 치트 패널 추가.** `scripts/cheat_panel.gd`(코드 빌드) + game.gd `const CHEAT_ENABLED`(배포 시 false) → 좌상단 "치트" 버튼 → 패널: 골드 +1K/100K/10M, 스테이지 -1/+1/+10/다음 보스/1리셋, HP 풀 회복, 세이브 초기화. 동작은 game.gd `_cheat_*`. 헤드리스 빌드 컴파일 검증(에러 0). 배포 전 `CHEAT_ENABLED=false`.
- 2026-06-02: **게임 이름 확정 = "복권 키우기 : Idle RPG".** 기존 임시 타이틀 "스크래치 아케이드"(멋대로 지은 이름) 폐기. 반영: `scenes/login.tscn` Title 라벨, `project.godot config/name`(웹 export HTML 타이틀에도 적용). export 파일명(`scratch.html` 등)은 화면 노출 아니라 그대로 유지.
- 2026-06-02: **로그인 화면 개편.** ①게임 제목을 **텍스트 라벨→타이포그래피 이미지**로(`Logo` TextureRect, `assets/ui/title_logo.png` 연결 — 1536×1024, 312×200 슬롯에 비율 유지. `login.gd @export title_logo_texture`로 교체 가능). ②**로그인 배경 = 자체 배경 없음 + 투명 캔버스** — `get_tree().root.transparent_bg=true`(login `_ready`)로 캔버스를 투명하게 해 **웹 페이지 배경 `frame_bg.png`가 그대로 비치게** 함(삭제된 외부 플랫폼 와이드 프리뷰에서도 끊김 없이 한 장). `project.godot [display] window/per_pixel_transparency/allowed=true` 필요. **게임 씬은 `game.gd _ready`에서 `transparent_bg=false`로 되돌려 불투명 유지**(frame_bg 안 비침, 게임 배경 커버 여부와 무관하게 안전). ⚠️ 웹 전용 효과(에디터/데스크탑에선 frame_bg 없음). ③계정 버튼 3개를 **중앙→화면 하단**(`Bottom` VBox, 하단 anchor). 노드 경로 변경(`Center/VBox/*`→`Bottom/*`)에 맞춰 `login.gd` `@onready` 갱신. 기획서 로그인 §3.
- 2026-06-02: **에셋/빌드 정리 2건.** ①로고 용량: `title_logo.png`(1536×1024) import에 `process/size_limit=1024` 적용(비파괴적 다운스케일, 원본 유지, compress는 lossless 유지) → pck 경량화. ②`web/frame_bg.png` ↔ `assets/backgrounds/frame_bg.png` **UID 중복 경고 제거**: `web/`는 `build_web.ps1`이 raw 복사로 쓰는 빌드 소스(Godot 리소스 아님)라 폴더에 **`.gdignore`** 추가해 스캔/임포트 제외 + 중복 원인 `web/frame_bg.png.import` 삭제. (build의 Copy-Item은 Godot 리소스 시스템과 무관해 영향 없음)
- 2026-06-04: **첫 시작 튜토리얼 (스크립트형, 긁기~강화).** 등장 조건=저장 데이터 없음(`BackendService.is_fresh_save`, `_load`에서 raw 비었으면 true — 슬롯별 첫 시작, 계정 무관). 형식=**입력 안 막는 상단 배너**(`tutorial_overlay.gd` `MOUSE_FILTER_IGNORE`, `set_text`) + `game.gd` 상태머신(`_tut`: SCRATCH/JACKPOT/UPGRADE). 플로우: ①긁기(적 **고HP**=강화0 잭팟뎀 `5×1×729`×1.5≈5500 → 한 방에 안 죽음, 데미지 정상) 9칸 완성→ ②강제 잭팟(`set_number_pool([9])`)+완성 시 **강제 처치**(`take_damage(current_hp)`)→칭찬"대단합니다!"→ ③강화 버튼 깜빡임, 누르면 종료. ⚠️ 예외처리(사용자 요청): **1·2단계 자동긁기 OFF**(`_enter_idle`)+**적 PC공격 OFF**(`_on_enemy_reached_position`가 `_tut==OFF`일때만 `start_attacking`)+PC HP바 숨김. 데미지0 대신 고HP로 불사(자연스럽게). 코어 훅: `_spawn_next`(고HP)·`_on_player_attack_completed`(단계전환·강제처치)·`_on_enemy_died`(2단계 분기, 스테이지/통계 제외)·`_on_upgrade_pressed`(종료). 문구는 사용자 제공(TUT_MSG_1/2/3). 빌드 컴파일 OK. 기획서 `docs/design/튜토리얼 시스템 정리본.md`. ⚠️ 라이브 플레이 검증 필요(인터랙티브 — 배너 위치·플로우 실기 확인).
- 2026-06-04: **게스트/인증 저장 슬롯 분리.** 게스트와 인증 계정이 같은 키(`삭제된 외부 플랫폼:save_v1`)를 공유해 데이터가 섞이던 문제 → **슬롯 분리**. Provider에 `set_slot(slot, cloud_sync)` 추가: 인증(`auth`)=`삭제된 외부 플랫폼:save_v1`/`user://save.json`(클라우드 동기), 게스트(`guest`)=`삭제된 외부 플랫폼:save_guest`/`user://save_guest.json`(로컬 전용). `삭제된 웹 Provider`: 슬롯 키·`_cloud` 분기(게스트는 storage-set 미전송), `_js_helper` `save/clearLocal/clear`에 key 인자 추가. `local_provider`: 슬롯별 파일. `backend_service`: `continue_as_guest`(guest 슬롯+로드)·`commit_auth_session`(auth 슬롯+로드, 구 `set_account_type` 대체)·`_on_provider_login_changed`는 **account_type이 인증일 때만** auth 슬롯 전환(⚠️ 삭제된 외부 플랫폼 자동 user-info가 게스트 데이터를 덮지 않게 게이트). `login.gd`: `await continue_as_guest`/`await commit_auth_session`. 계정 선택/변경 시 해당 슬롯 재로드로 데이터 분리. 빌드 exit 0. 기획서 백엔드 §2 갱신. **구글/삭제된 외부 플랫폼 자체 계정 분리는 TODO 보류**(2026-06-04 논의: 현재 둘은 같은 삭제된 외부 플랫폼 플랫폼 신원이라 동일 데이터=정상. 사용자는 **자체 OAuth/백엔드로 진짜 분리할 의향**이나 큰 작업이라 보류 — 슬롯 구조는 이미 있으니 인증 슬롯을 provider별로 더 쪼개는 방향. 그 전까진 구글/삭제된 외부 플랫폼 동일 동작 유지).
- 2026-06-04: **삭제된 외부 플랫폼 자동 배포 스크립트 세팅 (`tools/삭제된 배포 스크립트`).** docs.삭제된 외부 플랫폼/guide/cli 기준: `npm i -g @삭제된 외부 플랫폼/cli`(Node 20+, v24 확인) → `삭제된 외부 플랫폼 login`(브라우저) → `삭제된 외부 플랫폼 update [zip|folder]`(폴더/zip 지원, 첫 실행 대화형으로 프로젝트 선택/생성 → `삭제된 배포 메타데이터`에 ID 저장 → 이후 비대화형 재사용). `-p <id>`/`--new --title --public` 플래그. 스크립트: 빌드(index.html)→배포 zip(index.* 9개+frame_bg.png)→`삭제된 외부 플랫폼 update`. 옵션 `-SkipBuild`/`-SkipUpload`/`-ProjectId`. 치트 ON 경고 포함. ⚠️ **배포는 사용자 명시 요청 시에만**(작업 규칙에 고정). CLI 설치·login·첫 대화형 update는 사용자가 직접 1회. **현재 CLI 미설치·삭제된 배포 메타데이터 없음** — 첫 배포 전 사용자 준비 필요.
- 2026-06-04: **계정 변경 버그 수정 (게스트→구글 등이 이전 계정 유지되던 문제).** 삭제된 외부 플랫폼는 게임 로드 시 `user-info`를 **자동 전송**(`삭제된 웹 Provider._js_helper`의 message 리스너)해 `is_logged_in`이 먼저 true가 됨(단 `account_type`은 GUEST/NONE 유지 — `_on_provider_login_changed`가 안 건드림). 그 상태에서 계정 변경 시 `login.gd._start_login`의 `if is_logged_in: _enter_game()` 단축 경로가 `begin_login`을 건너뛰어 **선택한 provider가 account_type에 반영 안 됨** → 우측 상단에 이전 계정(게스트) 유지. 수정: `BackendService.set_account_type(provider)` 추가 + `_start_login`의 is_logged_in 분기에서 호출(재인증 없이 표기만 갱신). 기획서 로그인 §3 갱신. 빌드 exit 0, zip 갱신.
- 2026-06-04: **로그인 배경 = 더미 흰색 (투명 캔버스 폐기).** 기존 투명 캔버스로 frame_bg 비추는 방식은 텍스트 대비가 약해 가독성 나쁨 → `login.tscn`에 `$Background`(ColorRect 전체화면 흰색) 추가, `login.gd _ready`의 `transparent_bg=true`→`false`. **추후 진짜 배경 이미지로 교체**(Background를 TextureRect로). ⚠️ 흰 배경이라 기본 밝은 Label 글자색이 안 보여 StatusLabel에 `font_color=Color(0.15,0.15,0.2)` 지정(버튼은 기본 스타일박스라 OK). 레터박스 여백은 여전히 frame_bg. 기획서 로그인 정리본 갱신. (2026-06-02 투명 캔버스 결정 폐기)
- 2026-06-04: **배포 캐시 버스팅 (`삭제된 배포 스크립트`).** Godot 웹은 `index.pck`/`.wasm`/`.js` 파일명이 매번 동일 → 재배포해도 브라우저/CDN(R2)이 옛 pck를 캐시에서 서빙(문구·치트 변경이 "반영 안 됨" 반복의 진짜 원인. ※진단: 배포 원본 pck 해시 == 로컬 빌드 == 일치 → 배포는 정상, 사용자측 캐시 확정). → **버전 토큰(빌드 unix초)으로 리소스 리네임**: `index.<v>.{js,wasm,pck,audio.worklet.js,audio.position.worklet.js}` + `index.html`의 `<script src>`·`GODOT_CONFIG.executable`·fileSizes 키 패치(로더가 `${executable}` 기반이라 executable만 바꾸면 전부 새 URL). 원본 `build/web`은 불변, 별도 `build/deploy`에 버전화(로컬 serve·`-SkipBuild` 안전). 새 URL이라 캐시에 없어 무조건 새로 받음 → 앞으로 일반 새로고침으로 반영. 검증: 배포 index.html이 버전참조·pck 200 OK. 진입 `index.html`만 고정(삭제된 외부 플랫폼 요구)이라 그 캐시는 강력새로고침/시크릿창으로 우회.
- 2026-06-04: **웹 export 진입 파일명 = `index.html` (삭제된 외부 플랫폼 404 수정).** 삭제된 외부 플랫폼에 `scratch.html` 진입으로 zip 업로드 시 `shares/<id>/index.html` **404** 발생(삭제된 외부 플랫폼가 진입점을 index.html로 강제). → `export_presets.cfg export_path=build/web/index.html`로 변경, 모든 산출물 `index.*`(index.js/wasm/pck/worklet/아이콘). `tools/build_web.ps1`(`$OutFile`)·`tools/serve.py`(`ENTRY`)도 index.html로 갱신. **배포 zip = `index.*` 9개 + `frame_bg.png`**(10개). 메모리 [[reference_html5_build]] 갱신. (2026-06-02 "scratch.html 유지" 결정은 이로써 폐기)
- 2026-06-04: **일반몹도 PC 공격(보스보다 현저히 약하게).** 기존 "일반 적은 위협 X, 보스만 공격" → **일반몹도 약하게 공격**(생동감). 다른 시스템(사망=블록 리셋·보스 게이트 등)은 유지. `balance.gd`에 `MOB_ATTACK_INTERVAL=2.6`/`MOB_ATK_BASE=1.0`/`MOB_ATK_GROWTH=1.12`/`MOB_ATTACK_WINDUP=0.35` + `mob_attack_damage(stage)` 추가(보스 대비 간격↑·데미지 ~1/6, 잠정). `game.gd`: `_spawn_next`가 일반몹에도 attack params 주입 + **PC HP바 항상 표시**(보스전 한정→상시), `_on_enemy_reached_position`가 보스·일반 모두 `start_attacking`, 보스 처치 후 HP바 숨김 제거. ⚠️ **즉사 루프 방지**: 일반몹이 공격하면 사망 후 HP 0인 채 재시작 시 즉사 반복 → `_on_player_died` 부활에 **HP 풀충전 추가**(기존엔 보스만 죽여서 드러나지 않던 누락). 회복 정책: 보스 진입 + 사망 재시작 시 풀충전, 블록 내(1~9)는 누적(HP 업글 동기). enemy.gd는 변경 없음(공격은 start_attacking으로 결정, is_boss는 외형 틴트/스케일에만). 헤드리스 빌드 exit 0. 기획서 전투 §1·§2·§4·스테이지 §4·컨셉 §9 갱신. 수치 잠정(밸런스 미튜닝).
- 2026-06-04: **몬스터 외형 무작위 변종 시스템.** 적 외형을 한 종류(mon_001) → **스폰마다 무작위 변종**(일반몹·보스 공통). **스탯·타이밍 불변, 외형만** 변경. 폴더 구조 `assets/sprites/enemies/<id>/<id>_idle.png`+`<id>_attack.png`(현재 mon_001~003, 사용자가 직접 정리·임포트). `enemy.gd` 전면 교체: `VARIANTS=["mon_001","mon_002","mon_003"]` 중 **`ResourceLoader.exists`로 로드 가능한 것만 추려 무작위**(⚠️ 웹 export에서 `DirAccess` 폴더 스캔 불안정 → 명시 카탈로그 방식). **프레임 폭 256 고정, 높이는 시트별 가변**(idle 256 / attack 256·320 혼재) → 프레임수=width/256, region=Rect2(i*256,0,256,height). **발끝(하단) 정렬**: BASE_H(256)보다 큰 프레임은 위로 올려(`sprite.offset.y`) idle↔attack·몬스터 간 바닥선 일치(가정: 추가 높이는 프레임 위쪽=발은 바닥). **attack speed는 windup에 맞춰 자동 계산**(프레임 수 무관). ⚠️ 사용자 우려였던 "피격 데미지 타이밍"은 원래부터 외형과 분리돼 안전 — PC가 적 때릴 때 데미지는 PC 애니 종료 기준(적 애니 무관), 보스가 PC 때릴 때 데미지는 `attack_windup` 고정 타이머(적 attack 프레임 수 무관). 헤드리스 웹 빌드 검증 exit 0, 변종 스프라이트 패킹(pck 14.2→15.3MB). 기획서 스테이지 §8·전투 §2 갱신. ⚠️ 라이브 무작위 외형/정렬 시각 확인은 실행 필요(MCP 시각 검증 한계). **(후속 수정)** 초기 발끝 정렬은 "추가 높이=프레임 위쪽, 발은 바닥" 가정의 **높이 기반 offset**이었으나, mon_003 attack은 **발 아래 패딩**(캐릭터가 프레임 위쪽)이라 정반대 → idle↔attack 전환 시 y가 튐. → **콘텐츠 스캔 방식으로 교체**: `get_image()`로 시트별 **발끝(불투명 최하단)·캐릭터 높이(프레임 bbox 높이)의 중앙값**을 스캔(`_scan_sheet`/`_first_opaque_row`, 경로별 static 캐시). ⚠️중앙값인 이유: 시트 전체 최하단을 쓰면 **한 프레임의 투사체**(mon_003 attack 프레임9에서 던진 뼈가 바닥 row 308까지, 나머지는 ~231)가 이상치가 돼 attack 전체가 ~77px 떠버림. + **크기 정규화**: 시트마다 캐릭터 그려진 크기가 달라(같은 몬스터 idle 186px↔attack 153px) 전환 시 커졌다 작아짐 → `norm = idle_char_h / sheet_char_h`로 `sprite.scale = 0.35×norm`(**idle 기준**, 몬스터 간 고유 크기는 유지) + scale 후에도 발끝 바닥선 일치하도록 `sprite.offset.y` 계산(`_anim_render={scale,offset_y}`, `_play_anim` 적용). 임포트 무손실(`compress/mode=0`)이라 웹 안전. 실측 검산: idle/attack 화면 캐릭터높이·발끝 동일. 빌드 exit 0.
- 2026-06-04: **리텐션 기획 + 사운드 기반작업.** ①일반 키우기 게임 대비 부족 요소 점검 → 가장 큰 공백은 **환생(프레스티지)·복권 등급**(구조적), 그다음 일일보상·퀘스트·업적UI(리텐션). 사용자가 **리텐션 묶음**부터 가기로 선택 → 기획서 `docs/design/리텐션 시스템 정리본.md` 작성(수령식 claim·뱃지·로컬 자정 리셋·보상=스테이지 스케일 골드 `Balance.reward_gold`). **단, 리텐션 코드 구현은 사용자가 기획서 검토 중이라 보류**(코드 미착수, `balance.reward_gold` 헬퍼만 선반영). ②**사운드 효과 기반작업 먼저 구축**: `scripts/audio_manager.gd`(autoload `Audio`, project.godot에 `BackendService` 다음 등록) 신설 — 버스(Master→BGM/SFX) **코드 셋업**(.tres 의존 없음), SFX 풀 8개 라운드로빈, `play_sfx`/`play_bgm`, **카탈로그(key→경로) + 파일 없으면 조용히 skip**(더미 단계, 1회만 로그 — ogg 넣기만 하면 자동 재생), 볼륨/음소거 → `BackendService` settings(`audio_*`)에 저장(`save_ready` 후 로드).
- 2026-06-05: **UI 스프라이트 스킨 도입 (Kenney Pixel UI, CC0).** `assets/ui/kenney_ui-pack-pixel-adventure`(Large/Thick 32px 타일 91종, CC0). 타일이 `tile_NNNN`로 번호만 있어 **워크플로우로 91타일 이미지 분류→UI 역할별 매핑**(8 에이전트). `scripts/ui_skin.gd`(UISkin, static 헬퍼) 신설 — 9-slice `StyleBoxTexture` 빌더 + `skin_button(kind: nav/buy/close/subtab)`·`skin_progress`·`panel/nav_bg/header`. 매핑: panel_bg=tile_0000(베이지), nav_bg=8, nav_button=16, buy=35(블루), buy_disabled=3, close=33(레드), bar_track=70/fill=57, header=44(레드리본), icon_frame=39. 적용: `nav_dock`(바=베이지 패널·탭=스킨버튼), `upgrade_panel`(베이지 패널+서브탭/구매/닫기 스킨, **밝은 패널→텍스트 어둡게 전환**: 골드=호박/이름=갈색/값=초록), `game.gd _build_stage_banner`(스테이지명=레드 리본 배너+Kenney 진행바). 라이브 검증: nav 탭·레드 리본·강화 패널(베이지+블루구매+레드닫기) 시각 확인. 버그픽스: `attack_speed` 플로터 소수표시(0→0 → 0.80→0.90). 빌드 exit 0. ⚠️구매버튼 블루는 골드 있을 때(affordable) 표시 — 검증 시 gold 0이라 회색(disabled). 아이콘(원형프레임 등)·HUD 골드칩 스킨은 후속. 픽셀 톤 통일=밝은 패널+어두운 글자. ⚠️**사용자가 Kenney "별로"라 자체 UI 에셋 제작 중 → Kenney는 placeholder, 추후 사용자 시트로 스왑**(시트=단일 PNG여도 `AtlasTexture` region으로 사용 가능. `ui_skin.gd`의 역할→텍스처만 교체하면 됨. 시트는 균일 그리드 또는 조각별 좌표 legend 받으면 자동 슬라이스).
- 2026-06-05: **자체 UI 시트 적용 (Kenney 대체).** 사용자 제작 `assets/ui/UI_sheet.png`(1024×1536, 다크 우드/판타지 RPG 톤) 적용. 단일 시트 → `AtlasTexture(region)`. 영역 좌표는 **`tools/scan_sheet.gd`**(불투명 연결요소 바운딩박스 자동추출, 27개 검출)로 뽑아 `ui_skin.gd`에 `R_*` Rect2 상수로 박음(역할 매핑 1차+인게임 보정): R_PANEL #13·R_NAV_BG #1·R_NAV_BTN #6·R_BUY #3(초록)·R_BUY_OFF #2·R_CLOSE #24·R_BAR_TRACK #26/R_FILL #22·R_HEADER #21(배너)·R_RING #20. 다크 톤이라 nav/패널 텍스트 다시 라이트로(크림·골드·초록). `ui_skin.gd` public API 동일(소스만 시트로). 라이브 검증: 나무 nav 탭·다크우드 스테이지 배너·강화 패널(우드 프레임+초록 구매+밝은 글자) 전부 시트 톤 통일 — Kenney보다 게임 배경과 정합. 빌드 exit 0. ⚠️Kenney 폴더는 미사용 잔존(삭제/exclude 가능). 9-slice 마진은 1차 추정값 — 조각별로 더 다듬을 여지.
- 2026-06-05: **번호 매긴 시트 뷰어 + 사용자 지정 UI 매핑 조정.** 사용자가 시트 조각을 "#번호"로 가리킬 수 있게 **`scenes/sheet_labeler.tscn`+`scripts/sheet_labeler.gd`**(시트에 외곽선+#번호 오버레이, `_draw()`로 시트·외곽선·번호 동일 스케일 정렬·중앙배치 — TextureRect 크기버그 회피. F6 실행 또는 MCP run). scan_sheet 컴포넌트 인덱스 = 라벨 번호. 사용자 지정 반영(`ui_skin.gd` R_*): **강화 행 아이콘 배경=#13**(`icon_slot()`, upgrade_panel `_build_row`가 PanelContainer로 아이콘 감쌈), **스크래치 영역 뒤 배경=#16**(`scratch_bg()`, game.gd `_build_scratch_bg` — 내비 아래~바닥 밴드, 티켓/카드 뒤로 move_child), **스테이지 배너=#2**(#4→#2, 글자 가로·세로 중앙 정렬+진행바는 프레임 아래로). 레이아웃 보정: 스크래치 콘텐츠 아래로(Y_OFFSET 69→88·TICKET_BG_Y 560→579), #16을 내비 아래 26px 겹쳐 올려 사이 빈공간 제거. 라이브 검증 완료. 빌드 exit 0. ⚠️UID 중복 사고: `sheet_labeler.tscn`이 scripts/·scenes/ 양쪽 생겨 에디터 FS스캔·game.gd 리로드 막음 → scripts/ 복제본 삭제+재임포트로 해결(에디터 켜진 채 외부편집 시 주의). 시트 변경 시: scan_sheet 재실행 → ui_skin R_* + labeler RECTS 갱신.
- 2026-06-05: **설정 버튼 #19 스킨 + 스테이지 진행 바 = 마스크 방식 재구현.** ①설정 버튼($TopHUD/SettingsButton)에 #19 스킨(`R_SETTINGS`, skin_button "settings", game.gd _ready). ②진행 바: ProgressBar의 fill 9-slice 크롭이 어색 → **#24 초록 바(NinePatchRect 가득) + 어두운 마스크(ColorRect) 덮고 진행도만큼 왼쪽부터 걷는** 방식(`_bar_mask.anchor_left = 진행도`, _build_stage_banner의 bar_holder/green/mask, _refresh_hud에서 갱신). `UISkin.make_ninepatch(region,mh,mv)` 헬퍼 추가. 초록 아트 온전히 표시 + 좌→우 채움. 라이브 확인. 빌드 exit 0. (트랙 #28·skin_progress는 이제 스테이지 바엔 미사용.)
- 2026-06-05: **스테이지 게이지 연출 완성(100% 노출 + 마스크 정렬 + 가시성).** ①**클리어 시 100% 유지**: 마지막 몹/보스 처치로 스테이지를 넘기면 `_show_full_gauge=true` → 게이지 100% 유지, **이동시간 ↑**(`STAGE_CLEAR_DELAY=1.3` vs 일반 `POST_KILL_DELAY=0.6`), 다음 몹 조우(`_enter_idle`)에서 0% 리셋(보스는 1.0 유지). 기존엔 마지막 처치 즉시 0%라 100%를 못 봤음. ②**바 상하로 키움**(높이 12→30, `bar_holder` offset_top/bottom 86/116) — 초록 가시성. ③**마스크를 초록 채움 끝에 정확히 정렬**: 게이지를 NinePatch→**TextureRect 균일 스트레치**로, `tools/scan_bar.gd`로 #24 초록 bbox 스캔(x[39..395]·y[3..22]/472×29 = **좌캡39·우캡76 비대칭**) → 마스크 inner를 **분수 anchor**(39/472~396/472, 3/29~23/29)로 매핑. 이전엔 캡 18px 대칭 가정이라 마스크 우측이 초록 끝보다 길었음(사용자 지적 → 수정확인). 큰 우측 캡 장식 보존. 라이브 확인·빌드 exit 0.
- 2026-06-05: **좌상단 HUD 재배치 = PC 체력바 + 골드.** 좌상단을 **PC 체력바(위) → 골드(아래)** 스택으로. ①HUD 체력바 `_build_hp_bar()`(TopHUD, y5~27): 스테이지 게이지와 동일 구조(#24 초록 바 + 분수 anchor inner + 마스크) 재사용하되 **`modulate Color(2.6,0.62,0.4)`로 주황/빨강빛**(초록 게이지와 구분). `_hp_mask.anchor_left = HP/maxHP`로 갱신(`_update_hp_hud()` — `_refresh_hud`·피격(`_on_enemy_attacked_player`)·스폰서 호출). ②골드(ScoreBackground/ScoreLabel) main.tscn에서 y30~54로 내림. ③치트 버튼 y46→58(골드와 겹침 방지). ④골드(ScoreBackground/ScoreLabel) main.tscn에서 y30~54로 내림. 빌드 exit 0.
- 2026-06-05: **머리 위 HP 바 복귀 + 체력바 색 규칙 통일(채움/깎임).** 사용자: 캐릭터 머리 위 HP 바는 유지하되, **차있는 부분=색 / 깎인 부분=검은색에 가까운 어두운 회색(`Color(0.12,0.12,0.14)`)** 규칙. ①PC 머리 위 바(`player_controller._create_hp_bar`) 다시 표시(game.gd `_spawn_next` `set_hp_visible(_tut==TUT_OFF)` 복귀) + StyleBoxFlat 스타일(채움=**녹색** `Color(0.27,0.82,0.32)`/배경=어두운 회색). ②몬스터 머리 위 바(`enemy.gd _style_hp_bar`, `$HPBar`): 채움=**빨강** `Color(0.85,0.27,0.24)`/배경=어두운 회색. ③**좌상단 HUD HP 바도 주황 modulate 제거 → 녹색으로 통일**(PC HP = 어디서나 녹색). 결과: PC HP=녹색(HUD+머리위) / 몬스터 HP=빨강(머리위) / 깎인 부분=어두운 회색 / 스테이지 게이지=중앙 초록(위치로 구분). 라이브 확인(HUD·PC 머리위 녹색 바), 몬스터 빨강 바는 동일 코드(처치 빨라 풀캡처는 어려움). 빌드 exit 0.
- 2026-06-05: **기획서 전수 동기화 (감사→패치, 워크플로우 2회).** 최근 대규모 변경(데이터주도·전투2레이어·UI개편·버그픽스·배포)으로 stale해진 기획서들을 정리. ①감사 워크플로우(14 에이전트 병렬): 각 문서를 코드와 대조해 stale 항목+수정안 산출 → 12개 갱신 필요(설정·레퍼런스분석은 current). ②패치 워크플로우(12 에이전트 병렬): findings대로 정확·최소 수정 적용, 총 ~38건. 주요 정정: UI(점수→골드, NavDock/스테이지배너/UpgradePanel 추가, lock_duration 소유권, PC 2레이어 공격, 플로터 차등), 게임컨셉/스크래치/오프라인/스테이지(전투 2레이어·데이터주도 반영), 전투(미구현→구현완료 표기), 강화(attack_speed 계획→구현), 백엔드(삭제된 외부 플랫폼 배포완료·achievements 카탈로그 data출처), 로그인(set_account_type→commit_auth_session, clear_all→clear_local/clear_account), 사운드(hit/button 트리거), 튜토리얼(배너 문구). UI/강화/전투 직접 검토로 품질 확인. (감사·패치 결과 JSON은 세션 tasks/ 출력에 보존.) `settings_panel.gd`에 **사운드 섹션**(음소거 토글+BGM·SFX 슬라이더, `Audio` 직접 호출). 코어 이벤트 훅 배치: 긁기(`scratch_card._finish_cell`), 카드/잭팟/공격/타격/처치/보스/피격/사망/오프라인보상(`game.gd`), 강화구매(`upgrade_panel`), 버튼클릭. `assets/audio/{sfx,bgm}/`+README 안내. **웹 autoplay 정책**: 로그인 클릭 후 진입이라 `game._ready` BGM 시작 안전. 기획서 `docs/design/사운드 시스템 정리본.md`, 설정 기획서 갱신. **헤드리스 웹 빌드 검증 exit 0, 산출물 정상**(내 스크립트 에러 0, MCP 애드온 로거 노이즈만). ⚠️ 실제 사운드 ogg 미투입(더미). ⚠️ 리텐션 #2~#5 보류분 재개 예정.
- 2026-06-02: **스크래치 숫자 = 집합/가중치 분포로 변경 가능 (영역 확장 준비 1차).** 기존 `randi_range(min,max)` 고정 → 중앙화된 `_roll_number()` + 런타임 분포 API. `scratch_card.gd`: `set_number_weights({값:가중치})`(가중 추첨) / `set_number_pool([값…])`(균등 집합) / `set_number_range(min,max)`(균등 복귀) / `clear_number_weights()`. 분포 비면 `min~max` 균등 폴백(하위 호환). 변경은 다음 `new_card`부터 반영. `game.gd`: 상수 `SCRATCH_NUMBER_WEIGHTS`(={} 균등) + `_apply_scratch_numbers()`(configure 전 호출, 강화/등급 연동 단일 훅). `HandEvaluator`는 등장 숫자 무관(count²×value)이라 임의 집합 호환. 헤드리스 빌드 검증(파싱 에러 0). 기획서 스크래치 §4-1. **치트 패널에 "스크래치 숫자(분포 테스트)" 섹션 추가**(기본1~9/고숫자↑/9만(잭팟)/넓게1~20) — `cheat_panel.gd` `set_number_dist(spec)` 시그널 → game.gd `_cheat_number_dist`가 분포 적용 + `new_card()`로 즉시 재발행(잠금 유지). **강화 연결 위치 미정**이라 당분간 치트로만 테스트. (다음: 분포를 강화/복권 등급에 연동, 이후 그리드 영역 확장)
- 2026-06-05: **데이터 주도 전환 1차 (하네스 "방식"만 차용, 구현은 Godot 네이티브).** generator 하네스(Phaser/Unity·idlez 엔진계약)는 통합 안 함(별도 클로드가 `E:\generator`에서 담당) — 대신 **가치 3종(데이터 주도+검증 게이트+AI 양산)만** 가져옴. Godot은 YAML 네이티브 파싱 불가(웹 GDExtension 사실상 불가)라 **YAML 원본 + Godot 내부 변환단계** 채택(Python 의존 0). 파이프라인: `data/src/*.yaml`(주석 포함 원본) → `tools/build_data.gd`(순수 GDScript YAML **서브셋** 파서[중첩맵·인라인스칼라리스트·스칼라·주석; 블록리스트/앵커 미지원→카탈로그는 맵으로]+검증 가드레일[cost_growth>1·growth>1·order↔tracks·stat threshold정수]) → `data/*.json`(게임 로드). 런타임 로더 = `scripts/game_data.gd`(`GameData.table(name)`, 1회 캐시). **변환 산출 JSON이 const 데이터와 1:1 일치 검증 완료.** 전환 대상 4종: `upgrades.gd`(TRACKS/ORDER→lazy `_ensure`), `balance.gd`(const→`static var`+`_static_init()`로 채움 — game.gd가 `Balance.MOBS_PER_STAGE` 등 직접참조해서 식별자 유지 필수, JSON 누락 시 기본값 폴백), `achievements.gd`(CATALOG→lazy `_catalog`), `enemy.gd`(VARIANTS→`_variant_catalog()`). **public API·호출부 무변경**(데이터 출처만 교체). **경계선: 숫자·테이블·카탈로그만 데이터화, 로직(사망→블록복귀·보스게이트·HandEvaluator 공식·상태머신)은 코드 유지**(인터프리터화 방지). 웹 주의: `data/*.json`은 비리소스라 `export_presets.cfg include_filter="data/*.json"` 필수(설정함), `exclude_filter`에 `data/src/*`·`tools/*` 추가(원본·개발도구 미배포). ⚠️ 새 `class_name`(GameData) 추가 시 헤드리스 `--script` 전에 `--import` 1회로 글로벌 클래스 캐시 등록 필요. ⚠️ `--script` 모드는 autoload 미로드라 BackendService 참조 스크립트는 그 모드서 컴파일 불가(웹빌드로 검증). 검증: 변환 errors 0 / 데이터 로딩 값일치 / 웹빌드 컴파일 에러 0·json 4종 pck 포함 확인. **다음 후보(데이터 주도 본격 활용): 리텐션 시스템을 처음부터 data 주도로 신규 구축, 이후 스크래치 분포/등급.**
- 2026-06-05: **레퍼런스 게임 채택 + UI 개편 시작.** 레퍼런스 = **슬레이어 키우기(GEAR2)** — 리텐션·성장깊이·진행곡선 설계 기준. 학습 분석본 `docs/design/레퍼런스 게임 분석 - 슬레이어 키우기.md`(시스템 전반 + 우리 적용 우선순위; ⚠️나무위키 fetch 차단으로 검색요약 삼각측량, 수치 근사). 스크린샷 `docs/ref/`. 핵심 학습: 깊이=페이싱 다른 성장축 중첩(강화골드/성장SP/승급=클리어/환생) + "같은것N개→상위등급(새 효과 해금)" + 일일던전·반복미션·복귀출석 리텐션. **UI 방향: 레퍼런스식 레이아웃 채택**(상단HUD+진행바 / 전투 / 하단). 우리 차이=하단을 스크래치가 차지 → 메뉴는 **상시 도크 대신 하단 내비에서 띄우는 오버레이**(기존 upgrade/settings 패널 인프라 재사용). **Phase 1(완료): 하단 내비 도크** `scripts/nav_dock.gd`(코드빌드, 5탭 강화/장비/스킬/모험/상점, HEIGHT=58) — 기존 단독 강화버튼 제거→내비 강화탭(나머지=준비중 토스트), 스크래치/티켓 +28px 위로(BOTTOM_MARGIN 38→66·TICKET_BG_Y 560→532), BottomLabel 내비 위로, 튜토리얼 blink 타겟 내비로 재연결. 빌드 exit 0. ⚠️아이콘=더미텍스트(번들폰트 한글전용→이모지 깨짐, Phase 2 스프라이트). ⚠️스크래치 좌표 첫시도값 — 라이브 스크린샷으로 미세조정 필요. **Phase 2(완료): ①상단 스테이지 배너** — `_build_stage_banner()`(game.gd, 코드빌드): 대비용 배경 칩 + 스테이지명(그림자) + 진행 바(`_stage_bar` ProgressBar, 잡몹 처치 진행도 = mobs_killed/MOBS_PER_STAGE, 보스면 value=1+빨강 틴트) + `BOSS` 배지(`_boss_badge`). `_refresh_hud`가 갱신(기존 "[보스]" 텍스트태그 제거). **②강화 패널 레퍼런스식 재설계**(`upgrade_panel.gd` 레이아웃 전면 교체, 구매 로직·시그널·refresh API 유지): 중앙 작은 박스 → **하단 시트(anchor_top=0.40, 풀스크린 딤 제거 → 전투 위에 보임)** + 서브탭(강화/성장/승급, 강화만 동작·나머지 "준비 중") + 스크롤 행. 행 = [종류색 아이콘 placeholder] + 이름·Lv·Max Lv + **값→다음값**(초록 `_fmt_val_at(id,lv)` ARROW `_fmt_val_at(id,lv+1)`) + 비용버튼/MAX. ARROW="→"(웹 렌더 확인됨). MCP로 라이브 검증(에디터 run + screenshot): 내비 도크·스테이지 배너·강화 패널 시각 확인 완료(패널은 DEBUG 임시 자동오픈으로 캡처 후 복원). 빌드 exit 0. ⚠️아이콘 전부 placeholder(색 사각형) — 실제 스프라이트는 더미 이후. **다음: HUD 재화 아이콘·진행 연출 등 폴리시, 또는 리텐션 시스템(데이터 주도) 착수.**
- 2026-06-05: **버그픽스(잠복) — 처치 시 nil 크래시.** `game.gd _on_player_attack_completed`에서 `current_enemy.take_damage(dmg)`가 적을 죽이면 `died` 시그널이 **동기 발화** → `_on_enemy_died`가 그 자리에서 실행되어 `current_enemy=null` 설정 → 직후 `current_enemy.is_dead` 접근이 `Invalid access ... on Nil`. **원래 있던 버그**(비치명적이라 게임은 이어지나 매 처치마다 에러)인데, **강한 세이브(한 방 처치)에서만 killing blow 경로를 타 결정적으로 노출**(신규세이브·튜토리얼·약공격에선 미발생 → 재현 어려움이 진단을 늦춤). 수정: take_damage 이후 판정을 `current_enemy` 대신 **로컬 참조 `e`**로(노드는 사망 트윈 후 queue_free라 `e.is_dead` 안전). 사용자 라이브 확인 완료. ⚠️**교훈: 동기 시그널 핸들러가 멤버 변수를 무효화할 수 있으니, emit 유발 호출(take_damage 등) 이후엔 멤버 대신 로컬 참조로 판정.**
- 2026-06-05: **내비 도크 위치 변경 = 화면 맨아래 → 전투와 스크래치 사이.** 사용자 요청(배치 순서 위부터 **전투화면 → 강화 등 버튼(내비) → 스크래치**). 내비를 하단 앵커에서 **상단 고정 밴드**(game.gd `NAV_TOP_Y=490`, add_child 후 anchor_top=0·offset_top/bottom로 NavDock._ready의 하단앵커 덮어씀)로 이동. 스크래치는 다시 바닥으로(ScratchCardLayout BOTTOM_MARGIN 66→38·TICKET_BG_Y 532→560 원복), 힌트 라벨도 하단 원복(main.tscn offset_top -33). 라이브 스크린샷 확인 — 전투/내비띠/스크래치/힌트 깔끔 분리. 빌드 exit 0.
- 2026-06-05: **강화 패널 = 스크래치 영역만 덮도록 축소(전투·내비 비가림) + 세로 스크롤.** 기존 패널이 anchor_top=0.40이라 전투까지 가리고 잘려 보임 → game.gd `_build_upgrade_panel`가 패널을 **내비 바로 아래(offset_top=NAV_TOP_Y+NavDock.HEIGHT=548) ~ 화면 바닥** 밴드로 배치(상하 위치는 game이 지정, `upgrade_panel._ready`는 좌우 가득만). 영역이 작아진 만큼 행 리스트는 `ScrollContainer`(세로 스크롤, 기존부터 있음)로 — 약 2.5행 보이고 나머지 스크롤. 라이브 확인(전투+내비 보이고 스크래치 영역만 패널). 빌드 exit 0.
- 2026-06-05: **강화 패널 추가 수정 3건.** ①**우측 잘림·확대 느낌** = 긴 이름 라벨이 행 최소폭을 키워 비용버튼이 화면 밖으로 밀림 → 행 라벨 `clip_text=true`·비용버튼 폭 110→86·여백 12→8로 패널 안에 맞춤. ②**닫기 버튼 안 보임**(서브탭과 섞여 우측 밖) → 서브탭 옆에 **빨강 스타일박스 "닫기"** 고정폭(52)으로 또렷이. ③**드래그/스와이프 스크롤**(ScrollContainer는 마우스 드래그로 스크롤 안 됨) → `upgrade_panel._input`에서 마우스/터치 드래그를 직접 처리해 `scroll_vertical` 이동(DRAG_THRESHOLD=8, 드래그 판정 시 진행중 연타구매 취소). 터치 네이티브 스크롤과 이중 적용 방지로 드래그 중 `set_input_as_handled()`. 구매(탭/롱프레스) 로직은 그대로(빈 영역 드래그=스크롤). 라이브 확인(닫기/스크롤바/우측 안잘림). ⚠️드래그가 비용버튼 위에서 시작하면 button_down으로 1회 구매됨(빈 영역 드래그 권장, 추후 개선여지).
- 2026-06-05: **PC 공격 시 크기 작아짐 수정 (enemy 변종 정규화와 동일 방식).** PC 애니 시트(idle/run/attack/attacked/dead)마다 캐릭터가 그려진 크기가 달라(특히 attack 작게) 전환 시 크기가 튐 → `player_controller`에 `_scan_sheet`(불투명 픽셀로 발끝·캐릭터높이 중앙값 스캔, 경로별 static 캐시) + `_compute_anim_render`(idle 기준 norm=idle_char_h/sheet_char_h → scale 보정 + 발끝 공통 바닥선 정렬 offset_y) 추가. 모든 `play(...)`를 `_play(anim)`(scale·offset.y 적용) 경유로 변경. idle은 norm=1이라 기존과 동일, attack 등만 idle 크기로 맞춰짐. 스캔 실패(get_image null) 시 기본 스케일 폴백(무크래시). 빌드 exit 0. ⚠️라이브 attack 프레임 시각확인은 사용자 강한 세이브 필요(자동긁기 느린 신규세이브론 재현 느림).
- 2026-06-05: **전투 2레이어 개정 (기획 확정, 구현 보류).** 속도감 개선 목적 — 기존 "스크래치 완성 시에만 공격"이라 데드타임 많음 → **① 일반 공격(자동, 주기적, 공격속도 스탯 신설·상한) + ② 스크래치 = 스킬(강타/버스트)** 2레이어로. 데미지: 일반=`공격력×AUTO_ATK_COEF`(공격속도 주기), 스킬=`공격력×복권배율×족보배율`(기존 공식=버스트). **스킬 발동 = 상시 능동(긁으면 즉시, 쿨다운X) + 방치 자동발동**(auto_speed=방치 스킬 자동발동 주기로 의미 전환). 강화 트랙 재편: `attack_speed`(공격속도) 신설(상한), `attack`=둘 다 베이스, `card_mult`/족보=스킬 전용. 정체성=스크래치가 보스/벽 돌파·잭팟 주역으로 유지. 잡몹=자동만으로 처치(빠른 템포), 보스=스킬 버스트 필요. **사용자 결정: 발동방식 "상시 능동+방치 자동발동", 진행 "기획서 먼저".** 기획서 개정: `전투 시스템 정리본.md`(2레이어 전면 개정 §3·§4·§9), `강화 시스템 정리본.md`(attack_speed 트랙·auto_speed 의미전환·2레이어 공식·UI/데이터 위치 갱신). ⚠️**아직 코드 미구현** — 검토 후 구현. 구현 영향: game.gd 자동공격 타이머 루프, upgrades.yaml에 attack_speed, balance.yaml에 AUTO_ATK_COEF, 보스 밸런스 재튜닝(전투 §9).
- 2026-06-05: **전투 2레이어 구현 완료 + 라이브 검증.** 위 기획대로 구현: `data/src/upgrades.yaml` `attack_speed` 트랙(초당 공격수, base 0.8·+0.1/Lv·상한25·cost 50×1.30^Lv) + order에 attack 다음 삽입, `data/src/balance.yaml` `auto_attack.coef=1.0`, 변환 재실행. `balance.gd` `AUTO_ATK_COEF` static var+_static_init 로드. `game.gd`: `_process`에 교전 idle 중 `attack_speed` 주기 자동공격 타이머(인터벌=1/aps), `_auto_attack()`(데미지=공격력×AUTO_ATK_COEF, 즉시 적용·로컬e nil안전·play_auto_attack), 스크래치=스킬 분리(`_skill_pending` 플래그 — `_on_card_completed`서 set, `_on_player_attack_completed`가 플래그 true일 때만 버스트 적용 → 자동공격의 attack 모션 종료는 무시해 0플로터/중복 방지), `_enter_run`서 플래그·누적 리셋, 자동공격은 `_tut==TUT_OFF`에서만(튜토리얼 보호). `player_controller.gd` `play_auto_attack()`(가벼운 attack 모션+작은 잽, modulate 플래시 없음). **라이브 검증(MCP run+screenshot): 스크래치 무입력으로 스테이지 1→3 자동 진행 = 자동공격 동작 확인, 스크립트 에러 0, 빌드 exit 0.** 수치 잠정(자동:스킬 비중·공격속도 곡선·보스 재밸런스 미튜닝). ⚠️스크래치 스킬 버스트는 MCP 클릭 불가로 미검증(코드 경로 유지) — 사용자 라이브 확인 권장.
- 2026-06-05: **버그픽스 — 이동(run) 중 스크래치 재긁힘.** `scratch_card.gd _complete()`가 카드 완성 후 `await lock_duration → is_locked=false`로 **자체 잠금 해제**하던 게, game.gd가 run 중 걸어둔 잠금을 1초 뒤 덮어써서 이동 중 자동 긁기가 재개됨(자동공격 도입으로 처치→이동 루프가 빨라지며 노출). 잠금 생명주기는 game이 완전 소유(조우=`_enter_idle` 해제 / 적 생존=`reset_card` 해제 / 이동·처치=잠금)하므로 **카드 자체 해제 제거**. 라이브 검증: run 중 카드 "SCRATCH ME" 유지(안 긁힘) 확인. 빌드 exit 0. (lock_duration export는 미사용으로 잔존.)
- 2026-06-05: **삭제된 외부 플랫폼 배포(사용자 명시 요청).** 그간 작업분(데이터주도 파이프라인·레퍼런스 학습·UI 개편[내비/스테이지배너/강화패널]·전투 2레이어·각종 버그픽스) 묶어 배포. **배포 전 `CHEAT_ENABLED=false`로 끔(필수).** `삭제된 배포 스크립트` 무인자 풀 실행(빌드+캐시버스팅 zip 29.1MB+`삭제된 배포 메타데이터` 비대화형 업로드) → 기존 프로젝트 `wqTVB0B5_C2e`("복권 키우기") 덮어쓰기 성공. ⚠️에디터 열린 채 빌드라 godot-mcp autoload가 pck에 잔존(웹 콘솔 "File not found"·`get_frame_source` 무해, 게임 정상) — 완전 클린 배포 원하면 에디터 닫고 빌드. ⚠️배포 후 CHEAT는 false 유지 중(개발 재개 시 재설정). **(후속) 개발 재개로 CHEAT_ENABLED=true 복귀.**
- 2026-06-05: **전투 연출 juice (UI 폴리시/연출).** 2레이어 전투 가독성·타격감 보강. `effects.gd`: `spawn_impact_burst`에 size 파라미터(자동=14/스킬=34), `spawn_damage_floater`에 `big` 파라미터(스킬=금색·font36·스케일 팝·더 높이 상승 / 자동=기존 작게), **`screen_shake(node, amp, dur)` 신설**(node.position 무작위 감쇠 흔들림, `_shake_base` meta로 중첩 드리프트 방지). `game.gd`: 자동공격=작은 버스트+작은 플로터, 스크래치 스킬=큰 버스트+금색 큰 플로터 + **족보별 화면 흔들림**(잭팟 ≥9 → 화면 전체(self) amp10 / 고족보 ≥4 → 전투영역(arena) amp6 / 그 외 amp3, `_skill_hand_count`로 분기). 빌드 exit 0. ⚠️스킬 버스트·흔들림은 MCP 클릭 불가로 라이브 미검증(자동공격·무에러·빌드만 확인) — 사용자 긁기 확인 권장. (히트스톱은 time_scale 리스크로 보류.)
- 2026-06-05: **스크래치 스킬 더미 이펙트 (족보별 차등).** `effects.gd`에 `spawn_skill_effect(parent, at_pos, count)` + 헬퍼 — 절차적 도형(에셋0). 티어=`_skill_tier(count)` 1~4(≥9 잭팟). 구성: 코어 플래시(크기 tier비례)+확장 링(둥근테두리 Panel 스케일업=고리, tier개)+방사 파편(4+tier×4개, 색 `_SKILL_COLORS` 청→청록→주황→금)+고티어(≥3) 화면 플래시. `game.gd _on_player_attack_completed`가 스킬 적중 시 `spawn_skill_effect(arena, …, _skill_hand_count)` 호출(기존 큰 버스트 대체, 금색 플로터·화면흔들림과 병행). 검증: 임시 START_STAGE_TEST=43(적 HP 거대→자동긁기로 카드 완성→스킬 발동)으로 라이브 — 에러0, 자동(흰 작은)/스킬(금색 큰) 플로터 차등 확인(링·파편·플래시는 0.3초라 캡처 미포착이나 무에러 실행 확인). 테스트 플래그 0 복원, 빌드 exit 0. **더미 — 추후 정식 스프라이트/파티클 교체.**
- 2026-06-08: **코드 전체 리팩토링 패스 (감사→안전 적용, 동작 불변).** 워크플로우 2단(감사 10에이전트 병렬 ~30 findings → 정책/위험도 분류 → 안전·동작불변만 적용: 핵심 파일[`game.gd`·`scratch_card.gd`]은 직접, 독립 파일은 6에이전트 병렬). **적용**: `game.gd`(`_set_bar_fill`·`_clear_arena_effects`·`_apply_upgrade_stats(reset_hp)`·`_advance_stage_after_kill` 헬퍼 추출로 중복 제거 + 이펙트 오프셋/족보 임계 const화), `scratch_card.gd`(셀 지오메트리 3중복→`_cell_geometry()`/`_sub_total()` byte-identical 단일출처), backend(`_is_auth_provider`/`_switch_to_auth_slot` dedup + `SLOT_AUTH`/`SLOT_GUEST` const + get_gold 지역변수), `upgrade_panel`(DECIMAL_TRACKS/서브탭색 const·name 캐시), `nav_dock`/`ui_skin`(죽은 const·중복 arm 제거), `enemy`(죽은 `h` 필드·null가드·BOSS_PIVOT const), 데이터/`login`/`audio` 잡정리. **헤드리스 웹 빌드 exit 0·savepack DONE, 내 코드 에러 0.** **보류(제안)**: god object 분리(cheat/hud 컨트롤러), `_scan_sheet`→SpriteScan 공용화(발끝 드리프트 라이브검증 필요), 패널 오버레이 공용화, `_call_helper` JS dedup(웹전용 무검증), GameData.subtable — 전부 라이브/시각 검증 또는 큰 구조변경이라 사용자 복귀 후 결정. **유지**: 랭킹 API(정책상 코드 보존)·테스트 플래그(BOSS_ONESHOT_TEST/START_STAGE_TEST, dev 활용중). 상세 [[project_refactor_progress]].
- 2026-06-08: **재화 정책 제기(사용자) — 단일 골드 → 다중 재화 확장 필요성.** 현 `BackendService` 골드 단일 통화라 성장축이 한 풀에서 경쟁(레퍼런스 대비 깊이 얕음). 방향: 리팩토링 단계는 **통화 일반화 토대만**(public API·세이브키 유지, 내부 키 기반 라우팅), **재화 종류·획득/소비 설계는 기획 결정**이라 보류(클로드 임의 결정 금지). 상세 [[project_economy_currency]].
- 2026-06-08: **게이지 마스크 세로 정렬 버그 수정 (리팩토링 무관·잠복 버그).** 사용자 지적 — 스테이지/HP 게이지 마스크가 실제 초록 밴드보다 ~1px 위로 떠 하단에 초록 노출·상단 마스크 튐. 원인: `_build_masked_bar`에서 초록은 **비트맵**(#24 아트 29px→holder nearest 스케일), 마스크 밴드는 **벡터**(분수 anchor 3/29~23/29)라 래스터화 픽셀 반올림이 ~1px 어긋남(리팩토링과 무관, 원래 있던 잠복 버그). 1차 시도 = 마스크를 holder 세로 전체로 덮음 → "과하게 커서 어색"(빈 구간 나무 테두리까지 어두워짐) → 폐기. **최종 = 밴드 방식 유지 + `inner`를 `MASK_Y_NUDGE=1.0`px 아래로 시프트**(offset_top/bottom 동시 = 높이 유지 시프트)해 초록 밴드에 정렬. 스테이지·HP 바 공용. 사용자 라이브 확인 "딱 맞음". 정렬 안 맞으면 `MASK_Y_NUDGE` 한 값만 조정. 웹 빌드 savepack DONE(종료코드 -1073741819은 문서화된 godot-mcp 종료 크래시·무해).
- 2026-06-08: **족보 스킬 시스템 기획 정리 + 고족보 컷인 1차 구현.** 발라트로식 "족보=스킬, 족보별 강화" 차용. **기획(미구현)**: 패턴만(숫자 무관)·클래식 사다리·2레이어 강화(무한 기본강화 + 제한적 유물[보유+슬롯 제한])·빌드 선택(저족보 다발 vs 고족보/잭팟)·유물 획득=유물 복권(테마 일치, ⚠️심의=확률형 아이템이라 무환전·플레이획득·flavor 톤다운으로 관리). 기획서 `docs/design/족보 스킬 시스템 정리본.md`(WIP). **구현(1차)**: 사용자 "연출부터 우선" → `scripts/cut_in.gd`(`CutIn` 전체화면 오버레이, 던파 2차각성 컷인풍: 배경딤+사선밴드+스피드라인+일러스트 스케일팝+화이트플래시, 티어별 강도). game.gd 배선: `CUTIN_MIN_COUNT`(현재 5, max-count 신호 기준 — 분류기 도입 후 족보 종류 리매핑) 이상 스킬 적중 시 `_cut_in.play()`, **튜토리얼 잭팟에서도 발동**. 일러스트=더미(PC 스프라이트+포트레잇 카드 placeholder, 실제는 `@export illustration` raw). 게임 안 멈춤·입력 통과. 버그픽스: holder `position` 슬라이드가 앵커 중앙정렬 깨뜨림 → scale팝+페이드로 교체(웹 size=0도 회피). 라이브 검증(밴드·딤·플래시·JACKPOT 텍스트 확인, 에러0), 웹빌드 savepack DONE. dev: `CUTIN_SELFTEST`(연출 확인용, false 복원). ⚠️ 족보 분류기·강화·유물 본체는 미구현(기획 단계).
- 2026-06-08: **(컷인 후속) 플로우 = 화면 멈춤 시퀀스로 변경** (사용자: 연출 속도·순서 조정). 기존 "컷인+공격 동시·논블로킹" → **잭팟 → 화면 일시멈춤(PC·몬스터·자동공격·입력 정지) → 컷인 재생 → 끝 → 공격 재생 → 정상화** 순서. 구현: `_on_card_completed`가 고족보면 `_play_cutin_then_attack()` await(`get_tree().paused=true` → `_cut_in.play` → `await _cut_in.finished` → `paused=false` → `play_attack`). `cut_in.gd`에 `signal finished` + `process_mode=PROCESS_MODE_ALWAYS`(멈춤 중 재생) + `is_playing()`. `_on_player_attack_completed`의 컷인 호출은 제거(공격 전으로 이동). ⚠️ pause는 씬 전환에 유지되므로 await 후 반드시 해제. **라이브 검증(MCP): 멈춤 중 컷인 재생·"245" 플로터 정지(시간 멈춤)·finished로 정상 해제(고착 없음)·에러0**, 웹빌드 savepack DONE. 속도 노브 = `cut_in.gd HOLD`(현재 0.45, 멈춤 길이 직결)/`FADE_*`. (Godot tween: PROCESS_MODE_ALWAYS 노드의 create_tween은 paused 중에도 동작 확인.)
- 2026-06-08: **(컷인 후속) 실제 일러스트 로드 + 렌더 버그 2건 수정.** 사용자가 `assets/cutin/cutin.png` 업로드 → `cut_in.gd`가 `ILLUSTRATION_PATH`에서 자동 로드(`@export illustration` 우선, 없으면 PC placeholder). 파일만 떨궈놓으면 됨(`assets/cutin/README.md`). 적용 안 되던 원인 = 파일이 아니라 **컷인 렌더 버그 2건**(라이브 디버그로 격리): ①**중첩 anchor Control(holder)의 자식이 렌더 안 됨** — holder의 size가 레이아웃/pause에서 0으로 남아 anchor-fill 자식이 0×0(+명시 크기로 줘도 한 단계 중첩되면 안 보임). → **holder 제거, 일러스트/카드/문구를 self 직속 + 디자인좌표(360×780) 고정 위치**로. ②**`modulate:a` 트윈이 `get_tree().paused` 중 안 돎**(band/backdrop의 `color:a` 트윈은 됨) → 페이드인이 안 걸려 일러스트가 alpha 0으로 안 보임. → **등장/퇴장을 scale 트윈으로**(modulate 페이드 미사용, 들고남은 band/backdrop color:a가 커버). 실제 일러스트일 땐 더미 "JACKPOT!" 문구 숨김. **라이브 검증: 일러스트 정상 표시(JACKPOT 컷인)·에러0**, 웹빌드 savepack DONE. ⚠️**교훈**: 코드 빌드 컷인/오버레이는 (a)중첩 anchor Control 자식 렌더 신뢰 불가→self 직속+명시좌표, (b)pause 중 연출은 modulate 말고 color:a/scale 사용. 일러스트 표시 크기=`cut_in.gd` iw/ih(현재 잭팟 ~214px 폭, KEEP_ASPECT) — 사용자 아트가 가로형이면 더 키울 여지.
- 2026-06-08: **(컷인 후속) 레이아웃·플로우 다듬기.** ①**일러스트 가로 꽉**: 텍스처 비율 읽어 폭 360에 맞추고 높이=비율유지(`tex.get_size()`, STRETCH_SCALE). 세로 박스(슬롯) 제거, 위치=`cut_in.gd CUTIN_CENTER_Y`(세로중심, 작을수록 위로). ②**사선 컬러 밴드 제거**(가로 꽉 차니 뒤 노란 박스 거슬림). ③**컷인 후 깨끗한 스윙**: auto/skill이 같은 "attack" 애니라 `play("attack")`이 진행 중이면 replay 안 됨 → `_play_cutin_then_attack`이 멈춤 전 `player.play_idle()`로 정리 → 컷인 후 idle→attack으로 새 스윙(멈춤이 리셋 가림). 일반 족보(컷인X)는 그대로(진행 중 스윙 이어감=비어색). ④**스크래치 공개 후 컷인(순서)**: 셀 완료=70%+페이드 중 `card_completed` 즉시 발화→바로 멈춤이라 "덜 긁힌 채 컷인" 보임 → 멈춤 전 `CUTIN_REVEAL_DELAY`(0.4s) 대기로 공개 연출 끝나고 컷인. 대기 중 `_cutin_active`로 자동공격 억제(PC idle 유지). 빌드 savepack DONE. 튜닝 노브: `CUTIN_CENTER_Y`(위치)·`HOLD`(멈춤 길이)·`CUTIN_REVEAL_DELAY`(공개 대기). ⚠️ 라이브(실제 스크래치) 검증은 사용자 — MCP 클릭 불가.
- 2026-06-08: **성장 시스템 4축 정리 (족보=매칭, 장비/스킬 역할 정정).** ①**족보 체계 = 매칭 그룹**(같은 숫자 N개, 현행 max-count가 이미 그것 → 분류기 재작성 불필요). 포커/슬롯 폐기(무늬·9칸·심의·정체성). 포커식 구분족보(투페어·풀하우스)는 추후 **유물로 "족보 확장"**. 나인 잭팟 현행 유지. 기획서 `족보 스킬 시스템 정리본.md` 매칭 기준 갱신. ②**4축 역할 분리**: **강화**(골드 무한 스탯·수직 비가챠) / **장비**(능력치+가챠 = **BM 기둥**, 유지) / **유물**(슬롯 빌드 '재미'·수평·비파워 = **스킬 대체**) / **족보**(매칭 등급 레벨업). ⚠️**유물≠장비**(유물=빌드재미, 장비=능력치+BM 직결) — 사용자 정정. 스킬 시스템 미사용. ③**내비 '스킬'탭→'유물'**(`nav_dock.gd` id `skill`→`relic`, 동작은 아직 placeholder). 현재 탭=강화/장비/유물/모험/상점. ④⏸**장비 구체화**: 유력안=**외형 변경 없는 아이콘 장비**(키우기 표준 — 캐릭터 비주얼 불변·아트 부담↓, 스탯+가챠+강화 BM. 복권 테마[긁는도구·행운템]면 정합↑) 또는 펫/동료. 구체 설계는 BM·경제 기획과 함께. 빌드 exit 0.
- 2026-06-08: **BM·재화·유물 아이디어 기록 (미구현).** ①**BM**: 자동긁기 최대속도 맛보기→광고 N분 버프→광고제거 IAP(방치형 정석). 선결=삭제된 외부 플랫폼 보상형 광고 SDK 확인. 신규 기획서 `docs/design/BM 시스템 정리본.md` + 메모리 [[project_bm_monetization]]. ②**재화**: 단일 골드→다중 재화 확장 필요성([[project_economy_currency]]). ③**유물 효과 후보**(족보 doc §5-2): 분포조절·**족보확장**·**와일드카드 셀**(한 칸을 아무 숫자로)·등급보정. 유물 획득 주경로=유물 복권(테마·심의 고려). 전부 기획 단계, 코드 미착수.
- 2026-06-08: **재화(다이아) + 장비 뽑기 기반작업 구현 (장비 본체는 보류).** 사용자: 장비는 두되 일단 재화·뽑기 기반만. ①**통화 일반화**: `backend_service`에 `get/add/spend_currency(name)` + 골드/다이아 별칭(`CUR_GOLD/CUR_DIA`). `_migrate`에 `dia=0`·`equip_owned={}` 추가([[project_economy_currency]] 토대 실현). ②**다이아 HUD**(좌상단 골드 아래, 하늘빛 `_dia_label`, `_refresh_hud` 갱신). 치트 패널 "다이아 +100/1K/10K" + 치트버튼 y58→82(다이아 라벨 자리). `_cheat_wipe`에 dia·equip 초기화. ③**데이터**: `data/src/equipment.yaml`(placeholder 복권테마 더미 5종 + `gacha.cost_dia=100` + `rarity_weights` common70/rare24/epic5/legendary1) → `build_data`(FILES+검증 추가) → `data/equipment.json`(파이프라인). ④**`scripts/gacha.gd`**(`Gacha`): rarity 가중추첨→해당등급 무작위 1개, `{id,name,rarity}` 반환. ⑤**장비탭→`_open_equip_gacha`**(다이아 차감→롤→`add_equipment`→결과 토스트, flush. 풀 비면 환불). 검증: 헤드리스 Gacha 2000롤 분포=가중치 정확 일치(71/23/5/1%)·라이브 다이아HUD 표시·런타임 에러0·웹빌드 savepack DONE. ⚠️**교훈**: 에디터 stale 글로벌 클래스캐시 탓에 game.gd가 **새 class_name(`Gacha`) 직접참조 시 parse 에러**(헤드리스 빌드는 정상) → **`preload(GachaScript)`로 회피**(프로젝트 표준 — 새 class_name 추가 후 game.gd에서 쓸 땐 preload 권장). ⏸ 장비 효과/스탯·정식 뽑기 UI·10연차·천장·다이아 획득처는 장비 설계 후.
- 2026-06-08: **재화 표시 개편 + 상단 HUD 레퍼런스식 재구성.** ①**숫자 포맷**(`game.gd _fmt`): 10억 미만 콤마 raw, 그 이상 **÷1000(K)씩 축약하되 mantissa<10억 유지** → 10억=`1,000,000K`·1조=`1,000,000M`·1e18=`1,000,000T`. (사용자 의도=큰 수 그대로 보기. 첫 시도 ÷10억[10억=1K]은 오해, 정정.) 노브 `FMT_RAW_MAX`/`FMT_STEP`/`FMT_SUFFIXES`, `_comma()` 헬퍼. ②**색**: 골드=노란(`GOLD_COLOR`)·다이아=하늘(`DIA_COLOR`) 고정. ③골드 뒤 검은 백판(`ScoreBackground`) 제거(그림자로 가독성). ④**치트 큰 단위**(`cheat_panel`): 골드/다이아 +10억/+1조/+100경(포맷 테스트용). ⑤**상단 바 재구성**(`_build_top_bar`, #23=`R_TOPBAR` 9-slice 가로 가득): [가장 왼쪽 비움(프로필 예약)] 닉네임(=로그인 계정, `account_label` 좌측 이동) · **골드/다이아 가로 나란히**(검은 반투명 칩 `_make_chip` + 아이콘 대신 "골드/다이아 [숫자]" 텍스트) · **설정 버튼=바 오른쪽 끝**. **상단 HUD 체력바 비활성**(`_build_hp_bar` 미호출, 함수·머리위 바는 유지 — "언제 쓸지 미정"). 스테이지 배너 +18 아래로(바와 겹침 회피). 라이브 검증·웹빌드 savepack DONE. ⚠️ 아이콘은 placeholder(텍스트 표기) — 재화 아이콘 생기면 교체. 위치·폰트 1차값(사용자 미세조정 여지).
- 2026-06-08: **사용자 자체 UI 시트 교체 + 게이지 폴리시 + 삭제된 외부 플랫폼 배포.** ①사용자가 `assets/ui/UI_sheet.png`를 직접 리워크(코너 장식 정리·프로필 원형 추가, 첫 업로드는 투명도 누락 → 배경 지운 버전 재업로드). 레이아웃 동일·조각만 이동 → `scan_sheet` 재실행해 `ui_skin.gd` R_* 12개 좌표 전부 갱신(인덱스→역할 매핑 유지). ②**스테이지 게이지**: `scan_bar` 재실행(새 #24 = `Rect2(30,1416,465,27)`, 초록 bbox x[5..459]·y[5..21] 캡5px 대칭)으로 `_build_masked_bar` 분수(G_L/R/T/B) 재정렬 → 초록-검정-초록 깨짐 수정. **절반 두께**(bar_holder 높이 30→15). 마스크 좌/우/상단 1px 확장(`inner.offset_*`, 살짝 비던 것 보정). **초록 더 밝게** = `green.modulate=Color(1.3,1.65,1.25)`(에셋 불변, 코드 보정). ③**배포**(사용자 명시 요청): `CHEAT_ENABLED=false` 후 배포 → wqTVB0B5_C2e("복권 키우기") 덮어쓰기 성공(zip 31.6MB). ⚠️**교훈**: MCP 에디터가 떠 있으면 godot-mcp가 **포트 6550 점유** → `삭제된 배포 스크립트`의 headless 빌드가 비정상 종료(`$ErrorActionPreference=Stop`이라 [1/3]에서 중단) → **수동 빌드(--export-release, 충돌해도 savepack 정상 생성)+`frame_bg.png` 복사 후 `삭제된 배포 스크립트 -SkipBuild`로 우회**(zip+업로드는 godot 불필요). 배포 후 `CHEAT_ENABLED=true` 복원(dev).
- 2026-06-08: **기획서 오늘작업 동기화(감사→패치 워크플로우) + 잭팟 결과 태그.** ①사용자 "오늘 작업 기획서 정리 안된거 검수" → 감사 워크플로우(8문서 병렬: UI·게임컨셉·족보·강화·백엔드·BM·전투·스크래치) = 전부 stale·19 gap. 패치 워크플로우(8 에이전트)로 일괄 반영: UI(상단바 §3/§4 재작성·다이아 HUD·재화 포맷§5-4·nav 스킬→유물·게이지 폴리시), 게임컨셉(§7 단일골드→재화·§6 성장4축표·§10 로드맵), 족보(4축·유물≠장비·다이아/가챠 포인터), 강화(3→4축·골드전용 한정), 백엔드(세이브 dia/equip_owned·`get/add/spend_currency` 행), BM(채널2개·비사행성 단정 정정), 전투(§6 단일통화 정정), 스크래치(매칭그룹 확정·포커식=유물확장). 문체·마커 유지·사실만. ⚠️한 곳이 `[[장비 시스템 정리본]]`(미존재, 장비 미설계) 포인터 — 장비 설계 시 생성. ②**잭팟 결과 태그**: 스크래치 결과가 잭팟(count≥`JACKPOT_COUNT`=9)이면 "value X count !!!" 아래에 금색 "JACKPOT" 팝업(`effects.gd _spawn_jackpot_tag`, 화면중앙 HandResultLabel 기준 +44px). `show_hand_result(..., jackpot)` 인자 추가, game.gd가 `hand_count>=JACKPOT_COUNT` 전달. 빌드 exit 0. ⚠️MCP 잭팟 스크래치 못 띄워 실기 확인 필요(치트 "9만" 분포로 재현). ⚠️**워크플로우 교훈 반복**: `await parallel(...).filter(...)`는 await가 멤버접근보다 느슨 → `const raw = await parallel(...); raw.filter(...)`로 분리(또 냈음, 첫 런 실패→스크립트 수정 재실행).
- 2026-06-09: **장비 뽑기 → 상점 '소환' 메뉴로 이전 (레퍼런스 소환 화면 풀 프레임).** 사용자: 뽑기가 내비 '장비' 탭에 직결돼 있던 걸 → '상점' 탭 정식 소환 메뉴로. 레퍼런스=슬레이어 키우기 소환 화면(`docs/ref/뽑기 레퍼런스.jpg`). 결정(AskUserQuestion): **풀 프레임 재현 / 1회+11연차 / 소환레벨·광고버튼=시각 placeholder**. **신규 `scripts/shop_panel.gd`**(ShopPanel, 코드빌드 오버레이, game.gd preload): 카테고리 탭(소환/패키지/재화/황금열쇠)+서브탭(장비/유물)+좌측 캐릭터(cutin.png placeholder)·안내+우측 소환 리스트. 소환>장비=장비 가챠 **1회(다이아100)/11연차(1000=1회×10, 1회 보너스)** → `_do_pull(count)`(부족 가드·spend_dia·`roll_many`·add_equipment·flush·`currency_changed` emit) → **전체화면 결과 팝업**(희귀도색 카드 그리드, 딤 탭/확인 닫기, 부모=게임 루트라 전체 화면 모달). 나머지 카테고리·유물=준비중 stub. **`scripts/gacha.gd`**: `cost_11`/`pool_name`/`pool_effect`/`roll_many(n)` 추가(기존 `cost`/`roll` 유지). **`data/src/equipment.yaml`**+JSON 재생성(`build_data.gd`): `gacha.cost_dia_11=1000`/`name="장비 소환"`/`effect="장비 획득"` 추가. **`game.gd`**: `_open_equip_gacha`·`const GachaScript` 제거(로직 shop_panel로 이전), `ShopPanelScript`·`_shop_panel`·`const SHOP_TOP_Y=300`·`_build_shop_panel()`(_ready의 settings 다음)·`_open_shop()` 추가, `_on_nav_tab` 변경(**장비→default "준비중" 토스트**·상점→소환 메뉴), `closed`→숨김·`currency_changed`→`_refresh_hud` 연결. 패널 위치=`SHOP_TOP_Y=300`~바닥(강화 패널보다 넓은 바텀시트, 전투 하단 일부 가림 — 튜닝 노브). **`export_presets.cfg`**: `exclude_filter`에 **`docs/*`** 추가(기획서·ref 이미지 ~440KB 웹빌드 제외, 사용자 요청 — .md는 원래 비리소스라 미포함, ref .jpg는 임포트돼 포함되던 것). **검증**: 헤드리스 웹 export savepack DONE(스크립트 에러0), MCP 라이브 스크린샷(임시 `SHOP_DEBUG_OPEN` 자동오픈→검증 후 false 복원)으로 소환 화면 정상 렌더 확인(탭/서브탭/1·11회 초록버튼/다이아 placeholder/소환레벨바/좌측 캐릭터/닫기), **적대적 리뷰 워크플로우 5렌즈(GDScript API·레이아웃웹·뽑기로직·통합회귀·데이터) 통과**(blocker/major 0, nit 2 반영: 결과팝업=전체화면 모달로·패널 내 중복 다이아 라벨 제거). 기획서 UI §3(ShopPanel 행·dia 소비처)·BM §4(가챠 UI 구현) 갱신. ⚠️ 실제 스크래치/소환 클릭·11연차 결과 팝업 라이브 검증은 사용자(MCP 마우스 클릭 불가). ⚠️ 좌측 캐릭터=cutin.png 재사용 placeholder(전용 아트로 교체 여지). 소환레벨/광고 소환은 미구현 placeholder. ⚠️ **교훈**: 전체화면 모달(결과 팝업)은 바텀시트 패널의 자식이 아니라 부모(게임 루트)에 add_child(자식이면 패널 밴드만 덮음). **(후속, 같은 날)** 사용자 요청 — 상점 진입 시 UI가 위로 안 커지게: `SHOP_TOP_Y`(300) 폐기, **강화 패널과 동일 밴드**(`NAV_TOP_Y+NavDock.HEIGHT` ~ 바닥, 스크래치 영역만)로 고정. **좌측 캐릭터 영역은 패널 높이에 고정**(ScrollContainer 밖, `SIZE_EXPAND_FILL`로 밴드 채움)하고 **우측 소환 리스트만 `ScrollContainer`로 세로 스크롤**(행 늘어도 캐릭터 고정 — 사용자 2차 요청). 결과 팝업은 여전히 전체화면 모달(게임 루트). 라이브 확인(전투·내비 안 가림, 캐릭터 고정 렌더)·웹빌드 savepack DONE. **(후속2, 같은 날)** 사용자 — 풀 1개라 스크롤 검증 불가 → **소환 가챠를 멀티 풀(데이터 주도)로 전환**: `equipment.yaml`을 `pools` 맵+`order`로 재구성(기존 `equip` 풀 + 검증용 `dummy` 풀 추가), `build_data._validate`(pools 순회)·`gacha.gd`(`pool_ids()` + 모든 메서드에 `pool_id` 인자)·`shop_panel`(`pool_ids()` 순회로 행 생성, `_build_summon_row(pool_id)`/`_do_pull(pool_id,count)`) 갱신. 풀 추가 = 데이터만(코드 무변경). 라이브: 2행 → 우측 리스트에 스크롤바(캐릭터 고정) 확인. **뽑기 시스템은 사용자 요청으로 [[BM 시스템 정리본]] §4(소환(가챠) 시스템)에 전용 섹션으로 정식 정리** + UI §3은 레이아웃만(BM 포인터). 웹빌드 savepack DONE. **(후속3, 같은 날)** ①상점 캐릭터 칸의 컷인 재사용이 어색 → `shop_panel` 일러스트 경로를 컷인(cutin.png)→전용 `assets/shop/summon_char.png`로 교체(파일 없으면 빈 프레임 placeholder, 컷인 미표시). ②내비 '장비' 탭 → **보유 장비 인벤토리**(신규 `scripts/equipment_panel.gd`, 강화/상점과 동일 밴드): `BackendService.get_equipment_owned()`{id:개수}를 `Gacha.item_info(id)`(모든 pools catalog 검색)로 이름·희귀도 조회 → 희귀도색 카드 그리드(3열, 희귀도순 정렬·개수 x표기), 비면 안내. `_on_nav_tab` equip→`_open_equipment`, 같은 밴드 패널 겹침 방지용 `_hide_nav_panels()`(강화/상점/장비 상호 배타) 추가. 라이브 검증: 빈 상태 + 가짜 보유 오버라이드(`equipment_panel._debug_owned`)로 populated 그리드(전설/영웅/희귀/일반 색·정렬·x개수) + 상점 컷인 제거 확인. 웹빌드 savepack DONE. ⏸ 장착·효과는 장비 설계 후(현재 보유 표시만).
- 2026-06-09: **장비(무기) 시스템 기획 확정 + 기획서 신설.** 사용자와 무기 설계 결정(AskUserQuestion). ✅ **5등급 공용**(일반/희귀/영웅/전설/**신화**[`mythic` 신규] — **등급=공격력 수치만**, 차등 효과 없음) / 무기=**공격력 곱(배율)**(장착 시 `공격력×(1+보너스)`, 자동·스킬 둘 다 영향) / 성장=**등급+개별 강화(레벨, 골드 sink)** / 상위 등급=**뽑기+합성**(같은 등급 N개→상위 1개, N 잠정4·레퍼런스5) / **1슬롯 장착**. 개별 무기는 cosmetic(능력치=등급 파생, 편차 없음). 잠정 수치: 등급 base 보너스 +10/30/80/200/500%, `ENH_PER_LV` 0.10, 가챠 가중 60/28/9/2.5/0.5. **세이브 모델 변경 필요**(현 `equip_owned {id:개수}` → 무기 인스턴스 `weapons:[{uid,item,level}]` + `equipped_weapon`). **신규 기획서 `docs/design/장비 시스템 정리본.md`**(전 섹션 + 구현 단계, 수치 전부 잠정) — 그간 미존재였던 `[[장비 시스템 정리본]]` 포인터 해소. ⚠️ **코드 미착수**(사용자 "먼저 기획" 요청 — 검토 후 구현). 구현 1단계 = 등급 5단계 데이터화(신화 추가). **(+ 같은 날)** 사용자 요청으로 **방어구** 종류도 기획 추가 — 무기와 **완전 동일 형식**(5등급·곱 배율·등급+개별강화·뽑기+합성·종류별 1슬롯), **올리는 스탯만 최대 체력**(`최대HP×(1+방어구보너스)`, 강화 hp 트랙 위 곱). 종류별 슬롯(무기1+방어구1 동시 장착), 가챠는 무기풀+방어구풀(소환 리스트 2행), 합성은 같은 종류·등급끼리. 기획서를 전 종류 공용으로 일반화(§0·§1·§3 타입→스탯 표·§5~§8), 세이브 모델 종류키화(`equips:{weapon:[…],armor:[…]}`/`equipped:{weapon,armor}`). 여전히 코드 미착수(기획만).
- 2026-06-09: **장비(무기·방어구) 시스템 1차 구현 완료 (더미 데이터).** 기획대로 데이터→가챠→백엔드→전투→상점/인벤토리 전면 구현. ①**데이터** `equipment.yaml`(`grades{weight,base}`·`grade_order`·`enhance{per_lv0.1/max20/base_cost100/growth1.5/grade_mult4}`·`combine_n4`·pools `weapon`[stat=attack]/`armor`[stat=hp] 등급당 2종 더미) → `build_data` 검증 갱신 → json. ②**`gacha.gd` 전면 재작성**(class_name Gacha): 등급 가중추첨·`equip_bonus(grade,lv)=base×(1+per_lv×lv)`·`enhance_cost=base_cost×grade_mult^등급idx×growth^lv`·`next_grade`·`roll`(→{item,grade,name,stat})·`roll_many`·`random_item_of_grade`·`item_info`·풀 API. ③**백엔드** 세이브 인스턴스 모델(`equips{weapon,armor}`/`equipped`/`equip_seq`, 구 `equip_owned` 폐기) + `add_equip`/`get_equips`/`get|set_equipped`/`enhance_equip`(골드)/`combine_equips`(같은 종류·등급 비장착 N→상위 무작위, 장착 보호, **결과 확정 후 재료 소모**)/`equipped_bonus`. ④**전투**(game.gd): `_attack_power()=공격력×(1+무기보너스)`·`_max_hp()=hp×(1+방어구보너스)` 헬퍼, 자동·스킬·`player_max_hp`(5곳) 전부 반영, `_on_equipment_changed`(장비변동→max_hp 재계산+clamp+HUD). ⑤**상점** 무기·방어구 2풀 소환·결과 등급색 카드(`add_equip`). ⑥**인벤토리**(`equipment_panel.gd` 재작성): 종류 탭(무기/방어구)+장착 슬롯+목록(장착/장착중/강화 비용)+합성 버튼(`_combinable_grade`). nav 장비→인벤토리(`_hide_nav_panels` 상호배타). **검증**: 헤드리스 빌드 savepack DONE(에러0), 라이브 스크린샷(영웅+80%/희귀+5=+45%/일반+10% 등급색·보너스·강화비용[common100/rare+5≈3.0K/epic1.6K] 정확·장착중·합성 동작). **적대적 리뷰 6렌즈 통과(blocker/major 0)**: 수정=`combine_equips` 재료 erase 순서(상위풀 빈 경우 데이터손실 잠복→결과 확정 후 소모), nit(마이그레이션 타입가드=비도달 skip / 백엔드·BM 기획서 stale=동기화). **세이브 데이터손실 없음 확정**(에디터 직접실행 골드0 = 로그인 우회 슬롯 아티팩트, 마이그레이션 회귀 아님). 기획서 `장비 시스템 정리본` ✅구현으로 갱신, 백엔드·BM 동기화. ⚠️ 더미 카탈로그(이름·수치 잠정)·소환레벨/천장/다이아 획득처 미구현. `SHOP_DEBUG_OPEN`에 검증용 장비 시드(평소 false). **(+UI 후속)** 사용자 — 상점 내부 버튼이 강화 패널과 크기 달라 → **강화 패널 버튼 메트릭에 통일**(카테고리/서브탭 탭 높이40·폰트16, 1회/11연차 구매 높이44·content4, 닫기 54×40) + **'황금 열쇠' 카테고리 제거**(소환/패키지/재화 3탭). 라이브 확인·기획서(UI §3·BM §4) 동기화.
- 2026-06-09: **상점 버튼 강화와 정밀 일치 + 재화/닫기 아이콘화 + 시트 라벨러 갱신.** ①**버튼 미세차 제거**: 상점 탭 행 HBox separation 4→6(강화 헤더와 동일) → 카테고리 탭이 강화 서브탭과 픽셀 동일(마진 14/14/12/12·vbox sep 8·탭 0×40 font16 content4·닫기 54×40 공유). ②**재화 아이콘화**: HUD 골드/다이아 텍스트 prefix 제거 → 칩 좌측에 `assets/ui/icon/icon_gold`·`icon_dia` 아이콘(`game.gd._make_currency_icon`, Linear 필터)+숫자. ③**닫기 아이콘**: `UISkin.skin_close_icon(btn)` 신설(레드 close 스킨 + `icon_close`[빨간 X] 중앙·`icon_max_width=24`·텍스트 없음) → 강화/상점/장비 패널 코너 닫기 버튼 적용(설정/치트 전체폭 메뉴 버튼은 텍스트 유지). ④**`sheet_labeler` 갱신**: 어제 시트 교체 때 `ui_skin R_*`만 갱신되고 번호 뷰어 `RECTS`는 stale였음 → `scan_sheet` 재실행(31개)으로 `RECTS`·시트크기(1024×1536) 갱신. 라벨 #번호 = scan 인덱스 = `ui_skin R_*` 매핑 일치 확인. 라이브 스크린샷 확인(HUD 금화/다이아 아이콘·상점 닫기 X·번호 시트 31개), 빌드 savepack DONE. 기획서 UI §3-2/§4 동기화. ⚠️ **교훈**: 시트 변경 시 scan_sheet 재실행 후 **ui_skin R_*와 sheet_labeler RECTS 둘 다** 갱신할 것(어제 labeler 누락). **(+후속)** ⑤**재화 비용 아이콘화**: 상점 소환 비용(다이아, TextureRect+LINEAR), 강화 패널 buy("N 골드"→`icon_gold`+숫자, Button.icon+`icon_max_width`18+LINEAR), 장비 강화 비용(`icon_gold`+"강화 N")에 아이콘 적용(비용 숫자 앞). 라이브 확인(상점 💎+100/1000·강화 🪙+10/50, 초록 버튼 정상). ⑥**번호 시트 PNG** `docs/ref/ui_sheet_numbered.png` 생성(`sheet_labeler.gd` `CAPTURE` 일회성 캡처: 창을 시트 비율로 키워 뷰포트 save_png, 평소 false). ⑦**강화 패널 '보유 골드' 잔액**도 텍스트("보유 골드: N")→[금화 아이콘]+숫자(HUD 동일, HBox+TextureRect). 빌드 savepack DONE. **(+후속)** 닫기 아이콘(사용자가 흰색 PNG로 교체)이 다른 텍스트와 안 맞아 → `UISkin.skin_close_icon`에 `icon_normal_color`/hover/pressed/disabled 추가, **크림(`CLOSE_ICON_COLOR`=패널 텍스트색)으로 틴트**(흰 아이콘 modulate). 강화/상점/장비 닫기 일괄 적용·라이브 확인. **(+후속)** 사용자: #26을 9-slice로 늘리지 말고 원래 모양 그대로 → **#26이 팔각형(octagon)**이라 9-slice가 모서리를 깨뜨림 → `skin_close_icon`을 **`texture_margin 0`(통짜 스트레치)** + 닫기 버튼 크기를 조각 비율(79:74 → **43×40**)로 변경(강화/상점/장비). ⚠️ **교훈: 비사각형(팔각형 등) 조각은 9-slice 금지 — `texture_margin 0` + 버튼을 조각 비율로.** ⚠️ MCP 에디터가 game.gd **옛 버퍼로 실행**하는 현상 확인(외부 편집 후 에디터 스크립트 리로드 필요; 디스크·헤드리스 빌드는 정상). **(+장비 패널 레이아웃)** 사용자 요청 — `equipment_panel`: 기능 없던 '장비' 타이틀 제거, **무기/방어구 탭을 헤더로**(강화 패널 강화/성장과 동일 위치·크기 `0×40`·font16·subtab, expand-fill) + 닫기, **장착 현황 라벨을 합성 버튼과 같은 줄로** 올림(합성은 우측 위치 유지), 타이틀+합치로 비운 만큼 **보유 목록 영역 확대**(헤더+row2 2줄 구조). 라이브 확인(무기/방어구 헤더 탭·장착현황↔합성 정렬·리스트 행 증가)·빌드 savepack DONE. UI §3 EquipmentPanel 행 현행화. **(+정렬)** 장비 패널만 마진 12/12/10/10·vbox sep 6이라 강화/상점(14/14/12/12·sep 8)과 버튼 높낮이 어긋남 → 동일하게 통일(탭 크기·밴드는 이미 같음).
- 2026-06-09: **적 = 웨이브(여러 마리·같은 종류) 스폰.** 사용자: 1마리씩 → 여러 마리. 결정(AskUserQuestion): **랜덤 2~4마리 / 앞에서부터 1마리씩(순차) / 마리당 골드·웨이브=스테이지 진행 1 / 맨 앞 1마리만 PC 공격.** 보스·튜토리얼=1마리. 구현: `enemy.gd` static `pick_variant()`(웨이브 외형 통일) / `game.gd` **웨이브 큐 모델** — `current_enemy`=맨 앞(기존 1v1 유지) + `_wave_queue`=대기열, `_spawn_next`가 대형 스폰(slot별 `spawn_x+slot*44`·`z_index=-slot`), `_process` run이 대기열까지 좌로 이동·`_snap_wave_positions`로 정렬, 앞 죽으면 `_advance_wave_front`(다음이 STOP_X로 0.35s 전진·카드 잠금 → `_on_wave_front_arrived`서 해제+start_attacking), 전멸 시 `_advance_stage_after_kill`(웨이브=진행 1)+다음 웨이브. 마리당 골드, 앞 1마리만 `start_attacking`. 헬퍼 `_clear_wave`/`_snap_wave_positions`. 상수 WAVE_MIN2/MAX4/SPACING44/STEP_TIME0.35. **검증: 적대적 리뷰 5렌즈 → blocker 1건 적발** — `replace_all`로 `current_enemy` free 패턴을 `_clear_wave()` 호출로 치환할 때 **`_clear_wave` 정의 본문 내 동일 패턴까지 치환돼 무한 재귀(런치 시 스택오버플로 행)** → 본문을 올바른 정리(앞+대기열 free·`_wave_advancing`=false)로 복구. **이 행이 MCP 스크린샷 타임아웃 4회의 진짜 원인**(게임이 런치 시 무한 재귀로 멈춤). 수정 후 라이브: **스켈레톤 3마리 웨이브 줄지어 등장 확인**, 에러 0, 빌드 savepack DONE. ⚠️ **교훈: `replace_all`은 방금 정의한 헬퍼 함수 본문 안의 동일 패턴까지 치환 → 자기재귀 유발. 헤드리스 빌드는 런타임 재귀/무한루프를 못 잡으니 라이브(또는 코드 리뷰) 검증 필수.** ⚠️ 마리수·간격 수치 잠정(튜닝 여지). 기획서 스테이지/전투 갱신함. **(+후속)** 사용자 요청 — 앞 적 처치 후 **‘몬스터가 전진’ → ‘PC가 짧게 전진’**으로 변경: `_advance_wave_front`/`_on_wave_front_arrived`/`_wave_advancing`(+WAVE_STEP_TIME) 제거, 다음 적을 `current_enemy`로 잡고 **기존 `_enter_run()` 재사용**(PC가 run 애니로 ~WAVE_SPACING 전진+배경 스크롤 → run 루프가 STOP_X 도착 시 idle+공격 처리). 몬스터 **간격 44→50→70**(사용자 추가 요청, `game.gd const WAVE_SPACING` 한 값). ⚠️ 70에선 4마리 웨이브의 뒤 1~2마리가 화면 우측 밖에서 시작→전진하며 등장(STOP_X=220+3×70=430>360). 라이브 확인, 빌드 savepack DONE. **(+후속2)** ①**PC 공격 애니 속도 14→24**(`player_data.gd` ANIMATIONS.attack.speed — 전진에 스윙 끊김 완화·스냅감, 이 값만 조정). ②**스크래치 잠금/초기화는 웨이브(덩어리) 전멸 후에만**: `_enter_run(lock_and_reset := true)` 파라미터 추가 — 웨이브 내 다음 적 전진은 `_enter_run(false)`(카드 그대로·연속 조작), 전멸→다음 웨이브 이동만 잠금+리셋. 스킬 완성 리셋 조건도 `if not e.is_dead`→`if not scratch_card.is_locked`(웨이브 내 처치 시에도 완성 카드는 새 카드 발행, 전멸로 잠긴 경우만 유지). 빌드 savepack DONE.
- 2026-06-09: **기획서 전수 감사→패치 (워크플로우 2회, 16문서).** 사용자 "여기까지 작업한 거 기획서 업데이트 안 된 거 확인". ①감사(16에이전트 병렬, 문서별 코드 대조): 11문서 stale·37건. current=오프라인방치/설정/로그인(+족보/튜토리얼은 nit만). ②패치(13에이전트 병렬): 40건 반영(+전투 §7-1 피격 플로터 1건 후속 직접 수정). 주요 정정: **전투**(attacked/dead "보스전만"→일반몹·보스 공통·PC HP0, 웨이브 처치 흐름[웨이브내 즉시 run·카드유지 / 전멸 시만 잠금], 데미지 공식에 장비 곱[무기=공격력·방어구=최대HP]), **스테이지**(STEP_TIME 상수 삭제·PC 전진·웨이브 외형통일 pick_variant), **UI**(게이지 마스크 현재값 R_BAR_FILL 5px대칭, 닫기=#26 팔각·흰X 크림틴트·max_width20), **장비/게임컨셉/BM**(장비 "보류/더미"→무기·방어구 구현완료·5등급 mythic·인스턴스 세이브·다이아 가챠), **스크래치**(lock_duration 완전삭제·웨이브 연속조작·잭팟 태그), **강화**(즉시반영 brush·장비곱), **백엔드**(LocalProvider 더미로그인·commit_auth_session), **사운드**(shop/equipment 패널 훅), **리텐션**(단일통화 전제→다이아 존재). 코드 무변경(문서만, docs/는 빌드 제외). skip은 타당(UI 설정버튼 실측 36×36 유지 등). 감사·패치 결과 JSON은 세션 tasks/ 출력 보존.
- 2026-06-09: **자동공격 데미지 = attack 모션 종료 시점 적용 (스킬과 동일).** 사용자: 한 방 처치 시 전진(run)이 공격 스윙을 끊음(자동공격이 데미지를 **즉시** 적용→처치→`_enter_run`이 모션 0프레임에 덮음). → 자동공격을 스킬과 같은 지연 적용으로: `game.gd` `_auto_pending`/`_auto_pending_damage` 추가, `_auto_attack`은 모션 시작+데미지 저장만(즉시 take_damage·이펙트 제거, `_skill_pending`/`_auto_pending` 중이면 새 공격 안 시작), `_on_player_attack_completed`가 모션 종료(`attack_motion_completed`)에 **스킬/자동 분기**로 데미지·이펙트·take_damage 적용. `_enter_run`에서 `_auto_pending` 리셋. + **`player_controller._on_animation_finished`**: attack 종료 후 emit 핸들러가 모션을 바꿨으면(처치→`play_run`) idle 복귀 건너뜀(`if animation == "attack"` 가드) — 안 그러면 전진이 idle로 덮여 PC가 run 없이 미끄러짐(스킬 처치에도 있던 잠복 버그 동시 해결). 라이브 검증: 골드 누적·웨이브 2→1 진행·스크래치 자동 공개·에러0, 빌드 savepack DONE. ⚠️ **부작용 주의**: 자동공격이 모션 종료까지 다음 발동을 막으므로(중복 방지) **공격속도(attack_speed) 상한이 attack 모션 길이(~0.2s, speed 24)로 캡**됨 — 고 attack_speed에서 체감 DPS가 모션 길이에 묶임. 더 빠르게 원하면 `player_data.gd` attack.speed↑ 또는 데미지를 모션 종료 대신 hit-프레임 비율로 분리(미적용).
- 2026-06-09: **프리징 버그 수정 (컷인 pause 고착 — 강제 잭팟 연속 컷인).** 사용자 보고: 111단계 강제 잭팟 진행 중 프리징(모션과 잭팟 연출 겹침). 재현(START_STAGE_TEST=5 + `SCRATCH_NUMBER_WEIGHTS={9:1}` + auto_rate 25 → 카드마다 잭팟 컷인 연발): **두 스크린샷 완전 동일(골드·스테이지 고정, 렌더는 됨) = `get_tree().paused` 고착.** 원인 = **고족보 컷인(트리 pause + 0.4s 공개대기 await)이 웨이브 전진(run)·다른 컷인과 겹침**: 전진 중(카드 조작 가능) 자동긁기로 잭팟 완성→컷인이 전진과 겹치고, 공개대기 중 `_enter_idle`/2번째 컷인이 끼어들어 pause 토글이 꼬여 영구 pause. **수정 3중 안전장치**(`game.gd`): ①`_process` run 분기를 `not _cutin_active` 게이트(컷인 중 전진 정지 → `_enter_idle` 끼어듦 차단), ②`_on_card_completed` 컷인 조건에 `not _cutin_active`(중첩 컷인=중복 pause 방지), ③`_enter_idle` 진입 시 `_skill_pending`/`_auto_pending`/펜딩 데미지 클리어(스윙이 전환에 끊겨 종료 시그널 안 와도 플래그 고착 방지). **재현 → 수정 후 동일 스트레스에서 골드 483→496 연속 진행·컷인 정상 재생/해제·프리징 소멸 확인**, 임시 재현설정 원복, 빌드 savepack DONE. ⚠️ **교훈: `get_tree().paused`로 연출하는 컷인은 (a)재진입 가드(`_cutin_active`) 필수, (b)pause 중/공개대기 중 다른 상태전이(전진·재교전)가 끼어들지 못하게 게이트, (c)await 기반 연출은 비동기 재진입으로 pause 토글이 꼬일 수 있음. 헤드리스 빌드는 못 잡음 — 강제 잭팟 스트레스 재현으로 검출.**
- 2026-06-09: **스크래치 카드 "긁힌 채 초기화 안 됨" 버그 수정.** 사용자: 수동 긁기 후 카드가 한동안 긁힌(완성) 상태로 남고 새 카드로 안 바뀜(웨이브 변경 이전부터). 원인 = ①(오늘 회귀) "웨이브 전멸 후에만 잠금/초기화" 작업 때 스킬 완료 후 리셋 조건을 `if not e.is_dead`→**`if not scratch_card.is_locked`**로 바꿨는데, `scratch_card._complete()`가 완성 순간 항상 `is_locked=true`로 잠그므로 **그 조건이 항상 false → 적 생존 시 리셋이 영영 안 됨**(적이 죽을 때까지 = "한동안" 완성 카드 고착). → **`if current_enemy != null: reset_card()`**로 수정(적 생존·웨이브 내 다음 적 있으면 즉시 새 카드 / 웨이브 전멸 시만 인터-웨이브가 잠금+리셋). ②(안전장치) 스킬 스윙이 어떤 상태 전환에 끊겨 모션 종료 시그널(=리셋 트리거)을 못 받는 경로 대비 — **`_enter_idle`에서 잠긴(완성) 카드면 `reset_card()`**로 교전 진입 시 새 카드 발행(부분 긁기[웨이브 내 보존]는 안 잠겨 있어 그대로 유지). 검증: 빠른 자동긁기(temp)로 완성→스킬→리셋→재긁기 사이클 정상 순환 확인(부분 긁힘↔새 카드 교대), 에러0, 빌드 savepack DONE. ⚠️ 수동 긁기 후 ~2초 자동 재개 지연(`scratch_card.AUTO_RESUME_DELAY`)은 의도된 동작(수동 중 자동 간섭 방지) — 거슬리면 그 상수만 낮추면 됨. ⚠️ **교훈: 완료 시 잠그는(_complete) 카드의 리셋 조건에 `is_locked`를 쓰면 항상 막힘 — 리셋 판단은 전투 상태(current_enemy)로.**
- 2026-06-09: **잭팟(고족보) = 웨이브 전체 공격(AoE) + 빠른 자동긁기 치트.** 사용자: 높은 족보면 무리 전체를 치게(잭팟 터지면 그 무리 전멸). 결정(AskUserQuestion): **일단 잭팟(count=9)만 AoE, 추후 확장 가능하게.** 구현(`game.gd`): `const AOE_MIN_COUNT := 9`(이 값만 낮추면 더 낮은 족보부터 AoE) + `var _aoe_active`. `_on_player_attack_completed` 스킬 분기를 **`_skill_hand_count >= AOE_MIN_COUNT`이면 `_skill_aoe(dmg)`** / 아니면 앞 1마리(기존)로 분기. `_skill_aoe`=살아있는 모든 적(current_enemy+_wave_queue)에 같은 dmg 적용(개별 처치 시 `_on_enemy_died`는 `_aoe_active` 가드로 골드·연출만, 대형/스테이지 정리는 보류) → `_resolve_wave_after_aoe`가 생존자로 대형 재구성 / 전멸이면 웨이브 클리어(스테이지 진행+다음 웨이브). `_on_enemy_died`에 `if _aoe_active: 골드·fx만 + return` 게이트 추가. **라이브 검증(강제 잭팟+빠른 자동긁기 temp): 잭팟이 고블린 3마리 무리를 한 번에 전멸(골드 점프)·스테이지 1→2 진행·프리징/에러0**, 빌드 savepack DONE. ⚠️ 확장: AOE_MIN_COUNT를 5로 낮추면 컷인 족보부터 AoE. **(+치트)** 사용자: 강화 트랙 자동긁기 맥스를 8.0으로 올렸다가 취소(자동이 수동보다 빨라지면 안 됨) → `upgrades.yaml` 원복(per_level 0.25, JSON 미재생성이라 게임값 불변). 대신 **빠른 자동긁기(8.0)는 치트 전용**: `cheat_panel` "자동긁기 속도(치트)" ON/OFF(`set_auto_fast`), game `_cheat_auto_rate`(>0이면 `_enter_idle`이 우선 적용)·`_cheat_set_auto_fast`(즉시 반영). 정식 강화는 그대로. **(+치트 패널 축소)** 버튼이 많아져 세로로 넘쳐 닫기 버튼이 화면 밖 → `cheat_panel` 버튼 높이 44→28·폰트 18→13·라벨 16→12·타이틀 30→20·VBox sep 12→4·row sep 8→5로 축소(총 높이 ~514px<780) → 전체 화면 내 표시·닫기 접근 가능(임시 자동오픈으로 확인).
- 2026-06-09: **공격적 전체 코드리뷰 (울트라코드 워크플로우) + Major·안전장치 5건 수정.** 36에이전트(10영역 심층리뷰 → 발견별 적대적 검증으로 거짓양성 제거)로 전 스크립트 감사: raw 26건 → **확정 18건(major 2·minor 13·nit 3), 기각 8건**(대부분 "프리징 가능성"이 검증서 *도달 불가* 증명돼 기각 — 최근 컷인/시그널 핫스팟이 이미 잘 막혀 있다는 뜻). **수정한 5건(전부 동작 불변·크래시/고장 방지)**: ①(major) **PC 사망 시 `_on_player_died`의 전체 트윈 무차별 kill이 진행 중 screen_shake를 죽여 `_shake_base` 메타·position 영구 잔류 → 흔들림 영구고장+화면 영구 어긋남**(일반몹도 PC 공격해 보스전 외에도 발생) → `effects.gd reset_shake(node)` 신설 + kill 직후 `self`·`arena` 복구. ②컷인 pause 해제를 `is_inside_tree()` 분기 제거하고 **무조건 해제**(teardown 경합 시 다음 씬 프리징 방지). ③`_clear_wave`가 queue_free 전 `is_dead=true` 세팅(공격 windup await 중 free된 노드 재개 시 "script freed" 콘솔에러 방지). ④`Upgrades.value/cost/max_level` 무가드 `_tracks[id]`→`.get()` 가드(upgrades.json 누락 시 전투 첫 공격 하드크래시→안전 degrade). ⑤`login._login_timeout` await 재개 후 `is_inside_tree()` 가드(로그인 후 씬전환으로 free된 노드 타이머 재개 콘솔에러 방지). 헤드리스 빌드 savepack DONE(4파일 컴파일). MCP 연결 끊겨 라이브 회귀는 미확인이나 전부 방어경로라 위험 낮음. **미수정 보류(13건, 사용자 판단 대기)**: minor=웨이브 전진중 카드 비잠금(자가복구·연속조작 트레이드오프)·attack_interval≤windup 코루틴중첩(현재 데이터 안전)·세이브 타입미검증(손상세이브만)·웹종료 ~15s 골드유실(visibilitychange 훅 없음)·build_data 검증게이트 구멍3(null트랙 캐스팅·과들여쓰기 묵살·growth>1이 warn)·show_hand_result 트윈중첩(시각 깜빡임)·스크래치 900노드(모바일웹 프로파일 후). nit=재화 9.2e33↑ int오버플로(스테이지600+)·CHEAT_ENABLED 배포게이트·currency 음수인자(현재 호출부 전부 양수). 결과 JSON은 세션 tasks/ 출력 보존.
- 2026-06-09: **Codex 코드리뷰 지적 2건 수정 (P1 저장 dirty·P2 컷인 pause).** 병행 중인 Codex 리뷰가 짚은 2건을 코드로 검증→수정. **P1**: `backend_service._persist_now`가 `_dirty=false`를 **먼저** 하고 `_provider.save_all()`(3 provider 모두 `-> bool`) 반환을 무시 → 저장 실패 시 `flush()`의 `if not _dirty: return` 때문에 주기 autosave(15s)가 재시도 안 함(데이터는 메모리 생존하나 재영속화 막힘, 실패 직후 종료 시 손실). → **dirty 해제를 저장 성공 시에만**(`_data["last_seen"] 갱신 → var ok = save_all → _dirty = not ok`). **P2**: `cut_in.play()`가 `_busy`/트리밖이면 `finished` 미발화하고 early-return → game이 `paused=true` 후 `await _cut_in.finished` 영구 대기 = 프리징(현재 코드선 is_playing 체크+`_cutin_active` 게이트로 도달 불가하나, 비국소 불변식 의존이라 이미 한 번 터진 핫스팟). → **`play()` 반환 `bool`화**(시작 못하면 false), game은 `if _cut_in.play(...): await _cut_in.finished`로 **await를 play 성공에 묶어** 구조적으로 pause 고착 차단(unpause는 항상 실행). **라이브 검증(강제 잭팟+빠른 자동긁기 TEMP로 컷인 반복): 골드 1022→1118 연속 진행·컷인 정상 재생/해제·프리징 없음·에러0**, TEMP 원복, 빌드 savepack DONE. ⚠️ **교훈: pause+await 연출은 await를 "연출 시작 성공"에 묶어라(미발화 시그널 대기 금지). dirty 플래그는 영속화 성공 후 해제(낙관적 해제 금지).**
- 2026-06-10: **Codex↔Claude godot-mcp 포트 경합 해결 (config에서 제거).** Codex가 godot-mcp를 자꾸 점유하던 원인 = 프롬프트로 "MCP 쓰지 마"는 *모델의 툴 호출*만 막을 뿐, **Codex 프로세스가 config 보고 `npx @satelliteoflove/godot-mcp` 서버를 시작 시 무조건 스폰**하던 것(포트 6550 점유는 모델이 아니라 이 프로세스). 런치 순서로는 해결 안 됨(오히려 Codex 먼저면 선점). → **양쪽 Codex config에서 `[mcp_servers.godot-mcp]` 블록 제거**: 프로젝트 `.codex/config.toml`(주석만 남김) + 글로벌 `~/.codex/config.toml`(둘 다 `.bak` 백업). 이제 godot-mcp는 Claude Code 전용. ⚠️ Codex 세션 재시작해야 적용(config는 시작 시 1회 로드).
- 2026-06-10: **강화 자동구매 버그 수정 + 롱프레스 2→1초.** 증상=손도 안 댔는데 골드 찰 때마다 공격력 강화가 자동 구매됨. 원인(`upgrade_panel.gd`)=롱프레스 연타 중 골드 소진→`refresh()`가 버튼 `disabled=true`→**Godot은 눌린 채 disabled되면 `button_up` 미발화**→`_on_buy_up`이 `_held_id`를 못 비움→`_process`가 `_held_id` 유지한 채 방치 수입 찰 때마다 자동 구매. 수정=`_input` release(`_drag_end`)에서 버튼 disabled 무관하게 `_held_id` 비우고 flush(항상 들어오는 경로). +연타 발동 `RAPID_DELAY` 2.0→1.0s. ⚠️ **교훈: 완료 시 disabled되는 버튼은 button_up에만 의존 말 것(릴리즈는 _input에서 확정).**
- 2026-06-10: **어제 보류 minor/nit 8건 처리(②③④⑤⑥⑧⑨⑩).** 울트라코드 리뷰 보류분 중 안전한 8건: ②적 공격 코루틴 중첩 가드(`enemy._attacking_now` — interval≤windup이어도 중복 방지) ③세이브 타입 검증(`backend._migrate`에 `_num_or`+Dictionary/Array 가드, 손상/구버전 세이브 기본값 복구) ④웹 종료/탭전환 flush(`backend._setup_web_lifecycle_flush` — `visibilitychange`(hidden)·`pagehide`에 best-effort 저장, ~15s autosave 사이 유실 감소, 웹 전용) ⑤build_data 검증 게이트 강화(null 트랙 캐스팅 error / 과들여쓰기 무시→warn 표면화 / `cost_growth`·`growth`≤1 warn→**error 승격**, 현재 데이터 통과) ⑥`show_hand_result` 트윈 meta 추적·kill(연속 완성 깜빡임) ⑧재화 int64 오버플로(`game._comma` `int(v)`→`"%.0f"%v`, 9.2e33+ 부호붕괴 제거) ⑨치트 켜진 채 업로드 하드 차단(`deploy_삭제된 외부 플랫폼` throw, `-AllowCheats`로만 강행) ⑩재화 음수 인자 거부(`add/spend_currency`). **보류 2건**: ①웨이브 전진중 카드 비잠금=의도된 연속조작 설계(버그 아님), ⑦스크래치 900노드=모바일 웹 프로파일 후. build_data errors 0·웹빌드 savepack DONE.
- 2026-06-10: **Codex 공격적 코드리뷰 5건 수정(전부 실재 확인).** 코드 대조로 5건 모두 정확 진단 검증 후 수정(전부 삭제된 외부 플랫폼 웹 경로 중심). **(P1) #1 클라우드 로드 8초 타임아웃 후 stale 덮어쓰기**: `_syncThenNotify`의 `setTimeout(finish,8000)`이 클라우드 동기 완료 전 user-info emit→BackendService가 stale localStorage 로드→다음 저장이 클라우드 롤백. 수정=늦게 도착한 `storage-load-result`가 timeout 이후면 JS가 `storage-resynced` emit→`BackendProvider.resync_needed` 시그널→`backend._on_provider_resync_needed`가 `_data` 경량 재로드(save_ready 재발화 X). **(P1) #2 userId 빈 값 인증 사칭**: `_on_js_message` "user-info"가 userId 무시하고 무조건 `emit(true)`→`login.gd` 단축경로가 실로그인 없이 auth 슬롯 진입. 수정=`login_changed.emit(uid != "", name)`(빈 uid면 미인증). login 흐름 회귀 없음 확인. **(P1) #3 10KB 초과 성공 처리**: 단일 키 전체 저장+무한 장비 append라 클라우드 키당 10KB 한도 도달, 초과해도 `return true`. 수정=**세이브 gzip+base64 압축**(`삭제된 외부 플랫폼._encode/_decode`, `z:` 접두사, 구포맷 평문 JSON 하위호환). 독립검증: 300장비 JSON 14.6KB→압축 1.9KB(7.5배)·라운드트립·구포맷 OK. **(P2) #4 저장 실패 삼킴**: `localStorage.setItem` `catch(e){}`가 quota/프라이빗/iframe 실패 무시하고 `save_all` true→`_dirty` 거짓 해제. 수정=JS `save`가 setItem 성공 bool 반환→`save_all`이 실제 성공 반환(소스 오브 트루스=localStorage). **(P2) #5 사망 시 전역 트윈 전멸**: `get_processed_tweens()` 전체 kill이 UI 토스트·패널·결과팝업의 free 콜백까지 끊어 찌꺼기 노드/모달 박제. 수정=전역 kill 제거, 흔들림=`effects.screen_shake`가 트윈을 meta 보관→`reset_shake`가 명시 kill, 적=`_clear_wave` queue_free 자동정리, 이펙트=자연 종료+`_clear_arena_effects`. 웹빌드 savepack DONE(5스크립트 컴파일 0). ⚠️ **#1~#4는 삭제된 외부 플랫폼 웹 런타임이라 헤드리스 검증 불가**(압축만 독립검증) — 배포본 라이브 확인 필요. **#5만 에디터 사망 재현 가능**. ⚠️ **교훈: pause/연출 외에도 "타임아웃 폴백이 비동기 불변식을 깬다"·"단일 키 무한성장"·"JS catch 삼킴"은 웹 백엔드 전형 함정.**
- 2026-06-10: **커밋·푸시·배포(사용자 명시 요청).** 위 3건(MCP 정리 제외 코드분) 묶어 `5166a54` master 커밋→`origin/master`(github jaeyeopim-stack/lotto) 푸시. 배포=**`CHEAT_ENABLED=false`** 후 수동 헤드리스 빌드(에디터 열려 포트 6550 점유 → deploy 스크립트 `$ErrorActionPreference=Stop` 회피 위해 수동 빌드+`frame_bg.png` 복사+`삭제된 배포 스크립트 -SkipBuild`)→삭제된 외부 플랫폼 `wqTVB0B5_C2e`("복권 키우기") 덮어쓰기 성공(zip 30.9MB, 캐시버스팅 v=1781059269). **CHEAT는 false 유지**(working tree·HEAD 일치, 개발 재개 시 true 복원). ⚠️ **세이브 압축(#3)은 단방향 마이그레이션 — 이 빌드 배포 후 압축 이전 빌드로 롤백 금지**(기존 `z:` 세이브를 못 읽어 신규 시작으로 보임).
- 2026-06-10: **족보/유물 기획 다듬기 — 유물 중심 구조 확정(구현 미착수).** 다음 메인으로 족보·유물 선택, "기획 먼저"라 설계 세션 진행. **핵심 전환: per-grade 족보 강화 폐기 → 유물 중심.** 강화(골드)=균일 배율(현행 `card_mult`)만 유지(수직 노브레이너), **족보 커스텀=유물 전담**(빌드 깊이 전부 유물). 근거=멀티플라이어 수프 회피·2축 선명·점진적 복잡도(발라트로도 깊이는 조커=유물). 조건=유물 효과 다양+초반 일찍+갈아끼우기. **유물 = "장비의 사촌"**: 파워=등급으로만(per-유물 레벨업 없음=수평 정체성), 등급=장비 동일 5단계, 가챠·합성·슬롯 인프라 재활용, **동시 5슬롯**. **효과 4종 동작 확정**: ①분포조절(저숫자 억제 — 등급으로 확률조정 −30%→신화 완전제거 램프, `set_number_weights` 통일, 풀 하한3) / ⑤등급보정(저·중·고 3밴드 족보배율+%) / ④와일드(1칸 와일드=매칭+1, 등급=발생확률 40→100%, 칸 상한2) / ②족보확장(투페어·풀하우스 해금 = **그룹 크기 묶음 포커식 랭킹** — 큰 그룹 우선이라 `3+4`=포카드/투페어 아님, 투페어=최대2+2짜리 둘. 사용자 "3x4도 투페어냐" 고민을 포커랭크로 해소). 획득=유물 복권(긁어 공개)+중복→유물 가루(천장/합성, 골드외 재화 확정), 테마=복권 유지(심의=무환전+플레이획득). **구현 순서: 인프라(데이터·가챠·5슬롯·효과 hook·복권 UI)→①⑤→④→②. 획득 리듬(티켓 획득처)은 스텁으로 두고 진행 가능**(인프라 무관, 보스/블록 권장). 남은 ⏸=등급별 수치 곡선·획득 리듬·보유 상한(전부 구현 안 막음). 기획서 `족보 스킬 시스템 정리본.md` §1·§3·§4·§5·§11 갱신, [[project_hand_skill_system]] 메모리 갱신. **설계 확정·기록 완료, 구현은 다음 세션.**
- 2026-06-10: **유물 시스템 구현 완료 (인프라+효과 4종+UI, 더미 수치).** 기획대로 풀스택: ①데이터 `data/src/relics.yaml`→relics.json(5등급 가중·5슬롯·효과 4종 등급강도 테이블·카탈로그 7종, build_data 검증+grade_order↔grades 정합 게이트) ②`scripts/relics.gd`(데이터·가챠 롤·효과강도·표시 헬퍼) ③백엔드: 보유/5슬롯 장착·해제·가루 재화(`dust`), **`acquire_relic` = 같은 효과는 최고 등급 1개만**(미보유=추가/상위=업그레이드+하위 자동 가루/동급이하=자동 가루 — 사용자 결정으로 '분해' 버튼 폐기, 합성도 불필요) ④효과 배선: ①분포=`set_number_weights`(저숫자 억제 램프, pool_floor 가드)·⑤보정=스킬 데미지×밴드 보너스·④와일드=스크래치 ★칸(확률 proc·상한2, evaluator가 wild_count 합산)·②확장=evaluator `groups`(그룹 크기 내림차순)+포커식 랭킹(`3+4`=포카드) ⑤UI `scripts/relic_panel.gd`(가루 뽑기 1회/11연·슬롯 칩·보유 행·결과 팝업 신규/업그레이드↑/중복→가루 구분) — 내비 '유물' 탭 연결, 변경 시 분포·HUD 즉시 반영 ⑥치트(유물 지급/장착·가루·비우기). 검증: 가챠 분포 20000롤 가중 일치·evaluate 와일드/투페어/풀하우스/포카드 케이스 정확·MCP 라이브 스크린샷(패널 렌더)·웹빌드 savepack DONE. ⚠️ 획득 리듬(티켓/초반 5개 고정 지급) 미구현 — 가루는 치트로만. 수치 전부 잠정.
- 2026-06-10: **전면 리팩토링 — 클래스 상속+함수화 일괄 적용 (울트라코드, 동작 불변).** 감사 워크플로우(6차원 63 에이전트, 발견 57건 → 발견별 적대 검증: 확정40/수정15/기각2) → 기반 7파일 직접 작성 → 9 에이전트 병렬 마이그레이션 + game.gd 직접. **신규 기반**: `panel_overlay_base.gd`(밴드 패널 4종 베이스 — **경로 기반 extends**·closed 시그널·스캐폴드·탭/닫기 팩토리·탭 강조·_toast), UISkin 확장(GRADE_COLOR/NAME 단일화·fmt_abbrev·행 스타일 2종·make_buy_button[**skip 시멘틱** — 부재 속성 미설정 보존]·build_dim_center·리스트 소품 4종·버튼 상태 dedup), `sprite_scan.gd`(player↔enemy 스캔 중복 ~85줄 제거 — ⚠️scale/off_y 공식은 ULP·앵커 정책 상이로 각 클래스 보존), `relic_effects.gd`(유물 효과 계산 static 분리 — god object 티어1), `scene_nav.gd`(_goto_scene dedup), `sheet_rects.gd`(시트 좌표 단일 출처 — ui_skin R_* static var 매핑+labeler 공유, '06-09 labeler 갱신 누락' 사고 재발 방지), GameData 확장(subtable/ordered_ids/weighted_pick — upgrades·achievements의 '캐시 위 캐시' 제거, gacha·relics 가중추첨/정렬 dedup). **game.gd 추출**: _restore_full_hp/_lock_card/_alive_wave_enemies/_spawn_skill_hit_fx/_start_attack_swing/_finish_wave_clear/_open_nav_panel/_add_band_panel/_cheat_commit/_effective_auto_rate + 그림자 5곳 Effects.apply_label_shadow. **backend**: _find_inst/_alloc_uid·_migrate 테이블화+_ensure_container·_equip_grade·_commit·Relics 위임(slot_count/grade_index)+빌드 게이트·삭제된 외부 플랫폼 `_call_helper`(6중복 — save_all eval 2곳은 반환값 연동이라 제외). **⚠️ 통합 금지 확정(적대검증)**: effects._skill_tier↔cut_in._tier(임계 5/3 vs 6/4 — 의도된 독립 노브, 주석만)·결과 모달 shop↔relic(웹 size=0 사고 이력)·provider set_slot(자체 OAuth 로드맵 충돌). **검증**: build_data errors 0·웹 export savepack DONE·**MCP 라이브 5라운드**(전투 스프라이트 발끝 정렬 + 강화/장비/유물/상점/치트 패널 — 전부 기존과 동일 렌더, 런타임 에러 0). ⏸ 보류: god object 티어2(CheatController)·티어3(HudBuilder), 상세 [[project_refactor_progress]]. ⚠️ 싱글톤 매니저(사용자 예고): UI 우선 — 추천 EventBus→UIManager(register/unregister — autoload가 씬 노드 직접 보유 금지)→SceneNav 승격, static 데이터 헬퍼 autoload화는 비추.
- 2026-06-10: **싱글톤 매니저 도입 1단계 (UI 우선 — Events·UIManager autoload).** 사용자 방향(싱글톤 패턴 매니저, UI부터)대로 신규 autoload 2종(project.godot 등록: BackendService·Audio 다음). ①**`Events`**(`scripts/event_bus.gd`) = 전역 이벤트 버스 — `currency_changed`/`equipment_changed`/`relics_changed`/`upgrade_purchased`. 패널들의 로컬 시그널(`changed`/`currency_changed`/`purchased`) 폐기 → 패널이 `Events.*.emit()`으로 발행, game.gd가 `_build_nav_panels`에서 1회 구독(**패널이 game을 모름** — 점대점 배선 제거. 상태 값은 안 실음: 단일 출처=BackendService). ②**`UIManager`**(`scripts/ui_manager.gd`) = 내비 밴드 패널 매니저 — `register_band_panel(id, panel, host, top_offset)`(부착+closed→숨김+레지스트리)·`open(id)`(sfx+상호배타)·`hide_all()`. **register/unregister 패턴**: 패널은 main 씬 소속이라 `tree_exiting`→자동 등록해제(autoload가 씬 노드 영구 보유 시 dangling — 그래서 등록제 필수. Events 구독도 Godot이 freed 노드 연결 자동 해제라 안전). game.gd의 `_add_band_panel`/`_open_nav_panel`/`_hide_nav_panels`/`_build_*_panel` 4종 → `_build_nav_panels()` 하나로(UIManager 위임), `upgrade_panel.open()` 신설(refresh+표시 — UIManager 통일 인터페이스, 튜토리얼 종료 훅만 game `_on_upgrade_pressed`에 잔류). **검증**: 웹 export savepack DONE + 라이브(UIManager.open 2연속 → 상호배타 동작 스크린샷·런타임 에러 0). ⚠️ **에디터 stale autoload**: 떠 있던 에디터는 project.godot의 새 autoload를 몰라 에디터 쪽 Parse Error("Events not declared") 표시 — **게임/빌드는 정상**(새 프로세스는 정상 파싱). **에디터 재시작 필요**. ⏸ 후속 후보: SceneNav autoload 승격(현 static 유틸로 충분)·설정/치트 오버레이 UIManager 편입·`stage_changed` 등 이벤트 확대(리텐션/획득 리듬이 구독).
- 2026-06-10: **(UIManager 후속) 전체화면 오버레이 편입 — UI 표면 전부 UIManager 소속으로.** 설정/치트 패널을 `UIManager.register_overlay(id, panel, host)`/`open_overlay(id)`로 편입(부착·closed→숨김·tree_exiting 자동해제 동일 패턴, 밴드 패널과 달리 상호배타 없음 — 게임 위 독립 겹침). `settings_panel.open()`(refresh+표시)·`cheat_panel.open()` 통일 인터페이스 추가. game.gd `_build_settings_panel` 축소(계정 시그널만 연결), `_on_settings_pressed`→`open_overlay("settings")`, 치트 버튼→`open_overlay("cheat")`. **관리 경계 확정(ui_manager.gd 주석 명문화)**: UIManager = **열고 닫는 UI 표면**(밴드 패널 4종 + 오버레이 2종)만. 상주 레이아웃(내비 도크·상단 바·배너·게이지)=씬 소유(HUD 빌더 분리는 god object 티어3 별도), 게임플레이 연출(컷인·튜토리얼 배너)=game 소유, 토스트/알림=NotificationManager(예정) 몫. 검증: 웹빌드 savepack DONE + 라이브(설정 오버레이 UIManager 경유 오픈·사운드 동기화 스크린샷). ⚠️ **싱글톤 로드맵 메모리 신설** [[project_singleton_managers]] — **2026-06-11 사용자 리마인드 약속**(NotificationManager ⭐ 우선·TimeManager=리텐션·RewardManager=획득리듬·AdManager=BM과 함께).
- 2026-06-10: **버그픽스(소환 결과 팝업 중앙 깨짐) + 유물 소환 = 상점 이전.** ①사용자 보고: 장비 11연차 결과 팝업이 중앙이 아니라 오른쪽으로 커짐. 원인 = 세로 스크롤바 등장 시 ScrollContainer 최소폭(그리드 258+스크롤바+마진)이 카드 고정폭 300을 초과 → PanelContainer가 최소크기로 강제 성장하는데 `grow_horizontal` 기본값(END=오른쪽)이라 오른쪽으로만 자라 중앙 파괴. 수정 = **`SCROLL_MODE_RESERVE`**(스크롤바 자리 상시 예약 — 바가 생겨도 최소폭 불변, 사용자 요구 그대로) + 카드 폭 300→**320**(예약분 수용, 성장 자체 제거) + `grow_horizontal/vertical=BOTH`(안전망 — 만에 하나 자라도 좌우 대칭). ②사용자 제안: 유물 가루 가챠가 유물 탭에 있던 것 → **상점 > 소환 > 유물**로 이전(서브탭 스텁이 이미 있었음). `shop_panel`: 소환 레이아웃 공용화(`_build_summon_layout` — 좌측 캐릭터+우측 리스트), 유물 소환 행(가루 잔액+1회/11연, 가루 비용 텍스트 표기 — 아이콘 에셋 없음), `_do_relic_pull`(spend_dust→roll→**acquire_relic**→Events.relics_changed→결과 팝업), `_build_result_chip`에 **outcome 분기**(신규 등급색/업그레이드↑/중복=회색 "가루 +N") 추가. `relic_panel`: 뽑기·결과 팝업 제거 → **보유/장착 관리 전용**(헤더=가루 잔액+닫기), 빈 안내 "상점 > 소환 > 유물에서". **라이브 검증: 상점 유물 소환 행 + 11연 결과 팝업 정중앙(320px, 좌우 20/20)·outcome 칩(중복 가루+10 회색·신규 등급색) 스크린샷 — 중복 자동 가루 환급도 잔액으로 확인(5000→4.1K)**, 웹빌드 savepack DONE. 기획서 족보 §5-4 동기화(진입 위치=상점, 긁기 연출은 ⏸ 폴리시). ⚠️ 첫 실행에서 팝업 미표시 1회 = 창 활성화 클릭이 딤 "탭하면 닫힘"에 흡수된 것(의도된 동작, 무해).
- 2026-06-11: **기술 로드맵 1~4 + NotificationManager 구현 (리마인드 약속 이행).** ①**테스트 상설화**: `tools/tests/test_{hand_evaluator,gacha,relics,balance,save_codec}.gd`(워크플로우 5에이전트 병렬 작성, 각 `extends SceneTree`+`static run()->실패배열`로 단독/통합 실행 겸용) + `run_tests.gd` 통합 러너 — 총 ~368케이스 전부 PASS. 실행=`godot_console --headless --path . --script res://tools/tests/run_tests.gd`. autoload 의존 로직은 대상 외(--script 제약). save_codec의 손상입력 케이스는 의도된 엔진 ERROR 노이즈(테스트 헤더 문서화). ②**enum화**: `pc_state`→`enum PcState{IDLE,RUN}`·`_tut`→`enum Tut{OFF,SCRATCH,JACKPOT,UPGRADE}`(game.gd 내부 전용 확인, TUT_MSG_*/TUT_JACKPOT_MULT 등 비상태 상수 유지). ③**CheatController 분리**(god object 티어2): `scripts/cheat_controller.gd`(씬 소유 Node, untyped back-ref `game`) — 치트 버튼/패널/14개 동작 전부 이전, game엔 `_cheat_auto_rate`+`refresh_auto_rate_live()`만 잔류. 강화 패널 접근=`UIManager.get_panel("upgrade")`. ④**개발 플래그**: 치트 생성=`CHEAT_ENABLED and OS.is_debug_build()`(release export 자동 off + deploy 게이트 이중 안전) → **`build_web.ps1` 기본 `--export-debug` 전환**(`-Release` 스위치, 로컬 웹 테스트 치트 유지·debug 웹 템플릿 동작 확인. 배포는 삭제된 배포 스크립트 자체 release 빌드 그대로). ⑤**NotificationManager**(싱글톤 매니저 2단계): `scripts/notification_manager.gd` autoload(CanvasLayer layer 80, PROCESS_MODE_ALWAYS) — 토스트 단일 경로 `reward`(오프라인 보상)·`info`(준비중)·`panel_feedback`(패널 rect 기준, `panel_overlay_base._toast` 위임)·`achievement(s)`(**업적 칩 — 구 print placeholder 해소**, 다발 해금 큐 순차). game.gd 토스트 헬퍼 5종 제거. ⑥**export 정리(잠복 문제)**: 이전 빌드 산출물(`res://build/**` 스크린샷·아이콘)과 `삭제된 배포 메타데이터`이 pck에 재귀 패킹되던 것 → `build/.gdignore` + `exclude_filter`에 `build/*,삭제된 배포 메타데이터` 추가. 검증: 통합 테스트 exit 0 · 웹 debug export savepack DONE · main.tscn 헤드리스 부팅 에러 0 · **적대 리뷰 워크플로우(5렌즈 23에이전트, 발견 18→확정 17, 중복 정리 시 7이슈) 전부 수정**: (major) **-SkipBuild 배포가 debug 산출물을 업로드 가능**(build_web 기본 debug화의 부작용 — 에디터 포트 점유 시 -SkipBuild가 공식 우회라 실경로) → **`.build_flavor` 마커**(build_web/deploy가 빌드 시 debug|release 기록) + deploy -SkipBuild 시 release 아니면 throw(`-AllowDebug`로만 강행, 마커 없으면 경고) / (minor) build/.gdignore가 .gitignore `/build/`에 걸려 재클론·클린 시 소실 → **.gitignore `/build/*`+`!/build/.gdignore`로 변경** + 두 빌드 스크립트에 .gdignore 자가 복구 / (minor) 루트 `fmt_test.gd`(stale 스크래치 — 06-10에 고친 int 오버플로 구버전, pck에 패킹되던 잔재) 삭제 / (nit) panel_feedback 그림자 구 _toast 값(0.8/1px) 정확 재현 / (nit) 업적 칩 vbox `MOUSE_FILTER_IGNORE`(Container 기본 PASS가 layer 80에서 클릭 흡수하던 잠복) / (nit) test_gacha 더미 카탈로그 id·이름 하드코딩 → 데이터 파생(카탈로그 교체에도 생존) / (nit) build/web stale 산출물(구 scratch.* ~53MB·*.import·*.tmp) 정리 + deploy -AllowCheats 문구 현행화(release에선 is_debug_build 게이트로 치트 어차피 비활성). 수정 후 테스트 전체 PASS·재빌드 OK·**pck 19.2→14.8MB**(잔재 제외 효과)·pck 검증(fmt_test/res://build/삭제된 배포 메타데이터 0건, 신규 스크립트 포함, tools/tests는 uid 경로 문자열만·gdc 미패킹). ⚠️ **에디터 재시작 필요**(stale autoload — 떠 있던 에디터는 NotificationManager를 몰라 Parse Error 표시, 빌드/게임은 정상). ⚠️ 치트가 이제 debug 빌드 전용 — 로컬 웹 테스트는 build_web.ps1(debug)로. ⚠️ 테스트가 기록한 특성(버그 아님, 설계 판단 대기): HandEvaluator 와일드는 'mult 최적' 그룹이 아니라 '최다 등장' 그룹에 합류(예: [9,1,1]+와일드1 → 1페어(mult 9), 9에 붙이면 36) — 와일드=최선 해석을 원하면 별도 결정 필요.
- 2026-06-11: **AI 협업 브릿지 + 우편함 시스템 1차 구현.** ①**AI 브릿지**(Claude↔Codex 복붙 중계 제거): 로컬 파일 우편함 `.ai-bridge/thread.md`(append-only, 같은 디스크라 git 불필요) + `README.md` 프로토콜. `CLAUDE.md`/`AGENTS.md`에 규칙(브릿지 확인/남김 트리거, **리뷰·검토 요청 시 결과를 반드시 thread에 남김**, 남길 때 사용자에게도 내용 설명). `.gitignore`에 `.ai-bridge/`(미추적). 메모리 [[feedback_ai_bridge_reviews]]. **실사용 검증: 우편함 기획 검토를 6턴 왕복으로 교차 검증해 3자(Claude·Codex·사용자) 합의 도달**(Codex 6+4건 지적 → 검증→반영/반박). ②**우편함 시스템 1차 구현**(기획 확정 후): `scripts/mailbox.gd`(class_name Mailbox — 카탈로그 코드상수 더미3 + 순수로직 `now()`[MailboxTime 추상화]·`claimable_ids`·`has_claimable`·`is_currency_allowed`{gold,dia,dust}·`gc_claimed`) + `backend_service.gd`(`_migrate`에 `mail_claimed` 등록 + `get/is/mark_mail_claimed`·`gc_mail_claimed` 프로덕션 GC) + `scripts/red_dot.gd`(class_name RedDot — 공용 빨간점, 우편함·리텐션 공유) + `scripts/mailbox_panel.gd`(전체화면 register_overlay, 목록·단일/모두 받기, **수령 시퀀스=메모리 일괄 반영→flush 1회→Events.currency_changed.emit→NotificationManager.reward 합산**, whitelist 스킵) + `game.gd`(상단 HUD 우편 버튼[설정 왼쪽]+RedDot, 시작 GC, _refresh_hud dot 갱신). `tools/tests/test_mailbox.gd`(run_tests 등록). **claimed GC = "카탈로그에서 제거됨" 단일조건**(만료 조건 없음 — claimed는 id만 보유라 만료 판정 불가 + ⚠️**id 재사용 절대 금지**가 재수령 차단하는 안전전제). 검증: 상설테스트 PASS·웹 debug savepack DONE·**적대 리뷰(13에이전트 5렌즈, 8건 확정 전부 nit·minor)**: (major) 우편 버튼이 다이아 칩과 360 좌표 겹침 → 칩 좌측 이동(닉104·골드108~188·다이아192~272·우편280~314·설정320~354, **우편·설정 둘 다 34px 동일**) / (nit) 멱등 국소가드(_on_claim_all에 is_mail_claimed continue)·claimed+expired 배지 우선순위(수령완료 우선)·GC 중복 주석(mailbox.gd=테스트 미러/backend=프로덕션). **(+사용자 UI 요청)** 우편 버튼 텍스트 "우편"→`assets/ui/icon/icon_mail.png` 아이콘(설정과 동일 #19 배경 스킨+중앙 LINEAR TextureRect, import `size_limit=128`로 914KB→16KB ctex). **(+Codex 코드리뷰 #8 P2)** 무보상 수령완료 방지 — `Mailbox.valid_rewards`(양수+허용재화 필터) 신설, 지급가능 보상 0개면 claimed 안 찍고 abort(오타/빈 우편이 미지급으로 박제 방지), test_mailbox 회귀 케이스 추가. ⏸ 보류 nit(현 카탈로그 비도달): 무보상 우편 claimed+sfx·보상요약 whitelist → 발행측 검증이 정석. ⚠️ **라이브 미검증**(MCP 클릭 불가 — 받기 흐름·dot·레이아웃 사용자 실기 확인 필요). 기획서 [[우편함 시스템 정리본]] 확정 결정 전부 반영. ⚠️ 새 class_name(Mailbox/RedDot) — game.gd는 preload+Control타입+동적호출로 참조(stale 캐시 회피), 에디터 재시작 필요.
- 2026-06-11: **재화 포맷 단일화 + 버그픽스 2건 + HP 자동재생.** ①**재화 포맷**: `UISkin.fmt_currency` 단일 출처(정책 "10억부터 K"). 구 `fmt_abbrev`(천부터 K) 폐기, 패널 6곳(유물가루·우편보상·강화/장비비용·골드잔액) 통일, `game._fmt`는 위임(FMT_* 상수·_comma 제거). 가루 5000 "5.0K"→**"5,000"**. (커밋 c2e9df5 + **삭제된 외부 플랫폼 배포**). ②**버그 2건**: (a) 스크래치 "마지막 칸 긁힌 채 이동" — 한 방 죽음 시 `_celebrate_cell` 연출 트윈(페이드 0.15s)이 `new_card` 후에도 살아남아 새 카드 셀을 다시 숨김 → `new_card`에서 `_cell_tweens` kill + 시각(visible/modulate/scale) 강제 복원. (b) 강화 스크롤 반대 — 스크롤바 핸들 드래그가 커스텀(natural `-=`)과 VScrollBar 기본(traditional) 충돌 → `_drag_begin`에서 스크롤바 영역이면 커스텀 안 하고 VScrollBar에 맡김. ③**HP 자동재생**: `upgrades.yaml` `hp_regen` 트랙(char, 초당 2.0+2.0/Lv·더미) + `game._process`에서 일반 진행 중 초당 회복(사망·튜토 제외, 가득 스킵) — 기존 회복 수단이 사망/보스진입뿐이던 문제(사용자 제기, AskUserQuestion으로 "자동 재생" 선택). 검증: run_tests 전체 PASS·웹 빌드 savepack DONE. ⚠️ 수치 전부 더미(밸런스 미튜닝). 배포 후 CHEAT_ENABLED=true 복원(dev, working tree). ⏸ 미반영 병행 작업(게임컨셉/리텐션/밸런스·재화·퀘스트 초안·Codex)은 별도 커밋 대기.
- 2026-06-12: **타이틀(로그인) 화면 배경/캐릭터 + 웹 로딩화면(부트 스플래시) 도입 + 배포.** ①**배경**: `assets/backgrounds/bg_main.png`(세로 941×1672 판타지 게이트 일러스트) → `login.tscn $Background`를 흰 ColorRect→TextureRect **cover**(expand_mode=1·stretch_mode=6=KEEP_ASPECT_COVERED, 9:16↔9:19.5라 좌우 ~40px 크롭되나 양옆 깃발·보물상자·랜턴·아치 소품 보존). ②**캐릭터**: `bg_character.png`(1254×1254 알파, 붉은머리 검사) → `Character` TextureRect(렌더순 배경<캐릭터<로고/버튼, `mouse_filter=ignore`로 클릭 통과, 폭 360 KEEP_ASPECT_CENTERED, y290~650 길 위 grounded). ③**하단 가독성 패키지**: `BottomScrim`(GradientTexture2D 하단 360px 투명→0.72 다크 그라데이션) + `login.gd _skin_ui()`(버튼=다크 반투명+금테 StyleBoxFlat 5상태, 상태라벨=크림+그림자) — 밝은 일러스트 위 버튼/문구 또렷. StatusLabel 옛 어두운 font_color(흰 배경용) 제거. ④**웹 로딩화면=부트 스플래시**: `project.godot boot_splash/show_image`(기본 true 복귀)+`image=bg_main`+`bg_color=Color(0.094,0.176,0.133)`(다크 포레스트). `export_presets.cfg head_include`에 `#status-splash{object-fit:contain!important}` 주입 → 로딩 `#status`(창 전체 덮음)에서 이미지 전체 표시(잘림 0)+남는 곳은 bg_color 띠. 띠색=bg_main 좌/우 가장자리 평균×0.78 자동 매칭(가로 데스크탑 좌우 띠가 숲과 이어짐). ⚠️**아키텍처 교훈**: 게임 캔버스는 모든 기기에서 **항상 360:780 세로**(stretch keep). bg_main이 **이중 역할**(login 캔버스 배경[cover] + 부트 스플래시[contain, 창 전체]) — 한때 "웹 데스크탑이 작게 보임"을 이유로 가로 1672×941로 교체했으나 세로 캔버스에선 양옆 프레임이 다 잘려 **세로로 복귀**(모바일=실타겟 우선). 데스크탑 웹이 작아 보이는 건 frame_bg surround 레터박스 구조상 의도이지 bg_main으로 못 키움. ⚠️`bg_main.png`/`bg_character.png`가 git 미추적이라 세로본이 가로본에 덮여 **한 번 유실**(복구 불가→사용자 재업로드) → 이번 커밋에 포함(에셋 확정 시 커밋 교훈). 검증: MCP 라이브(로그인 세로배경+캐릭터+가독성 렌더, 에러0) + 웹빌드 savepack DONE(index.html `show-image--true`/`object-fit:contain`/`#status bg #182d22` 확인) + 로컬 serve 로딩 확인. **CHEAT off→release 빌드→삭제된 외부 플랫폼 배포(wqTVB0B5_C2e '복권 키우기')→CHEAT on 복원.** ⏸ 로그인 캐릭터 위치 미세조정은 사용자 후속(현 y290~650 확정 배포).
- 2026-06-12: **(로딩화면 후속) 부트 스플래시 index.png 캐시버스팅 + 재배포.** 첫 배포 후 로딩에 **Godot 기본 로고가 뜸**(bg_main 아님). 원인: `삭제된 배포 스크립트`이 `index.png`를 static(버전 없는 고정 이름)으로 업로드 → **이전 배포들의 index.png(=Godot 기본 로고, 당시 show_image=false라 숨겨져 있던 것)가 브라우저/삭제된 외부 플랫폼 캐시에 잔존** → 이번에 show_image=true로 켜니 그 캐시된 로고가 노출됨. R2엔 올바른 bg_main이 올라가 있었음(curl Content-Length 2435499=로컬과 일치) = 순수 캐시 stale. 수정: `삭제된 배포 스크립트`이 **index.png도 버전화**(`index.<v>.png` 복사 + `<img id=status-splash src>` 패치, 기존 js/wasm/pck 캐시버스팅과 동일 방식·`$StaticFiles`에서 제외). 새 URL이라 캐시 미스로 무조건 bg_main 로드. 재배포(v=1781236680) 검증: 배포 index.html `src="index.1781236680.png"` + 그 URL이 2435499(bg_main) 200 OK. ⚠️ 진입 `index.html`만 고정 이름이라 그 캐시는 강력새로고침(Ctrl+Shift+R)/시크릿으로 우회 필요(2026-06-04와 동일). ⚠️ `frame_bg.png`도 아직 static이라 바뀌면 같은 stale 위험(현재 미변경이라 보류).
- 2026-06-12: **퀘스트 시스템 1차 구현 (Codex와 기획 정리 → 반영).** 기획서 `docs/design/퀘스트 시스템 정리본.md` §11 1차 범위대로 풀스택 구현(우편함 패턴 정밀 미러). ①**순수 토대**: `scripts/quests.gd`(class_name Quests — 카탈로그[메인 8 + 일일 5, 소환 `exposed=false` 비노출] + 순수 판정 progress/complete/valid_rewards/has_claimable, autoload 무참조=헤드리스 테스트 가능) + `scripts/day_util.gd`(DayUtil.today() 로컬 날짜). ②**backend**: `quests_daily`{date,base,claimed}·`quests_main_claimed` 컨테이너(_migrate) + 접근자 + `roll_daily_quests_if_needed`(날짜 변경 시 base=현재 stat 스냅샷·claimed 리셋) + `get_stats`. ③**진행도 stat 훅**(델타 기반, §4): 기존 total_kills 외 신규 **gold_earned**(game `_grant_gold` 헬퍼로 처치/잭팟/오프라인 3곳 골드획득 단일화)·**boss_kills**(보스 처치 시 `in_boss` 게이트)·**upgrades_bought**(upgrade_panel buy)·**equipment_pulls/relic_pulls**(shop_panel 소환). ④**UI**: `scripts/quest_panel.gd`(전체화면 오버레이, UIManager.register_overlay, panel_overlay_base 재사용, 메인/일일 탭, 행=제목·진행바·x/target·보상·받기, 우편함 수령 시퀀스 미러[valid_rewards 0개면 claimed 미표시]) + game.gd 상단 "퀘스트" 버튼(설정 버튼 아래·우측 정렬)+RedDot+_refresh_hud 갱신+튜토리얼 잠금+_ready 일일 롤오버. ⑤**테스트** `tools/tests/test_quests.gd`(run_tests 등록). 보상=다이아 주력+가루(유물 뽑기권 미도입 — 1차 dia/dust 운용, §6). 검증: **run_tests 전체 PASS**(test_quests 포함 7스위트)·웹빌드 savepack DONE·**MCP 라이브**(상단 "퀘스트" 버튼 + 메인 탭 패널[6행·진행바·보상색] 렌더, 부팅 에러 0). ⚠️ 수치(target/보상) 전부 더미(밸런스 미튜닝). ⚠️ 퀘스트 버튼 위치 잠정(§7). ⚠️ 일일 탭 라이브 클릭 검증은 godot-mcp 클릭/exec 미지원이라 미확인(메인 탭과 동일 `_build_row`라 동작 동일) — 사용자 실기 확인 권장. ⚠️ 새 class_name(Quests/DayUtil) — game/quest_panel은 preload 참조(stale 캐시 회피), 에디터 재시작 필요. **교훈**: GDScript `var x := null` 은 null에서 타입추론 불가(컴파일 에러) → 무타입 `var x = null`. 미구현=§11 1차 제외분(반복/이벤트/광고/운기반·일일소환노출·전체완료보너스·서버시간) 그대로. ⚠️ **아직 커밋·배포 안 함**(사용자 검토 대기). **(+UX 개정, 같은 날)** 사용자 요청 — 메인 퀘스트를 패널 탭 → **HUD 우측 하단 상시 트래커**(`scripts/quest_tracker.gd`)로 분리: "다음 미수령 메인 퀘 1개"만(**목표 desc**, 예 "강화 5회" — 이름 아님·진행바·x/target), 완료 시 위젯 클릭=수령→즉시 다음 노출, 전부 수령 시 숨김(수령 시퀀스 우편함 미러). **패널은 일일 전용**(메인 탭/`_build_main_row` 제거, 제목 "일일 퀘스트"). `quests.gd` `has_claimable`→`has_claimable_main`/`has_claimable_daily` 분리(상단 버튼 red dot=일일만, 메인은 트래커 자체 "받기!"+펄스). 트래커=내비 도크 위 우측(`NAV_TOP_Y-66~-8` 잠정), **오버레이보다 먼저 add**해 dim 이 덮게(z-order 버그 1회 수정), 튜토리얼 중 숨김. 검증: run_tests PASS·웹빌드 savepack DONE·MCP 라이브(트래커 "메인/첫 사냥/1·30" 우하단 + 일일 패널이 트래커 덮음 확인). **(+커밋·배포)** `968a99b` master 푸시 + CHEAT off→release 빌드→삭제된 외부 플랫폼 `wqTVB0B5_C2e` 배포(v=1781255303)→CHEAT on 복원. **(+Codex 코드리뷰 #15 수정 2건, 미커밋)** ①[P1] **자정 롤오버 누락**(롤오버가 _ready/패널open만 → 앱 켜둔 채 자정 넘기고 패널 열기 전 행동이 새 base에 흡수돼 유실) → `game._process`에 저빈도(30s `QUEST_DAY_CHECK_INTERVAL`/`_quest_day_accum`) 날짜 변경 체크(변경 시 roll+flush+refresh, 기획 §5 충족). ②[P2] **daily_summon stat 불일치**(desc "장비/유물 소환"인데 stat=equipment_pulls만 → 유물만 소환 시 진행X, exposed=false라 잠복) → 통합 `summon_pulls` stat(shop_panel 장비·유물 둘 다 +count) + daily_summon.stat→summon_pulls(메인 퀘는 equipment_pulls/relic_pulls 구분 유지). 검증 run_tests PASS·웹빌드 DONE. 브릿지 #16 회신.
- 2026-06-15: **기획서 전수 감사→패치 (퇴근 시 보류분 처리) + 체력 회복 설계 결정 + 재화 톤다운.** 퇴근 시 남긴 [[project_pending_tasks]](기획서 stale 패치 미실행)을 재개. ①**감사+패치 워크플로(20문서 1에이전트씩 병렬, 코드 대조 후 stale이면 정밀 수정)**: 18문서 패치 / 설정·스테이지 current. 주요 정정 — 게임컨셉 §10 우편함·퀘스트 "미구현"→1차 구현 완료, 로그인 §3 흰배경→bg_main cover+캐릭터(취소선 이력 보존), 오프라인 §7 `_show_offline_toast`→`NotificationManager.reward`, 강화 롱프레스 2초→1초(`RAPID_DELAY`)·`purchased` 시그널→`Events.upgrade_purchased`·`_build_upgrade_panel`→`_build_nav_panels`(UIManager), 백엔드 dust/세이브스키마/웹생애주기flush/삭제된 외부 플랫폼 압축(z:)·resync, 사운드 SFX 훅 중앙화+신규 패널 5종, 장비/족보/전투/스크래치/튜토리얼 구현완료 반영(유물 2레이어·잭팟 AoE `AOE_MIN_COUNT=9`·자동공격 모션종료 지연적용·enum Tut·웨이브 가드 `current_enemy`), UI/리텐션/밸런스/우편함 명칭·예시수치. **문서만 수정**(docs/는 export 제외, 코드·빌드 무관). ②**체력 회복 논의 재개(AskUserQuestion)** — 06-11 hp_regen 신설 후 보스 게이트와의 상호작용이 미검토로 남아 있던 걸 매핑 워크플로(코드/데이터/기획/이력 4렌즈)로 격리. **결정: [HP-1] hp_regen은 보스전 중에도 작동(현행 `in_boss` 게이트 없음 = 의도된 동작으로 확정, 코드 변경 없음) / [HP-2] 역할 = "찔끔 유지형"**(일반몹 찔끔 피해만 상쇄, 큰 피해·연타는 누적 → **HP 풀(`hp` 강화 트랙) 성장이 보스 생존의 진짜 축**으로 유지·블록 내 누적 압박[HP 업글 동기] 보존. 회복이 작아 보스전 ON이어도 보스 순간딜이 압도 → 무한생존 위험 없음). **메커니즘 확정, 수치는 전부 더미·미튜닝** — 현재 더미값(초당 2+2/Lv)은 일반몹 피해(약 0.4/초) 대비 다소 강해 "찔끔"보다 세므로, **적 데미지·보스 HP와 함께 튜닝할 때 하향 검토**(삼각 튜닝: hp_regen 회복량 vs MOB_ATK 누적 vs 보스 HP — 회복>피해면 일반몹 위협 무의미·회복<피해면 블록 farming 깨짐). 전투 §3-6·스테이지 §4·강화 §2/§3·게임컨셉 §6/§9·밸런스 §6-1에 명문화 + 스테이지/게임컨셉의 "블록 내 회복 없이 버틴다"(hp_regen 도입 이전) stale 정정. ③**재화 톤다운(AskUserQuestion)**: 다이아·가루 퀘스트/우편함 보상을 "정식 획득처(구현됨)"→**"임시 더미 지급(정식 인게임 획득 루프·BM·천장 미설계)"**로 하향(BM/UI/게임컨셉/백엔드/밸런스/재화정책). "치트만"이라는 옛 stale은 되살리지 않음(퀘스트/우편함도 더미로 지급은 함). ④**경제 포인터 정리**: 전투 §6의 dangling `[[경제 시스템 정리본]]`(미존재, 다중재화 설계는 메모리에만)을 **`[[재화 정책 초안]]`으로 교체**(경제 기획서 미신설 — 사용자 결정). 검증: 경제 포인터 잔존 0건·diff 스폿체크. **(+퀘스트 카탈로그 데이터화)** 사용자 지적 — 퀘스트만 데이터 파이프라인 밖(코드 상수 `quests.gd const MAIN/DAILY`)이라 데이터화. 신규 **`data/src/quests.yaml`**(main_order/main·daily_order/daily) → `build_data.gd`(FILES+검증 케이스: 필수필드·target>0·보상 통화 화이트리스트·금액>0·order↔key 양방향·stat 화이트리스트 오타차단·exposed bool) → `data/quests.json` → `quests.gd`가 `GameData.table` 로드+복원. **⚠️ rewards는 YAML 서브셋 파서가 dict 리스트 미지원 → 통화키 맵(`rewards: {dia:50}`)으로 저장하고 `_rewards_to_array`가 `[{currency,amount}]`로 복원**(순서=main_order/daily_order, `GameData.ordered_ids`). 순수 판정 함수(main_progress/daily_complete/valid_rewards/has_claimable*)는 불변, has_claimable_*만 const→main_quests()/_daily_all() 순회. 모든 소비처(game/quest_panel/quest_tracker/backend)는 함수 API만 써서 무변경. 검증: build_data errors/warns 0·**생성 json이 기존 const와 1:1 일치(직접 대조)**·run_tests 전체 PASS·웹 export savepack DONE(data/*.json include)·**적대적 리뷰 4렌즈(로더등가성/검증정확성/소비처통합/파이프라인) 전부 clean**(nit 2건=현 데이터 미발생 방어 가드레일 → 둘 다 반영). 기획서 §8 데이터화 완료로 갱신. **(+메인/일일 시트 분리, 사용자 요청)** 단일 `quests.yaml` → **`data/src/quests_main.yaml` + `data/src/quests_daily.yaml`** 두 시트로 분리(각각 `data/quests_main.json`·`quests_daily.json` 테이블). 시트 구조 단순화 = `order: [...]` + `quests: {id:{…}}`. `build_data` FILES 2개 등록 + 검증 케이스를 `"quests_main", "quests_daily":` 공용(단일테이블·`order↔quests` 양방향)으로, `quests.gd _build(table_name)`이 `GameData.table("quests_main")`/`("quests_daily")` 로드. 옛 `quests.yaml`/`quests.json` 삭제. 검증: build_data errors/warns 0·두 json 생성·run_tests PASS·웹빌드 savepack DONE·pck에 두 시트 포함+옛 quests.json 제거 확인. `data/quests_main.json`·`quests_daily.json`·두 yaml 모두 git 추적(커밋 대상). **(+퀘스트 정렬 = yaml 파일 순서, 사용자 요청)** 새 퀘 추가 시 `order` 리스트도 갱신해야 하던 불편 → **별도 order 폐지, yaml 작성 순서(위→아래)가 곧 정렬.** 핵심 = `build_data` `JSON.stringify(..., sort_keys=false)`(기본 true가 키를 알파벳 정렬해 파일 순서를 깨던 것 — **모든 data/*.json 에 적용**, json이 yaml과 같은 순서로 직렬화됨) + `quests.gd._build`가 `ordered_ids(order)` → `for id in cat`(맵 순회=파일 순서, Godot4 Dictionary·JSON.parse 삽입순서 보존) + 두 시트에서 `order:` 줄 제거 + build_data 검증의 order 체크 제거. ⚠️ **타 테이블(upgrades·relics·equipment)은 명시 order 배열 + `ordered_ids` 사용이라 sort_keys=false 영향 없음**(order 우선, 잔여키만 파일순서인데 order가 완전). 검증: build_data errors/warns 0·**quests_daily.json 키 순서=파일순서(daily_kill_01/02/03/upgrade/boss/gold/summon) 확인**·run_tests PASS·웹빌드 savepack DONE·적대 리뷰(순서보존+광역영향 2렌즈). 모든 data/*.json 이 재정렬돼 git diff 큼(값 불변·키순서만). **(+퀘스트 버튼 아이콘화, 사용자 요청+에셋)** HUD 퀘스트 버튼을 텍스트 `"퀘스트"`(nav 우드 스킨) → **`assets/ui/icon/icon_quest.png` 아이콘만(배경 없음)**으로 교체(game.gd `_build_top_bar` 6-3). ⚠️ 1차로 메일·설정과 동일 `#19` 배경 스킨을 줬으나 **사용자 요청으로 배경 제거** → `flat=true`+`StyleBoxEmpty`(normal/hover/pressed/focus/disabled 전 상태)로 완전 투명, `_set_button_icon` 중앙 LINEAR·여백 2(**40×40 버튼·아이콘 36px**). 아이콘 import `size_limit=128`(787KB→ctex 13KB, 메일·설정과 동일). MCP 라이브 검증(여러 차례): 배경 박스 버전 → 배경 제거 → 크기 조정(36px), 정상 렌더·런타임 에러 0. **(+장비 HP 풀충전 버그픽스)** 사용자 지적 — 강화 hp 구매는 현재 HP를 풀충전(`_apply_upgrade_stats reset_hp`)하나, **장비(방어구) 변경은 `_on_equipment_changed`가 `player_hp = min(player_hp, max)` clamp만 해서 최대 HP가 늘어도 현재 HP가 안 늘던 비대칭**(상한 하향 시 깎이기만, 상향 시 불변). 결정(AskUserQuestion)=**강화처럼 풀충전(보스전 제외)**. 수정: `_on_equipment_changed`가 `not in_boss`면 `player_hp = player_max_hp`(아니면 기존 clamp) + **`player.set_hp(player_hp)` 추가**(머리 위 바 즉시 동기화 — 기존엔 `set_max_hp`만 불러 풀충전 시 바가 안 차는 문제). 강화의 `not in_boss` 게이트와 일관. 헤드리스 웹 빌드 savepack DONE. 장비 시스템 정리본 §1 동기화. ⚠️ 남은 일 = **퀘스트 시스템 미배포**(repo master `493f660` ↔ 라이브 삭제된 외부 플랫폼 `968a99b`, Codex P1 자정 롤오버 수정 미포함 — 엣지라 비급).
- 2026-06-15: **공격속도(attack_speed) 강화 트랙 폐지 → 자동공격 속도 고정값화 (사용자 결정).** 자동공격은 "바닥 DPS 층"이라 별도 강화 레버(공격속도)가 노브레이너에 가까웠음 → 트랙 제거, 옛 max값을 모두에게 기본 제공. **자동공격 DPS는 이제 공격력(`attack`)으로만 성장(속도 고정), 스크래치 스킬이 버스트/깊이 담당(불변).** 변경: `data/src/upgrades.yaml` `attack_speed` 트랙·order 삭제 / `data/src/balance.yaml` `auto_attack.speed=3.3`(옛 max=0.8+0.1×25) 추가 → `Balance.AUTO_ATK_SPEED`(static var+_static_init 로드) / `game.gd` 자동공격 타이머 `Upgrades.value("attack_speed")` → `Balance.AUTO_ATK_SPEED` / `upgrade_panel.gd DECIMAL_TRACKS`에서 제거. **base 3.3/초(간격 0.303s)는 attack 모션 캡(speed 24, ~0.21s 스윙 = ~4.8/초) 아래라 그대로 발동** — 더 빠르게 하려면 `player_data.gd` attack speed↑(현재 불필요). 기존 세이브의 `upg_attack_speed` 키는 사장(무해, 이제 최대치 무료 제공이라 손해 없음). 검증: build_data errors/warns 0(트랙 제거·speed 추가)·run_tests 전체 PASS·웹빌드 savepack DONE·**MCP 라이브: 신규 세이브가 빠른 자동공격으로 골드/처치 즉시 누적(공격력5 플로터)·에러 0.** 강화/전투 등 기획서 7문서 동기화(워크플로). 수치 3.3 = 사용자 지정(옛 max). ✅ 2026-06-16 커밋·배포 완료(아래 06-16 항목).
- 2026-06-15: **퀘스트 진행도 stat = 트랙별 강화 추가 (`upgrades_bought_<track>`).** "강화 N회"(`upgrades_bought` 전체)를 강화 종류별로 쪼갠 퀘스트("공격력 강화 N회" 등) 지원. **백엔드·`quests.gd` 무변경 — 데이터 주도**: `upgrade_panel._do_buy(id)`가 구매 시 `upgrades_bought`(전체) + `upgrades_bought_"+id`(트랙별) **둘 다** `add_stat`(임의 키 허용) / 퀘스트 데이터 `stat:`에 `upgrades_bought_attack`/`_hp`/`_hp_regen`/`_card_mult`/`_auto_speed`/`_brush`/`_storage` 사용 / `build_data` `qstats` 화이트리스트에 트랙별 키 7종 추가(오타 경고 가드, ⚠️upgrades.yaml tracks와 동기화). 메인·일일 공통(일일 base는 `daily_stat_keys` 자동 포함). ⚠️ **소급 안 됨**(도입 빌드부터 집계, 기존 세이브 과거 구매 미산입). 검증: build_data errors/warns 0·웹빌드 컴파일 OK(라이브 buy→stat은 MCP 클릭 불가로 미검증, 기존 `upgrades_bought` 줄과 동일 패턴). 기획서 [[퀘스트 시스템 정리본]] §4-1 신설. **(+메인 퀘 4종 추가)** quests_main.yaml에 공격력/체력/체력재생/복권배율 강화 퀘(`upgrades_bought_<track>`, 더미 target 15/10/10/10·dia50) — 자동긁기속도/긁기범위/저장소 제외(사용자 요청). **(+"N단계 도달" 형식 = `level_<track>`)** 사용자: "N회 강화" 외 "N단계까지 완료" 형식도 필요 → `backend_service.get_stat`/`get_stats`가 **`level_` 접두사를 `Upgrades.get_level`로 해석(파생 stat, 저장 키 없음·즉석 계산)**. 구매횟수(`upgrades_bought_<track>`)와 구분: level은 **실제 레벨이라 소급 O + 환생/리셋 반영**(신규 세이브선 둘이 동일, 차이는 기존세이브·향후 환생). build_data qstats에 `level_<track>` 추가. 호출처 변경 0(get_stats 읽기전용·level 머지). 검증: build_data 0·웹빌드 OK·**MCP 라이브: 새 get_stats로 매프레임 구동·트래커 진행도 정상·에러 0**(godot_exec는 애드온 미지원이라 buy→stat 단위검증은 코드 자명성으로 갈음). 기획서 §4-1에 A(횟수)/B(레벨) 2형식 명시. **(+레벨 메인 퀘 4종 추가)** quests_main.yaml에 공격력30/체력20/체력재생20/복권배율20 단계 달성 퀘(`level_<track>`·dia100, A 횟수보다 높은 상위 마일스톤). 메인 총 16개(원래 8 + 횟수 4 + 레벨 4). build_data 0·run_tests PASS·기획서 §4-1에 현재 시트 A/B 예시표 추가. **(+세이브 초기화 치트 = 퀘스트도 리셋)** `cheat_controller._wipe`가 골드·강화레벨·장비·유물·스테이지만 지우고 **`stats`(퀘스트/업적 진행도)·`quests_daily`·`quests_main_claimed`·`mail_claimed`는 안 지워** 퀘스트 진행/수령이 남던 버그 → 그 4개도 기본 컨테이너로 set_value 추가(`_migrate` 기본값과 동일: stats {}/quests_daily {date:"",base:{},claimed:{}}/main_claimed {}/mail {}). 레벨 도달 퀘는 upg_ 리셋으로 이미 초기화됐고 이번 건 횟수·수령 상태. `_goto_stage(1)`→`_refresh_hud`로 트래커·레드닷 자동 갱신. 웹빌드 컴파일 OK(치트 버튼은 MCP 클릭/exec 불가로 라이브 미검증 — set_value 자명). **(+메인 퀘 target 일괄 하향)** 메인은 순차 노출이라 각 퀘가 "받고 ~30초-1분 내 달성"하도록 grind 수치 전부 축소(적 처치 30→3·300→20, 강화 5→3·30→8, 트랙별 2~3, 레벨 3~5, 보스 5→2). id도 새 수치에 맞춰 재명명(main_kill_3 등 — claimed 도장 orphan은 dev 무해). 보상 유지. ⚠️ **보스·소환 퀘는 진행도/재화 게이트라 숫자만으론 30초-1분 불가**(보스=스테이지 도달, 소환=다이아/가루) — 기획서 §4-1에 플래그, 시퀀스 벽이면 후순위/제외 검토. **(+사용자 (a) 선택: 보스·소환 시퀀스 뒤로)** 게이트 4개(보스1·장비소환·유물소환·보스2)를 메인 시퀀스 맨 뒤(13~16)로, 앞 1~12 = 빠른 비게이트 퀘(처치/강화/트랙별/레벨). 보스1을 유물소환 앞에 둬 가루(보스 보상) 수급 후 소환 가능. 수치·보상·id 불변, 순서만. build_data 0·run_tests PASS·기획서 §4-1 예시표·게이트 노트 갱신. **(+스테이지 도달 퀘 + 보스 더 뒤로)** ①신규 stat **`stage_reached`(최고 도달 스테이지, monotonic max)** — `BackendService.set_stage`가 `stats["stage_reached"]=max(...)` 갱신(사망/블록복귀로 stage 내려가도 안 줄음), build_data qstats 추가. ②메인 스테이지 도달 퀘 3종(스테이지 10/30/60, `stage_reached`). ③메인 재배치(19개): **① 빠른(1~12) → ② 소환(13~14) → ③ 스테이지(15~17) → ④ 보스(18~19 맨 뒤)**. 소환 가루/다이아는 테스트 메일·보스 보상 충당. 검증: build_data 0·run_tests PASS·**MCP 라이브(첫 퀘 "적 3마리 처치" 완료·수령가능 표시, 에러 0; set_stage 후크 무에러 — runtime_state/exec는 애드온 미지원이라 stage_reached 단위읽기는 코드 자명성으로 갈음)**. 기획서 §4 stat표·§4-1 갱신.
- 2026-06-16: **배포(사용자 요청) + 퀘스트 재배치 + 보스 실패 패널티 완화(구조).** ①**배포**: 그간 작업분(퀘스트 시스템·진행도 stat 확장·공격속도 폐지·장비 HP 버그픽스 등)을 삭제된 외부 플랫폼 `wqTVB0B5_C2e`("복권 키우기")에 배포(에디터 열려 포트6550 점유 → `build_web.ps1 -Release`+`삭제된 배포 스크립트 -SkipBuild` 우회, CHEAT off→빌드→업로드 v=1781574408→CHEAT on 복원). ⚠️ 이후 ②③ 변경은 **미배포·미커밋**(working tree). ②**메인 퀘 재구성**: 유물 소환 메인 퀘(`main_relic_pull_1`) 제거(기능·`relic_pulls` stat·유물 패널은 유지, 주석으로 재도입 지점 표시 — 유물 미사용 계획) / 스테이지 도달 퀘 10/30/60 → **10스테이지 단위 사다리(10·20·…·100, 10종)**, 보상 dia 100→300 점증(잠정). `quests_main.yaml` 수정 → build_data 재생성·run_tests PASS. ③**보스 실패 패널티 완화(구조, 밸런스 수치 아님)** — 사용자 "보스 허들·블록 초반 복귀가 가혹". 결정(AskUserQuestion)=실패 패널티 완화. 사망 시 **블록 첫 스테이지 통째 복귀(보스10→1, 9칸 후퇴) → `DEATH_SETBACK`(현재 3) 만큼 소폭 후퇴**(보스10→7, 블록 첫이 바닥선). `balance.yaml stage.death_setback:3`(데이터값, 밸런스 패스 때 조정) → `Balance.DEATH_SETBACK` static var + **`Balance.retry_stage(s)=max(block_start(s), s-DEATH_SETBACK)`** 신설, `game.gd _on_player_died`의 `block_start`→`retry_stage`. `block_start`는 바닥선 계산용으로 유지. 블록 farming 루프 유지(후퇴분 farming, 고스테이지라 골드 수입 큼)하되 슬로그 축소. test_balance에 retry_stage 케이스 추가, run_tests PASS·웹빌드 savepack DONE. 기획서 6문서 동기화("블록 첫 복귀"→"소폭 후퇴": 스테이지 §5·게임컨셉 §1/§9·전투·UI·밸런스 초안). ✅ **②③도 같은 날(2026-06-16) 커밋·배포 완료** — 커밋 `c878daa`(②③) 이후 `67a46fb`/`3797b85`/`372fc9c`/`98cddf9`(메인퀘 트래커 개편·보상·웹 깨짐 수정·재화 아이콘 표시)까지 master 푸시, 마지막 커밋 `98cddf9`(18:39) 직후 삭제된 외부 플랫폼 `wqTVB0B5_C2e` 재배포(빌드 18:39:45). ⚠️ (기록 정정 2026-06-17: 이 항목은 작성 시점 기준 "미배포·미커밋"이었으나 직후 커밋·배포까지 완료했는데 그날 갱신을 못 했음 — 다음 날 git 대조로 바로잡음.)
- 2026-06-17: **기록 정정(어제 누락) + 유물 가루 정식 획득 리듬 = 보스 첫 클리어 (설계·구현).** ①**기록 정정**: 06-16 작업분이 실제로 전부 커밋(`98cddf9`)·배포됐는데 CLAUDE.md/메모리에 "미배포·미커밋"으로 stale 표기 → git 대조로 바로잡음(위 06-16 항목·`project_pending_tasks` 메모리·MEMORY.md). 교훈: 세션 종료 전 git status/log ↔ 기록 일치 확인. ②**유물 가루 획득 리듬 설계**: 그간 가루 순획득 루프가 없었음(치트 + 중복 환급뿐). 결정(AskUserQuestion) — **주 획득=보스 첫 클리어 / 첫 클리어만(블록 재파밍 차단) / 오프라인 가루 없음(능동만)**. 퀘스트·우편함 더미 가루는 보조 트리클로 존속. ⚠️ 가챠 비용 고정(100/1000)이라 가루는 다이아처럼 "고정 sink + 완만 수입"(골드식 기하 금지). ③**구현**: `data/src/balance.yaml relic_dust{per_boss_base 50, per_boss_step 10}`(완만 선형, 수치 더미) → `Balance.RELIC_DUST_PER_BOSS_*` + `Balance.boss_dust(stage)=base+step×(블록인덱스-1)`(비보스 0). `backend_service`: `_NUM_DEFAULTS`에 `dust_boss_max:0`(가루 지급한 최고 보스, 진행 선형이라 단일 int로 재파밍 차단·`DEATH_SETBACK` 후퇴 무관) + `grant_boss_dust(stage)->float`(보스 & stage>dust_boss_max면 지급+즉시 flush, 아니면 0). `game.gd _on_enemy_died` 보스 분기(boss_kills stat 옆, AoE/비AoE 공통 경로)에서 `grant_boss_dust` 호출 → 지급 시 `Events.currency_changed.emit()` + `NotificationManager.reward("유물 가루\n+N")`. ④**온보딩 미해결(후속)**: 가루가 보스에서만 나오면 신규 플레이어는 보스10 전까지 가루 0=유물 0 → 초반 메마름 → §5-4 "초반 유물/가루 고정 지급" 온보딩은 별도 후속으로 분리(이번엔 보스 리듬만 확정). ⑤검증: build_data errors/warns 0·`data/balance.json` relic_dust 확인·**run_tests 전체 PASS**(test_balance에 boss_dust 케이스 추가: 비보스0/선형/비감소)·웹 export savepack 컴파일 클린. ⚠️ 보스 처치 시 실제 가루 지급·토스트는 보스10 도달 필요라 MCP로 미구동 — **라이브 확인 권장**(grant_boss_dust는 autoload 의존이라 --script 테스트 불가, 로직은 boss_dust 순수함수로 검증). 기획서 `족보 스킬 시스템 정리본.md` §5-4 갱신. 수치 더미(밸런스 보류). ⚠️ 미커밋·미배포(working tree).
- 2026-06-17: **데미지 플로터 색 노랑→빨강 + 스킬 버스트 = 다타격(연타) 연출.** ①**플로터 색**(사용자): `UIPalette` 공격 플로터 노랑→**빨강 계열** — 자동 `DAMAGE_FLOATER_OUTGOING`(진한 빨강 `1.0,0.28,0.22`) / 스킬 `DAMAGE_FLOATER_SKILL`(밝은 빨강 `1.0,0.45,0.35`, 신규 상수, 구 금색 `1.0,0.85,0.2` 대체). 밝기로 자동/스킬 구분, 피격(연한 붉은색)과도 톤 구분. `effects.gd`는 상수 참조(중앙화). ②**스킬 다타격**(사용자: "데미지 플로터 다다다닥 보고싶어") — 결정(AskUserQuestion): **쪼개기(총딜 유지)** + **잭팟=무리 전체 연타**. 스킬 버스트를 한 방 → **족보 count만큼 연타**: 타수=count(페어2~나인9), 한 타=총딜/타수(누적 반올림 차분→합=총딜). `count²×value` 중 count 한 겹을 "타수"로 시각화 → **총 데미지·밸런스 불변**(밸런스 보류 준수). 구현: `game.gd _skill_multihit(targets,total,hits)`(구 `_skill_aoe` 통합·폐기) — `SKILL_HIT_INTERVAL`(0.05s) await 연타, 발동 풀이펙트는 대상별 1회·연타는 작은 임팩트+빨강 플로터, 단일=앞1마리/잭팟=무리전체. 시퀀스 동안 `_aoe_active`(개별 처치=골드·연출만) + 신규 `_skill_resolving`(자동공격·재진입 억제, `_process`/`_auto_attack` 게이트 + `_enter_idle`/`_enter_run` 안전 해제), 끝에 `_resolve_wave_after_aoe` 일괄 정리. 사망/씬이탈 시 플래그 해제 후 위임. **튜토리얼은 스크립트 단일 타격 유지**(다타격 제외). ⚠️ 부작용: 스킬 처치도 이제 AoE 경로(snap 재정렬) — 기존 단일 처치의 "PC 전진" 연출 대신 대형 snap(자동 처치는 전진 유지). 미세 차이, 거슬리면 후속. 검증: run_tests 전체 PASS·웹 export savepack DONE(game.gd 컴파일 클린). ⚠️ **라이브 전투 미확인** — MCP가 로그인 클릭/키입력 통과 불가(godot_input 타임아웃 반복, 문서화된 블로커) → 보스10·다타격·색 실기 확인은 사용자 권장. 기획서 전투 §3-3/§7-1·족보 §3 동기화. **(+후속) 플로터 겹침 버그픽스**: `_skill_multihit`이 매 타 플로터를 같은 위치에 띄워 한 방처럼 보이던 것 → 각 타에 랜덤 흩뿌림(`jitter` 가로±18/세로 -10~+4)으로 부채꼴 연타. **(+사용자 요청) 약한 적도 연타 끝까지 보여주고 마지막 타에 처치**: 마지막 전 타는 `applied = min(hit_dmg, current_hp-1)`로 HP 최소 1 남김(클램프) → 첫 타 즉사 방지, 마지막 타에서만 사망(총딜 유지=오버킬은 원래 낭비). 튜토리얼은 단일 타격이라 무관.
- 2026-06-17: **디버그+치트 빌드로 삭제된 외부 플랫폼 배포(사용자 명시 요청 — "치트 킨 상태로 배포").** 위 3건(가루·플로터·다타격)을 라이브 테스트하려고 **치트 활성 빌드** 업로드. 치트 생성 게이트가 `CHEAT_ENABLED and OS.is_debug_build()`라 **디버그 빌드**여야 치트가 삼 → `build_web.ps1`(기본 --export-debug, flavor=debug) + `삭제된 배포 스크립트 -SkipBuild -AllowCheats -AllowDebug`(치트·디버그 공개차단 둘 다 강행). 삭제된 외부 플랫폼 `wqTVB0B5_C2e`("복권 키우기") 덮어쓰기 성공(v=1781664069, zip 34.9MB — debug wasm이라 큼). `CHEAT_ENABLED=true` 유지(이번엔 끄지 않음). ⚠️ **이 라이브 빌드는 치트가 전원 노출**(테스트용) — 정식 공개 시 release(치트 off)로 재배포 필요. ⚠️ 에디터 열린 채 빌드라 godot-mcp autoload가 pck에 잔존(콘솔 "File not found" 무해). ⚠️ **배포한 코드가 아직 미커밋**(working tree — 라이브 빌드가 어떤 커밋과도 대응 안 됨, 테스트 후 커밋 예정). **(+곧바로) 사용자 테스트 확인 후 정식 release 재배포**: `CHEAT_ENABLED=false` → `build_web.ps1 -Release`(flavor=release) → `삭제된 배포 스크립트 -SkipBuild`(치트·디버그 강행플래그 불필요 = 게이트 통과로 치트 off·release 확정) → 삭제된 외부 플랫폼 `wqTVB0B5_C2e` 덮어쓰기(v=1781664733, zip 34.4MB). 배포 후 `CHEAT_ENABLED=true` 복원(dev 관례). **라이브 = 치트 off release.** 코드는 여전히 미커밋(working tree, CHEAT_ENABLED=true). **(↑ 이후 96b9e71로 커밋·푸시 완료.)**
- 2026-06-17: **유물 가루 잔액 패널 실시간 갱신 버그픽스.** 가루는 HUD에 없고 패널(`_dust_label`)에만 표시되는데, `shop_panel`(유물 소환)·`relic_panel`(유물 관리) 둘 다 `Events.currency_changed`를 구독 안 해서 보스 처치 등 외부 가루 변동이 패널 열린 채로는 반영 안 됐음 → 둘 다 `Events.currency_changed.connect` 추가(열려 있을 때만 가루 라벨 갱신, 목록은 재화 무관이라 재구성 안 함). **다이아는 패널 표기 없이 HUD에만 있고 HUD는 이미 currency_changed 구독 중이라 원래 실시간**(수정 불필요 — 사용자 추측과 달리 정상). autoload→씬노드 연결이라 free 시 자동 해제. 웹 export 컴파일 클린(real_errors 0). ⚠️ 미커밋·미배포(working tree). ⚠️ 라이브 미확인(MCP 로그인 블로커) — 보스 처치 시 패널 가루 즉시 갱신은 사용자 실기 확인 권장.
- 2026-06-17: **스크래치 칸 완성 = 가운데 영역 조건 추가(OR).** 사용자: "숫자가 다 보이는데도 완성 안 됨"(숫자는 칸 가운데에 그려지는데 완성 임계는 칸 전체 70% 기준이라 모서리·여백까지 안 긁으면 미달). → 기존 70%(`completion_threshold`) 유지 + **OR `center_complete_ratio`(기본 0.5=칸 중앙 50% 정사각) 영역이 다 긁히면 완성**. `scratch_card.gd`: `_compute_center_indices`(중앙 영역 서브셀 인덱스 1회 계산, `_build_cells`)·`_center_cleared(c)`(그 서브셀 전부 비가시면 true)·`_scratch_at` 완성 판정에 OR 추가. **수동 긁기에만 적용**(자동 긁기는 `scratched_count=sub_total`로 칸 전체를 한 번에 벗겨 무관 — 사용자 확인). 브러시 반경(30) > 중앙 영역이라 가운데 한 번 긁으면 클리어 → "숫자 보이면 완성". 튜닝: 잘 안 잡히면 ratio 낮춤(영역 작아져 쉬움)/너무 쉬우면 올림. 웹 export 컴파일 클린(real_errors 0). 기획서 [[스크래치 시스템 정리본]] §3-4/§9 동기화. (디버그 시각화: 가운데 영역 반투명 패널 임시 표시로 사용자 확인 후 제거.) **커밋 `ab4f4ca`(스크래치 가운데 조건 + 유물 가루 패널 실시간 갱신) master 푸시 + release 배포**(`CHEAT_ENABLED=false`→`build_web.ps1 -Release`→`삭제된 배포 스크립트 -SkipBuild`→삭제된 외부 플랫폼 `wqTVB0B5_C2e` v=1781673638→CHEAT on 복원). 라이브=치트 off release. ⚠️ 가운데 완성 조건·패널 가루 실시간 갱신은 라이브 미확인(MCP 블로커) — 사용자 실기 확인 권장.
- 2026-06-17: **기획서 전수 감사→패치→적대 검증 (울트라코드 워크플로우 3단).** 사용자 "기획서 포함 모든 문서작업 안된 거 확인". ①**감사**(20 기획서 1에이전트씩 병렬, 코드 직접 대조 + 이력/TBD/더미 거짓양성 제외): 11 stale / 9 current / 20 findings. ②**패치**(워크플로우 args 직렬화 실패 → 직접 적용): 11 문서 정정 — UI(플로터 노랑/금색→빨강·`DAMAGE_FLOATER_SKILL` 신규·스킬 다타격 `_skill_multihit`·칸완성 70% OR `center_complete_ratio`·`lock_duration` 완전제거 반영), 게임컨셉/밸런스/스테이지/재화정책/전투(유물 가루=보스 첫 클리어 정식 획득 `Balance.boss_dust`/`grant_boss_dust`/`dust_boss_max`), BM(유물 소환=상점>소환>유물 구현), 사운드(보스 가루 reward 행 + **코드: `_on_enemy_died` 보스 분기에 `Audio.play_sfx("reward")` 추가** = 다른 보상과 일관), 설정(버튼 텍스트→아이콘), 튜토리얼(2단계 잭팟 컷인), 퀘스트(`GameData.table("quests")`→`quests_main`/`quests_daily`·`quests.yaml`경로·버튼 좌→우·일일 7종/노출6종). ③**적대 검증**(11 패치문서 병렬 재대조): 7 clean / 5 issue → 전부 수정(BM `order`→`pool_order`, 게임컨셉 퀘 카탈로그 메인8+일일5→**메인25+일일7(노출6)**, 재화정책 `quests.gd`→데이터 카탈로그 2곳, 퀘스트 §4-1 메인 시퀀스 19개→**25개**[빠른12+장비소환1+스테이지10(stage_reached 5~60)+보스2, 유물소환퀘 제거]·relic_pulls `main_relic_pull_1` 참조 제거). 검증으로 내가 놓친 퀘스트 카탈로그 규모변화(2026-06-16분) 포착. 웹 export 컴파일 클린(real_errors 0, 사운드 코드변경 포함). ⚠️ 미커밋(working tree — 기획서 11 + game.gd + CLAUDE.md). 문서만(docs/는 빌드 제외)이라 라이브 무관.
- 2026-06-17: **다타격 웨이브 버그픽스 2건 (사용자: 고족보 처치 후 PC 전진 모션 안 나옴·적 안 죽는 듯).** ①**`_resolve_wave_after_aoe()` 이중 호출 제거** — `_skill_multihit` 끝에 `_resolve_wave_after_aoe()`가 두 번 있었음(편집 중 생긴 중복). 웨이브를 두 번 정리 → 대형/다음 적 상태 꼬임(처치/전진 이상). ②**고족보/잭팟 처치 후 PC 전진(run) 복원** — 다타격은 AoE 경로라 앞 적 사망 시 `_snap_wave_positions`로 대형을 텔레포트(전진 모션 없음)했음 → `_resolve_wave_after_aoe`가 **재할당 전 옛 앞 적 생사(`front_alive`) 판정**해서, **앞 적 죽음 = `_enter_run(false)`로 다음 적까지 PC 전진**(일반 처치와 동일), 앞 적 생존(부분 타격) = 기존 snap+교전+새 카드. 웹 export 컴파일 클린(real_errors 0). ⚠️ 미커밋·라이브 미확인(MCP 블로커) — 고족보 웨이브 처치 시 PC 전진 모션·다음 적 정상 등장 사용자 실기 확인 권장.
- 2026-06-17: **신화 돌파/별(초월) + 장비 전체 합성 (사용자 결정·구현).** ①**신화 중복 처리 = 돌파/별**(AskUserQuestion): 신화는 합성으로 올릴 등급이 없어 중복이 죽은무게 → **자동 흡수 → 별(stars) +1**(클러터 0, 유물 자동환급 철학). 같은 종류 신화 보유 중 신화 추가(가챠·합성 결과 공통) → 새 인스턴스 대신 기존 신화 `stars+1`. 보너스 = `base×(1+per_lv×lv)×(1+star_bonus_per×stars)`. 구현: `equipment.yaml star{bonus_per 0.20, cap 0=무제한}` → `Gacha.star_bonus_per/star_cap/is_top_grade` + `equip_bonus(grade,level,stars=0)` + `backend.add_equip`(신화면 `_find_top_grade_inst`로 흡수)·`equip_bonus`/`equipped_bonus`가 inst.stars 전달·`get_equip_stars`. 세이브 = 신화 인스턴스에 `stars` 키(하위호환 `get("stars",0)`, 마이그레이션 불필요). UI = 인벤토리 "N성"(★은 번들폰트 미존재 웹깨짐 → 한글). game 전투는 `equipped_bonus` 경유라 자동 반영(무변경). ②**장비 전체 합성**(사용자: 누를 때마다 1종류 1회 → 전부 한 번에): `backend.combine_all_equips(pool)` = 저등급→고등급 cascade(common 합성으로 생긴 rare도 이어서), `_combine_one`(영속화 없음) 반복 후 **1회 flush**(삭제된 외부 플랫폼 과호출 방지). `combine_equips`는 `_combine_one`+flush 래퍼로 유지. equipment_panel 버튼 "합성"→"전체 합성", 토스트 "합성 N회 완료 → 등급별". 검증: build_data errors 0(star 블록)·**run_tests 전체 PASS**(test_gacha에 별 보너스·is_top_grade 케이스 추가)·웹 export real_errors 0. 기획서 `장비 시스템 정리본` §5/§5-1/§7/§8/§9 갱신(신화 중복 TBD 해소). ⚠️ 미커밋·라이브 미확인(MCP 클릭 불가 — 전체 합성·신화 별 흡수·N성 표기 사용자 실기 확인 권장). 수치 더미(밸런스 보류).
- 2026-06-17: **유물 무료 확정 소환 + 가벼운 온보딩(red dot) (사용자 결정·구현).** 가루가 보스에서만 나와 신규는 보스10 전까지 유물 0이던 메마름 해소. 결정(AskUserQuestion): **확정 소환=무료 보장 지급 / 온보딩=별도 가벼운 가이드(강제 튜토리얼 아님)**. ①**무료 확정 소환 = 데이터 주도 사전 설정 유물, 첫 보스 처치 후 해금**(사용자: 랜덤 아니라 정해둔 유물 + 시작 직후가 아니라 첫 보스 후 — `backend._relic_free_unlocked`=`boss_kills>=1`, `relic_free_pulls_left`가 그 전엔 0): `relics.yaml free_pull{effects:[catalog키...], grades:[등급...]}`(병렬 인라인 리스트 — 파서가 dict 리스트 미지원, 기존 `catalog_order` 패턴; 무료 횟수=리스트 길이; 현재 `[r_dist_low]/[rare]`). `Relics.free_pull_count`(=effects 길이)/`free_pull_grant(index)`(사전 설정 {effect,grade,name,kind}, 범위밖 {}, grades 짧으면 _default_grade) + `backend.relic_free_pulls_left/next_free_relic_grant/do_free_relic_pull`(`relic_free_used` 인덱스로 순서 지급, 즉시 flush). build_data 가드레일(effects=catalog키·grades=grades키·길이일치·null/스칼라 방어). UI=상점>소환>유물 "무료 확정 소환" 버튼 + 안내("무료 [등급] 이름 확정" = 다음 지급 유물, `next_free_relic_grant`)(`shop_panel._do_free_relic_pull`, 남은 횟수 있을 때만 노출, `_refresh_relic_costs`에서 토글, 결과 팝업 공용). ②**가이드(red dot)**: `nav_dock.set_red_dot(id,on)` 신설(탭 버튼 우상단 RedDot) + `game._update_nav_guide_dots`/`_has_unequipped_relic`(`_refresh_hud`에서 갱신, 튜토리얼 중 숨김) — **무료 소환 대기→상점 탭 / 미장착 유물(빈 슬롯)→유물 탭**. 장착하면 사라짐. 검증: build_data errors 0(free_pull)·run_tests 전체 PASS(test_relics에 free_pull/roll_free 케이스)·웹 export real_errors 0. 기획서 `족보 스킬 시스템 정리본` §5-4 갱신(온보딩 TBD 해소). ⚠️ 미커밋·라이브 미확인(MCP 클릭/로그인 불가 — 무료 소환 버튼·red dot·장착 유도 사용자 실기 확인 권장). 수치 더미. ⏸ 현재 1개(`free_pull` 리스트에 항목 추가 = "초반 5개"로 확장)·안내 토스트 미도입(red dot 갈음). **(+적대 리뷰 4렌즈 → 수정 4건)**: [major] shop 안내 라벨 가운데점(·)→공백(웹 폰트 깨짐, 반복 이슈 또 유입) / [minor] `cheat_controller._wipe`에 `relic_free_used`·`dust_boss_max` 리셋 추가(와이프 후 유물0인데 무료소환/보스가루 사라지던 잠복) / [minor] 튜토리얼 시작 시 상점 red dot 켜지던 타이밍 → `_set_tutorial_ui_lock`이 `_update_nav_guide_dots` 호출 / [nit] build_data free_pull 가드레일 null/스칼라 입력 방어. 스킵: write-before-validate(모든 validator 공통 기존 패턴·런타임 안전·exit1). 재검증 build_data 0·run_tests PASS·웹 real_errors 0.
- 2026-06-17: **우편 가루 지급 제거 + 상점 재화 탭 = 유물 가루 구매(다이아) (사용자 요청).** ①**우편 더미 가루 제거**: `mailbox.gd` `mail_20260611_test_reward` 보상에서 `dust 5000` 줄 삭제(gold/dia만 유지, mail id 불변·화이트리스트 dust 잔존). 가루는 보스/상점/중복환급/퀘스트로만. ②**상점 > 재화 = 유물 가루 구매(일단 다이아 결제, placeholder)**: 빈 "준비 중"이던 재화 카테고리 구현. 데이터 `relics.yaml dust_shop{amounts:[100,1000], costs:[100,1000]}`(병렬 인라인 리스트, 잠정 1:1) → `Relics.dust_shop_count/dust_shop_pack(index)` + `backend.buy_dust_with_dia(가루,다이아)`(spend_dia→add_dust→flush) + `shop_panel` 재화 뷰(`_build_currency_view`/`_build_dust_pack_row`/`_refresh_currency`/`_buy_dust`, 보유 다이아·가루 표시 + 구매 팩 행[다이아 아이콘 비용+구매 버튼], `_refresh_view`에 currency 분기, `_on_currency_changed`도 갱신). build_data 가드레일(dust_shop amounts/costs 인라인·길이일치·양수). 검증: build_data 0·run_tests PASS(test_relics dust_shop 케이스·test_mailbox 통과[가루 제거 무영향])·웹 real_errors 0. 기획서 재화정책(§1/§5-2/sink표)·BM(§4)·UI(§3 ShopPanel)·우편함(예시) 동기화. ⚠️ 미커밋·라이브 미확인. 결제 통화(다이아)·환율(1:1) 잠정 — 추후 재검토.
- 2026-06-18: **유물 접근 잠금(스테이지 10) + 튜토리얼 단계별 내비 탭 음영 + '모험'→'던전' 개명 + 던전 설계 확정.** ①**유물 잠금**(사용자 요청): 첫 보스(스테이지 10) 처치 전 유물 진입 차단 — 내비 유물 탭 + 상점>소환>유물 서브탭 둘 다 **음영(`nav_dock.set_locked` modulate, 누름 통과) + 진입 시도 시 통일 토스트** `NotificationManager.info("스테이지 10을 클리어하면 열립니다")`. `backend.relic_unlocked()`(=`boss_kills>=1`) 신설, 게이트=`game._on_nav_tab`·`shop_panel._select_sub`. ②**튜토리얼 탭 음영/강조**(`game._update_nav_tab_locks`): SCRATCH/JACKPOT=전탭 음영 / UPGRADE=강화만 해제+깜빡임 / OFF=유물 제외 해제. 버그픽스: `_on_nav_tab` 가드가 튜토리얼 내내 강화 탭을 통과시켜 1·2단계 조기 패널오픈 → UPGRADE 단계 강화 탭만 통과로 좁힘. 컴파일 0(사용자 라이브 "잘 됨"). ③**'모험'→'던전' 개명**: 표시명+id `adventure`→`dungeon`(주요 컨텐츠 예정), 주석·기획서 5곳. ④**던전 설계 확정(다각도 워크플로 12에이전트: 코드통합·경제·차별화·장르패턴 분석 → 설계안 4종 → 심사 3인 만장일치 "능동 보스 사다리" + 타안 강점 이식)**. 결정 = **"시련의 탑"** = 메인 방치 등반과 별개인 **능동 보스 사다리**(같은 스크래치/족보, 보스마다 다른 **룰**[hand_gate 족보게이트·time_limit·modifier·token_limit]을 빌드로 풀어 깸). **차별화 = 메인은 방치라 룰 못 검 / 던전은 능동이라 룰 가능 → 단일 전투력벽 vs 룰별 빌드벽.** 경제(사용자 결정): **클리어=완료(재입장 불가)→반복 채널 제거→무한파밍 구조적 불가**, 보상=첫 돌파 1회성 다이아·가루(`grant_boss_dust` 패턴 복제, 단일 int `dungeon_max_cleared` 게이트)로 **다이아·가루 획득 공백 해소**. 다이아=10층까지만(맛보기). 입장 무제한(현 벽 층 무제한 재시도)·스태미나 없음·실패=입구(후퇴 없음). 아키텍처=별도 씬 X, `game.gd enum Mode{MAIN,DUNGEON}` 분기 3곳(스폰/처치/사망)+룰 훅(핫패스 회귀 최소화). **사용자 결정: MVP 10층 / 룰 4종(plain·hand_gate·time_limit·pc_atk_down) / 난이도 초반완만(페어)→후반강화 / 진입=밴드 패널+아레나 모드전환.** 기획서 **`docs/design/던전 시스템 정리본.md` 신설**(전 섹션 + 구현 로드맵 Phase0~4 + 파일 앵커), UI §3 NavDock 포인터. ⚠️ **코드 미착수**(설계만, 검토 후 구현). 수치 전부 잠정. ⚠️ 미커밋. [[project_dungeon_system]].
- 2026-06-18: **던전 "시련의 탑" MVP 풀스택 구현 (위 설계 → 같은 날 구현).** 사용자 "구현 들어가고 + 던전 몬스터 빨강 틴트(보스처럼)". 로드맵대로 데이터→백엔드→전투 모드 분기→UI 전면 구현. ①**데이터**: `data/src/dungeon.yaml`(10층, stage_equiv/hp_mult/룰[hand_gate/time_limit/pc_atk_down]/보상[dia·dust], 정렬=파일순서) → `build_data` "dungeon" 검증 케이스 → `data/dungeon.json` → `scripts/dungeon_data.gd`(class_name DungeonData, autoload 무참조: floor_count/stage_equiv/boss_hp[=base_enemy_hp×BOSS_HP_MULT×hp_mult]/boss_attack/rule_text). `balance.gd base_enemy_hp`(보스배수 미적용) 신설. ②**백엔드**: `_NUM_DEFAULTS` `dungeon_max_cleared:0` + `get_dungeon_max_cleared`/`grant_dungeon_clear(floor)`(grant_boss_dust 패턴 복제 — floor<=prev면 {} 재파밍 차단, 첫 돌파만 dia·dust + flush). cheat wipe 리셋 추가. ③**전투**(game.gd): `enum Mode{MAIN,DUNGEON}` + 멤버(_mode/_dungeon_floor/_dungeon_time_left/_dungeon_hand_gate/_dungeon_pc_atk_mult). 함수 9개(_enter/_exit_dungeon·_spawn_dungeon_boss·_dungeon_clear/_fail·_return_to_dungeon_panel·_update_dungeon_hud·_reset_combat_flags·_on_dungeon_enter_requested). 분기 8곳(_process 타이머·_spawn_next·_on_card_completed[hand_gate 무효+잭팟골드 메인전용]·_auto_attack[hand_gate 봉인+pc_atk_down 배수]·_on_enemy_died[골드/통계/보스가루 메인전용]·_finish_wave_clear[승리 단일 chokepoint→_dungeon_clear]·_on_player_died[→_dungeon_fail]·_refresh_hud[던전 배너]). **던전 몬스터 빨강**: `enemy.gd @export tint_override`(보스 보라 틴트보다 우선) — 던전 보스=is_boss(스케일1.8)+빨강. ④**UI**: `scripts/dungeon_panel.gd`(밴드 패널, 층 리스트 완료✓/도전/잠금+룰+보상, "도전"→`Events.dungeon_enter_requested.emit`) + 내비 "던전" 탭→`UIManager.open`. ⑤**테스트** `tools/tests/test_dungeon.gd`(run_tests 등록). **검증**: build_data errors 0·**run_tests 전체 PASS**(test_dungeon)·웹 export savepack(game.gd 컴파일 클린)·클린 부팅. **적대적 리뷰(6렌즈 37에이전트) → 확정 3건 수정/28건 기각**: 전부 **await 시퀀스(_skill_multihit 다타격) 중 모드 전환이 코루틴을 어긋난 상태로 재개시켜 상태 손상**류 → ①_on_dungeon_enter_requested 핫패스 플래그 가드(다타격 await 창에 "도전" 차단) ②_process time_limit 가드에 `not _skill_resolving`(자동공격 분기와 대칭) ③_skill_multihit `entry_mode` 캡처+재개 가드 `_mode != entry_mode`(PC 사망發 fail은 _process 밖이라 ①②로 못 막음). ⚠️ **교훈: 모드 전환은 await 핫패스와 겹칠 수 있어 (a)진입 플래그 가드 (b)타이머 가드 대칭 (c)코루틴 재개 세대(mode) 가드 3중 방어.** ⚠️ **중간 함정**: 새 class_name(DungeonData) 추가 시 에디터 stale 글로벌 클래스 캐시로 game.gd 가짜 파싱에러("함수 not found in base self") → **웹 export(리임포트) 후 해소**(디스크·헤드리스는 정상, 에디터 stop/run만 stale). build_data "dungeon" 케이스 1탭 과들여쓰기로 첫 변환 실패 → de-indent. MVP 룰=plain/hand_gate/time_limit/pc_atk_down(token_limit/no_jackpot/only_number·무한꼬리 11층~=2차). 수치 전부 잠정(밸런스 보류). ⚠️ **라이브 전투 미검증**(MCP 로그인 블로커 — 사용자 플레이로 빨강 보스·룰·클리어/실패·HUD 확인 권장). ⚠️ 미커밋. 기획서 `던전 시스템 정리본.md`(✅구현/리뷰 반영)·[[project_dungeon_system]] 갱신.
- 2026-06-18: **유물 접근 잠금(스테이지 10 전) + 튜토리얼 단계별 내비 탭 음영 (사용자 요청·구현).** ①**유물 잠금**: 가루·무료소환이 첫 보스(스테이지 10) 후에야 의미 있으므로 그 전엔 유물 진입 차단(빈 UI 노출 방지). 잠금 2곳=**내비 유물 탭 + 상점>소환>유물 서브탭**, 둘 다 **음영 + 진입 시도 시 통일 토스트** `NotificationManager.info("스테이지 10을 클리어하면 열립니다")`. `backend.relic_unlocked()`(=`boss_kills>=1`, `_relic_free_unlocked`와 단일 기준으로 통일) 신설. `nav_dock.set_locked(id, locked)`(modulate 음영, `disabled` 아님 → 누름 통과해 토스트 발동). 게이트=`game._on_nav_tab`(유물 탭)·`shop_panel._select_sub`(서브탭, 잠겼으면 토스트 후 장비 유지)+`_refresh_view`에서 서브탭 음영. 보스 처치 시 `_refresh_hud`→음영 자동 해제. ②**튜토리얼 단계별 탭 음영**: `game._update_nav_tab_locks`(`_update_nav_guide_dots`→`_refresh_hud`/잠금 훅에서 호출) — SCRATCH/JACKPOT=모든 탭 음영 / UPGRADE=강화만 음영해제+깜빡임강조(blink가 modulate 소유)·나머지 음영 / OFF=강화·장비·모험·상점 해제, **유물만 스테이지 10 게이트로 음영 잔존**. **버그픽스**: 기존 `_on_nav_tab` 가드가 튜토리얼 내내 강화 탭을 항상 통과시켜 SCRATCH/JACKPOT에 강화 탭 누르면 패널이 조기 오픈되던 구멍 → **UPGRADE 단계의 강화 탭만** 통과로 좁힘(음영과 동작 일치). 검증: 에디터 stop/run 재시작으로 fresh 로드 파싱 에러 0(사용자 라이브 확인 "잘 됨"). ⚠️ 웹 빌드는 에디터 6550 점유로 보류(닫고 빌드 or 배포 시). 기획서 튜토리얼 §3·§4·§5, 족보 §5-4, UI §3(NavDock/ShopPanel) 동기화. ⚠️ 미커밋.
- 2026-06-18: **던전 데일리화 + 폴리시 (라이브 플레이 반복 피드백 → 연속 수정).** 던전 MVP 위에 사용자 플레이 피드백으로 다수 변경: ①**30층 확장 + 난이도 완화**: 10층(8→180 stage_equiv, 층당 5~350배 점프) → **30층**(층당 +3~10 = HP 1.6~5배, hp_mult 1.25~1.8). `dungeon.yaml` 재작성, test_dungeon 매핑 assertion 갱신. ②**던전 스테이지10 잠금(유물과 동일)**: `backend.dungeon_unlocked()`(=boss_kills>=1) + `_on_nav_tab`·`_update_nav_tab_locks` 게이트(음영+토스트). ③**hand_gate = 스킬만 게이트(자동공격 복구)**: 기존 hand_gate가 자동공격까지 봉인 → 사용자 요청으로 **기본공격은 동작**, 족보 게이트는 스크래치 스킬에만(`_auto_attack` 게이트 제거, rule_text "스킬은 족보 N+ 만"). ④**던전 진입 이동 연출**: 전체화면 페이드(검정)+"시련의 탑 N층 / 이동 중…" 짧은 로딩(`_dungeon_travel_cover/_reveal`, CanvasLayer 70, 가려진 동안 모드/적 교체). ⑤**던전 전투 중 HP 회복 없음 + nav 탭 잠금(결정 C)**: 보스전 규칙(`in_boss`)이라 강화/장비로 최대HP만 늘고 현재HP 미충전(입장=풀피, 치즈 방지). 전투 중 메뉴 탭 전부 음영+차단("던전 전투 중에는 사용할 수 없습니다"). ⑥**데일리 던전화(사용자 결정 = 입장 무제한 기둥 뒤집음)**: **입장권**(일일 무료 10개[잠정], 자정 리셋 use-it-or-lose-it, `backend.dungeon{tickets,date,auto}` 컨테이너·roll_dungeon_tickets_if_needed, 광고/IAP 충전은 광고SDK 후) + **반복 보상**(클리어 층 재도전 = 골드[stage_equiv 스케일]+소량 가루[첫돌파×0.2], `grant_dungeon_repeat`, 입장권 게이트라 무한파밍 X) + **정식 결과 모달**(`_show_dungeon_result`: 층·소요시간·보상카드·다음층/재도전/나가기, 첫돌파 dia/dust vs 재도전 골드/가루 분기) + **자동 등반**(`backend.get/set_dungeon_auto`). `balance.yaml dungeon{daily_tickets, repeat_gold_mult, repeat_dust_ratio}`. ⑦**자동 등반 = 기본 상시 ON, 끄기는 설정에만(사용자 결정)**: 던전 패널 토글 제거(읽기전용 상태표시만) → 설정 패널 "던전>자동 등반" 체크. 입장 시 실패/최고층/입장권소진 전까지 자동 진행(클리어→결과모달 없이 토스트+바로 다음 층, 연출=이동전환). 멈춤=결과모달은 정지 시에만. **기본값 ON 1회 마이그레이션**: 구 세이브 stale `auto:false` → `auto_init` 마커 없으면 강제 on(이후 설정 OFF 유지). ⑧입장 가드 완화: "잠시 후 다시 시도" 토스트가 자동공격(`_auto_pending`)마다 떠서 → 가드를 `_skill_resolving`/`_cutin_active`(await 시퀀스)로만 좁힘. **검증**: build_data 0·run_tests 전체 PASS(test_dungeon)·웹 export 컴파일 클린·**적대 리뷰 2회**(MVP 6렌즈 37에이전트 → 확정3 수정[await시퀀스 중 모드전환 가드 3중] / 데일리 6렌즈 42에이전트 → 확정1[패널 스크롤 보존] 수정·35기각). ⚠️ **수치 전부 잠정**(입장권10·층 곡선·반복배율·룰). ⚠️ 라이브 검증=사용자 플레이(MCP 로그인 블로커). ⚠️ **교훈 반복**: 새 class_name(DungeonData)·대량 편집 시 에디터 stale 글로벌클래스/컴파일 캐시로 game.gd 가짜 파싱에러("함수 not found"/"too many args") → **웹 export(리임포트) 후 해소**(디스크·헤드리스 정상, 에디터 stop/run만 stale). 기획서 `던전 시스템 정리본`(데일리 모델 반영)·UI·튜토리얼·족보 동기화. [[project_dungeon_system]].
- 2026-06-19: **포즈투포즈 공격 연출(기본값) + 빠른 이동감(호흡 불변) + 레퍼런스 콘텐츠 분석 문서 (커밋·배포).** ①**포즈투포즈 공격**(`player_controller.gd`): attack 시트에서 핵심 포즈 3장(준비 15%/타격 55%/마무리)만 샘플 → 타격 프레임 HOLD(=히트스톱 대체, `time_scale` 미사용) + 타격순간 슬래시 호+코어플래시 + 스윙시작 휘두름 궤적(스워시·잔상 2겹). `effects.gd spawn_slash`/`spawn_swing_trail`(절차적, 에셋0). `_build_attack_pose`(attack 프레임 재사용 `attack_pose` 애니 빌드)·`frame_changed`로 임팩트 슬래시 발동·`animation_finished`→기존 `attack_motion_completed` 계약 그대로(전투 로직·데미지 타이밍 무변경). **`pose_mode` 기본 true로 채택**, 치트 "공격 모션" 토글로 옛 풀프레임 방식과 비교(dev·release엔 치트 없음=항상 포즈투포즈). ⚠️더미=기존 프레임 샘플(정식은 강한 키포즈 새로 그려야 진가). 노브=`player_controller` POSE_* 상수·`effects _swing_arc`. ②**빠른 이동감(호흡 불변)**: run 시간 = 이동거리/속도(고정 타이머 없음, 앞 적이 STOP_X 도달까지 흘러옴) → `PlayerData.RUN_SPEED 150→225`·`game.gd WAVE_SPACING 70→105`·`EnemyData.SPAWN_OFFSET_FROM_RIGHT 50→145`를 **같은 배율 K=1.5로 묶어** 두 run거리(웨이브내=WAVE_SPACING·진입=arena360+offset−STOP_X220) 둘 다 ×1.5 → 이동 '시간'(=전투 재개 타이밍) 정확히 불변, 화면 이동·간격·배경스크롤만 1.5배 빨라짐. + run 애니 `12→18fps`(발 미끄러짐 완화, 시각 케이던스라 호흡 무관). ⚠️세 값은 항상 같은 K로 묶어야 호흡 보존(주석 명시). ③**레퍼런스 콘텐츠 분석 문서**: `docs/design/레퍼런스 게임 분석 모음.md` 신설(콘텐츠 모드 전용, 슬레이어/메이플). 웹조사 신뢰도 한계(나무위키 403·패치 변동, 1차 메이플 표가 실제와 다수 불일치) 확인 → **실플레이 골격으로 전환**(메모리 `feedback_reference_research` 신설). 메이플 미도달 구간(파티퀘스트/아레나/보스레이드)만 워크플로 웹 3각조사(공식뉴스/공략/커뮤니티)+교차검증 → "🔒 미확인(웹 추정)" 격리 표(신뢰도·해금조건·출처). ④검증: 웹 export 컴파일 클린(EXIT 0). ⚠️라이브 시각 검증 미완(MCP 로그인/인게임 블로커) — 사용자 플레이로 확인 권장. **커밋·푸시·release 배포(치트 off).** **(+후속)** Godot 웹 기본 로딩바(`#status-progress`) 제거 — 테마색 슬림 바 스타일 시도했으나 템플릿 레이아웃과 충돌해 이상 위치로 떠서, `export_presets.cfg head_include`에 `#status-progress{display:none!important}`로 숨김(부트 스플래시 bg_main만 유지). 별도 커밋·치트 켠 debug 재배포(테스트용).
- 2026-06-25: **무기 분리 브랜치 master 병합 + 전체 코드 리팩토링 패스(동작 불변).** ①`feature/weapon-separation`(무기-PC 분리 1차 `cb9f345` + 보조 `aa97f4b`[AGENTS 포즈 가이드·login/main.tscn uid재직렬화·구조정리 문서])을 master로 fast-forward → origin/master 푸시. 스크래치(루트 실험PNG·output·concepts·old)는 미추적 유지. ②**리팩토링 = 울트라코드 워크플로**(감사 9영역[game 전/후·backend·패널2·전투·데이터·일관성·죽은코드] 저동시성 웨이브 + 발견별 적대 검증[실재·동작불변·가드레일]). raw 33 → **apply_safe 15 / recommend_only 17 / reject 1**. ⚠️1차 시도는 9에이전트 동시 spawn이 서버 rate-limit에 걸려 전멸 → 감사 3·검증 4 웨이브로 재실행 성공. **적용 15건(전부 byte-identical 추출·미참조 죽은코드, 1px·1프레임·1바이트 불변)**: `game.gd`(`_dungeon_result_button`·`_result_center_label`·`_halt_dungeon_combat`·`_reset_pending_attack_flags` 헬퍼 추출[던전 결과 모달 3버튼·5라벨, 전투 정지 2경로, pending/AoE 플래그 6줄×3곳] + 미참조 const `DUNGEON_RESULT_AUTO_DELAY` 삭제), `backend_service`(`_default_dungeon` 던전 리터럴 dedup[⚠️비-const — Godot4 const 컨테이너 read-only 전파 트랩 회피]·`grant_currencies` 보상가산 인스턴스 헬퍼), `ui_skin`(`reward_toast` static), `panel_overlay_base`(`_status_badge`·`_reward_summary_row` 베이스로 끌어올림), 패널 dedup(우편/퀘스트패널/트래커의 `_show_reward_toast`·`CURRENCY_NAME`/`CURRENCY_COLOR`·보상가산 루프 → UISkin/베이스/BackendService 공용 / shop `_make_dia_cost_icon`·`_shop_toast`[가루 토스트 DUST_COLOR 1곳 제외] / relic `_refresh_dust_label`). ⚠️`panels-b#4`↔`consistency#2`(동일 배지/보상행 중복, 제안 위치만 상이)는 **panel_overlay_base 방식으로 단일화**(호출부 무변경, consistency#2 스킵). **검증: 웹 export `savepack DONE`(전 스크립트 컴파일 0 에러, godot-mcp 애드온 노이즈[6550 점유·`get_frame_source`]만)·`run_tests` 9스위트 전체 PASS·MCP `main.tscn` 직접실행 런타임 에러 0·정상 렌더(HUD·트래커·전투·내비·스크래치)**. **+ recommend_only 안전 2건 추가 적용**(사용자 "일부 진행": `game.gd _make_icon_texrect`[아이콘 TextureRect 셋업 dedup, game-1#3]·`shop_panel _make_title_eff_row`[소환행 제목/효과 라벨 dedup, panels-a#3]) → **총 17건 적용**(웹 export 재검증 컴파일 0). **나머지 15건 미적용**(god object 분리[game.gd HUD빌더·backend 도메인]·gacha↔relics `roll_many`·shop 소환 함수 골격·모달 스캐폴드 등 — 구조적/저가치, 사용자 판단 대기). reject 1 = `_build_hp_bar` 호출 주석처리(의도적 보존). **master 직접 커밋**(사용자 결정). 상세 [[project_refactor_progress]].
